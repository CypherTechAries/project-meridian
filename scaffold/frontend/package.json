{
  "name": "meridian-prototype-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "MERIDIAN C0 interactive prototype — fixture data only, not connected to the simulation engine.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "screenshot": "node scripts/screenshot.mjs"
  },
  "devDependencies": {
    "@vitest/ui": "^3.2.4",
    "jsdom": "^26.1.0",
    "playwright": "^1.56.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^3.2.4"
  }
}
