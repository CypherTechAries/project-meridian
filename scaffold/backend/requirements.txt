# Core web
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.1,<3.0

# Simulation engine (Apache-2.0)
mesa>=2.1,<3.0
networkx>=3.2,<4.0

# Real-time
python-socketio>=5.11,<6.0
websockets>=12.0

# Database
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0

# LLM gateway (stubbed by default; installed so the interface is real). MIT-licensed.
litellm>=1.34,<2.0

# Tests
pytest>=8.0,<9.0
httpx>=0.27,<1.0
