## Democracy 4 (Positech Games, Cliff "Cliffski" Harris, 2020 EA / 2022 release)
ONELINE: Democracy 4 is the only widely-shipped commercial product that makes an entire simulation's causal graph the primary navigational surface rather than a debug view — and its documented failure modes (a search bar retrofitted onto the graph, "difficult to feel confident in your strategy", no traceability of long chains) are precisely the failure modes MERIDIAN's Eight Questions requirement will hit.
  FAIL[The graph became unnavigable at scale — and the fix was a search box] Patch 1.130 added a search bar for icons on the main screen, triggered from a new button at the top. | LESSON: Budget for text search over the graph from day one, and treat the moment you need it as the signal that the graph has stopped being the primary navigation. MERIDIAN's entity graph will exceed Democracy 4's node count almost immediately.
  FAIL[Long causal chains are not traceable — only one hop is ever visible] The hover mechanism shows a node's IMMEDIATE inputs and outputs. There is no shipped affordance for 'show me the whole path from this policy to this outcome three hops away'. Following a chain requires the player to hover, remember, click through to a details screen, hover the next node, and hold th | LESSON: This is MERIDIAN's most important negative finding. The Eight Questions include 'what caused it' and 'what future options changed' — both are multi-hop. MERIDIAN must ship an explicit PATH view (pick source, pick outcome, render the ranked contributing chains with per-edge weight and latency), becau
  FAIL[Magnitude encoded in animation speed is unreadable, unquantified and inaccessible] Effect strength is communicated by how fast arrow particles move. There is no numeric readout on the arrow, no legend mapping speed to magnitude, and no static equivalent. | LESSON: MERIDIAN must not encode magnitude in motion. Use stroke weight plus an on-edge or on-hover numeric value, and reserve motion strictly for change-of-state. This also protects MERIDIAN's 'accessibility is not optional' commitment.
  FAIL['Feels like tuning sliders' — the interface teaches optimisation, not governance] The dominant critical framing is spreadsheet-like: the game described as 'little more than a spreadsheet akin to Football Manager' and as 'one huge Excel spreadsheet, in which moving one number or slider will affect something else'; another reviewer notes you are 'bombarded with a user interface tha | LESSON: Direct hit on MERIDIAN's hardest charter constraint. A causal graph is not itself a defence against 'menu of pre-authored options' — Democracy 4 has the best causal graph in the genre and is still criticised as a slider menu, because the ACTION VOCABULARY is closed. MERIDIAN's open-composition inten
  FAIL[No adequate onboarding for the causal model, and reviewers directly contradict each other on whether one exists] Multiple reviews report 'no proper tutorial or clear roadmap for how systems connect, leaving new players guessing about cause and effect', and 'you're left stumbling until you eventually piece together the mechanics yourself'. At least one other review praises a 'phenomenal introduction' covering e | LESSON: Showing a causal edge is not the same as justifying it. MERIDIAN's Eight Questions #3 ('which rule/mechanism applied') and #5 ('what assumptions were used') exist precisely to close this gap — they must be attached to individual edges and reachable in one interaction, not deferred to a codex.
  FAIL[Overcomplexity defended as doctrine rather than measured] Harris's public position is 'Overcomplexity is a feature, not a bug in Democracy 4. This is something that a few reviewers, and a few players do not get, and I understand why', paired with a broadside at streamlining: "'Streamlining' it inevitably means 'its way worse, but it looks simpler'." | LESSON: MERIDIAN can and should keep depth. But it must separate 'the SIMULATION is complex' (charter-required) from 'the INTERFACE is complex' (a defect). Harris's most valuable work is where he treated a comprehension failure as his problem — the distribution-placement post is the model, not the overcompl
  FAIL[Filters/lenses shipped without a question each one answers] A reviewer notes the filters exist but 'some — such as this filter showing the popularity of policies — are more useful than others'. | LESSON: For MERIDIAN: every lens must be introduced with the named question it answers, and lenses that don't map to a question in the Eight should be cut. Same test as 'causal value, not fake depth', applied to view options rather than entity attributes.
  XFER(strong) Draw the ego network on demand, never the whole graph. Stable authored layout; ephemeral edges revealed by focus; causes and effects surfaced in the SAME gesture. -> MERIDIAN's centre operational view renders entities/institutions at rest as an unconnected, spatially stable field grouped by authored zone. Focusing any node draws inbound causes and outbound effects together. This is the primary browse affordance for the world model and satisfies D-1/D-2 because an authored layout is a pure projection consuming n
  XFER(strong) Two renderings of one causal record: graph for locating, enumerated strip list for auditing. The strip list is canonical and carries per-edge metadata (sign, magnitude, latency, source). -> MERIDIAN's right context panel holds the strip list; each row is one causal edge from the resolver with the mechanism cited, the assumption used, the uncertainty, and its confidence label. The Eight Questions are answered per-row, not per-screen. Graph edges hold only sign and magnitude.
  XFER(strong) Explainability must be a read-back of the computation, not a parallel authored artefact. Because Democracy 4's 'why' is literally the neuron's input list, it cannot drift from the simulation. -> MERIDIAN's engine emits a uniform causal-edge record during resolution; every explanatory surface is a projection over that record. Nothing explanatory is authored anywhere else. This is the concrete implementation of the determinism boundary at the interface: the LLM narrates the record, it never produces it. Directly addresses MERIDIAN's stated d
  XFER(strong) Encode agency and provenance as the node's own pre-attentive channel, before any interaction. -> Node fill/treatment partitions the field into engine-computed authoritative state, LLM-proposed advisory, and player-actionable lever. Satisfies the PROVENANCE RULE at the interface level rather than in a footnote. Must use MERIDIAN's own original palette and shapes — not Democracy 4's grey/blue/red/green assignment.
  XFER(strong) Per-edge time-to-effect and instant-vs-lagged marking; committed-target ghost state distinct from current effective state. -> Every ordered action shows current value, committed target, approval/implementation progress, and per-edge latency. Delivers the charter's 'how long it will take' promise and gives the 'delay an operation' and 'trigger an approval process' primitives a visible home.
  XFER(strong) A permanent cohort rail with nested encoding: bar width = population share, fill = state; and cohort MEMBERSHIP is itself endogenous and inspectable. -> Meso-tier cohorts live permanently in MERIDIAN's shell so 'who does this land on' is never out of sight. Membership flows between cohorts are simulated and have their own 'what is driving this' view — which is also a natural home for fidelity-tier promotion indicators.
  XFER(strong) Drill from aggregate to a single named individual with a full summed attribution of how policy reached their life. -> MERIDIAN's dossier drill-down from cohort to focal individual, showing per-line attribution of how each institutional action reached this person. MERIDIAN's mandatory upgrade: each line carries an engine-computed confidence label, and Unknown/Restricted render as present rows — it is an intelligence product, not the omniscient view Democracy 4 give
  XFER(strong) Show distributions, not means, wherever an outcome is threshold- or tipping-driven — and place the visualisation on the screen where the player already asks that question, not on a dedicated analysis  -> MERIDIAN's macro indicators should render as cohort distributions on the operational view. This would also make the open critical finding (macro saturation) visible during play rather than only in audit, the way Harris's wealth graph exposed his own income bug.
  NOCOPY: The specific colour taxonomy grey=policy / blue=statistic / red=bad situation / green=good situation, and the 'Emotion' field naming that drives it; Animated particle arrows whose travel speed encodes effect magnitude — this is Democracy 4's signature visual identity and also a poor encoding channel; use static stroke weight plus numerals; The circular segmented ring around an icon indicating affo
## Paradox Interactive's nested tooltip and modifier-breakdown system (EU4 / HOI4 / Stellaris / CK3 / Victoria 3 / EU5)
ONELINE: Paradox has spent two decades building the only commercially proven mechanism for making an opaque simulation's arithmetic inspectable on demand — and its well-documented failure modes (depth, discoverability-by-accident, null hops, cycles, controller/screen-reader collapse, and mods that exist purely to surface data the engine already computed) map almost one-to-one onto the risks in MERIDIAN's Eight Questions and dossier design.
  FAIL[Depth burial — key information two or three layers down] The most-liked feature is also the most-criticised, and specifically on execution, not concept. The Uppsala/Paradox study's central tooltip finding: "The most significant issue was the lack of information priority, with key information often buried two or three layers deep within the tooltips." And  | LESSON: Budget depth per fact by *access frequency*, not by conceptual tidiness. Instrument it: a fact retrieved often must sit at depth 0 or 1. Depth is a cost that must appear on someone's ledger or it will not be paid down.
  FAIL[The frequency inversion — routine tasks requiring four hops] Players report that checking a country's army composition requires "opening 4 nested tooltips" through multiple layers before reaching actual unit data — a task performed constantly. Another: "The bloc system is the worst offender with this. Nested tooltip after nested tooltip after nested tooltip t | LESSON: Design from workflows, not from mechanics. Enumerate the ten things a player does most, count the hops each takes today, and treat a hop count above two on a top-ten workflow as a defect requiring a designed screen — not a better tooltip.
  FAIL[The null hop — a nesting level that carries no information] In EU5, tooltips for individual Papal Bulls contain "an unnecessary level that contains no information, it just says that enacting this bull will enact the bull with this name, but to see what it actually does you need to drill down a level which is annoying especially when going through the whole l | LESSON: A layer that adds no information must be elided automatically. Make it a rendering invariant: if a nested node's content is derivable from its own label, collapse it into the parent. MERIDIAN can enforce this mechanically since its tooltips would be generated from structured breakdown data, not auth
  FAIL[Cyclic nesting — tooltips that loop back to themselves] An EU5 player reports tooltips that "nest back to the same one, making it a loop." | LESSON: Treat the explanation graph as a graph and validate it in CI: detect cycles, and suppress or visually mark a link that would return to an ancestor already on the current chain. MERIDIAN's causal-explanation graph is far more cycle-prone than a glossary, because causal chains genuinely do contain fee
  FAIL[Discoverability only by accidental hover] There is no way to know which elements hold explanation without sweeping the cursor over them. The information exists but is unaddressable except by chance. Victoria 3 players complained that "tooltips were overused, with some information better displayed in the main UI." EU5 players report inabilit | LESSON: Every fact reachable by hover must also be reachable by a browsable, searchable route — the auto-generated encyclopaedia pattern, plus ledgers. Hover should be an accelerator for facts that have an address, never the sole address.
  FAIL[Controller collapse — nesting requires a mouse] CK3's console edition could not carry the nesting interaction across. Reviewers report the PC nested tooltips "are now hidden behind a specific tooltip mode, summoned by holding down the left stick on console", producing "a secondary mouse pointer that doesn't move the map". Assessment: "It works, b | LESSON: Nesting is an input-modality-locked pattern. If MERIDIAN ever wants controller, touch, or even comfortable trackpad use — or a demo on a tablet — the *authoritative* route to a breakdown must be a focusable, keyboard/D-pad-navigable panel, with hover-nesting as a mouse-only accelerator layered on to
  FAIL[Screen-reader inaccessibility — the explanation layer is invisible to assistive technology] Paradox titles render text in a way screen readers cannot parse, forcing blind players onto OCR. Stellaris "writes text to the screen in a way which cannot be picked up by a screen reader at all", and dependence on OCR is described as a major issue across all Paradox titles. CK3's rendering makes ev | LESSON: This is the deepest structural lesson and it is an architecture decision, not a UI one. MERIDIAN must model a breakdown as a serialisable object — base, ordered modifier rows with source entity IDs, confidence labels, total — that the UI *renders* and that the API can also return. Do that and screen
  XFER(strong) Make the breakdown a serialisable object, not a rendered string. Base value + ordered modifier rows (signed value, human name, source entity ID) + total, returned by the API and rendered by the UI fro -> This single decision cashes out the Eight Questions (Q1/Q2/Q3 become fields, not prose), satisfies dossier property D-1/D-2 (a projection with no mutation and no randomness), makes the explanation testable in CI, exportable, keyboard-navigable and screen-reader-readable, and gives the provenance tag a structural home (a field on the object, not a c
  XFER(strong) Generate the concept reference from the engine's own registries, so documentation cannot drift from implementation (CK3's auto-generated encyclopaedia). -> Generate MERIDIAN's reference for the ~16 consequence primitives, the eight confidence labels, the four entity views and every named mechanism directly from engine registries. This converts 'causal value, not fake depth' from a doctrine into a build-time check: a dossier attribute that does not resolve to a registered named mechanism fails to rende
  XFER(strong) Explanation must have an address as well as a hover. Every fact reachable by hovering must also be reachable by browsing or searching a durable destination. -> Pair every hover breakdown with (a) an entry in the generated reference and (b) a row in a ledger screen. MERIDIAN currently has no ledger specified; the meso tier and the four population fidelity tiers need one, and it is the only place a confidence-coverage audit (how much of what I know about this entity is Confirmed vs Assessed vs Unknown) can 
  XFER(strong) Cap nesting depth by access frequency and promote past the cap to a designed screen rather than a third floating layer. -> Hover = level 0 preview. Pin = level 1, promoted into the right context panel (persistent, scrollable, focusable, can host controls). Anything deeper opens the full dossier or ledger in the centre view. The charter-critical 'here is what is required, what may stop it, who will resist, what it will cost, how long it will take' brief must be a screen
  XFER(strong) Validation must return the failing predicate with its operands and the resisting actor, never a generic refusal. -> The engine already validates and prices compositions of consequence primitives; the boundary must not discard why. Render the specific unmet condition, the named entity that will resist, and the named rule that applies. Victoria 3's 'cannot reach' and CK3's unexplained tradition warning are the exact anti-patterns, and for MERIDIAN they would be ch
  XFER(strong) Every entity mention anywhere in the interface is the same component with the same behaviour: hover previews, click selects and opens, and the map/graph highlights. -> Apply uniformly to modifier source rows, event-stream entries, dossier relationship rows, ledger cells and advisory text. Make it a framework guarantee so it cannot be forgotten per-feature — CK3's inconsistent province highlighting shows what per-feature implementation produces. In MERIDIAN this component is also the natural carrier of the confide
  XFER(strong) Explanation eagerness is a persistent user setting with a sane default, not a per-interaction decision (CK3's timer lock vs action lock). -> Implements MERIDIAN's 'three layers of progressive disclosure' without forking screens or requiring the player to re-choose hundreds of times. The Uppsala study's finding that novices and experts want opposite densities of the same content is the empirical warrant.
  XFER(strong) Inside an explanation, use tables, valence colour, small functional glyphs and inline sparklines — not paragraphs. Plain language governs wording, not form. -> MERIDIAN's 'plain language first' rule should be read as constraining vocabulary while permitting tabular and graphical form. The Uppsala finding is that experts did not object to information volume, only to prose delivery. Victoria 3's third icon register — flat, minimal, colour-coded mechanic glyphs legible at small size — is the right model for 
  NOCOPY: Victoria 3's Art Nouveau panel framing, ornamented headers, and aged-paper / Victorian newspaper-and-blueprint infographic treatment — a signature period aesthetic tied to that title.; Wood-textured buttons with emerald colouring and thin gold borders, and Art Nouveau ornamentation reserved for high-priority buttons — Victoria 3's specific button vocabulary.; CK3's faux-medieval typeface and heavy
## Crusader Kings III (Paradox Development Studio, 2020–present)
ONELINE: CK3 is the closest shipped proof that a simulation of thousands of persistent, related, motivated entities can be made inspectable by ordinary players — and its specific failures (relationship asymmetry erased at the interface, tooltip depth without navigation, full outcome disclosure killing decision tension, a search tool so weak players mod around it) map almost one-to-one onto the risks in MERIDIAN's dossier and consequence-preview screens.
  FAIL['Balrog tooltips' — unbounded nesting depth with no navigation model] Reaching a routine number can require traversing multiple nested tooltip layers, with the intermediate layers being information the player did not want. Players report needing four nested layers to reach army composition detail. The stack is fragile: it collapses if the cursor briefly leaves the ele | LESSON: Cap nesting at two or three layers and provide a click-to-pin escape hatch that promotes any tooltip into a real, navigable panel with tabs and history. Never make hover the only route to a fact the player needs repeatedly. For MERIDIAN specifically: causal chains in a three-tier simulation are natu
  FAIL[Relationship asymmetry exists in the model but is erased at the interface] Opinion totals shown in the UI essentially always mean 'this character's opinion of the player character'. Players cannot systematically inspect character A's opinion of character B, and cannot inspect their own character's opinion of others. Narrow exceptions exist (a landed vassal's view of their  | LESSON: This is the most important failure in the report for MERIDIAN. MERIDIAN's world model explicitly requires DIRECTIONAL, ASYMMETRIC, HISTORIED relationships and FOUR views per entity. CK3 demonstrates that a model can have this property while the interface silently collapses it to one direction and on
  FAIL[Complete pre-commit outcome disclosure destroys decision tension] Because hovering an event option reveals its consequences, players optimise rather than decide. A mod exists purely to strip this, arguing that with tooltips 'the player doesn't have to think about anything, just choose the consequences that suit them.' | LESSON: MERIDIAN has the representational vocabulary CK3 lacks: eight confidence labels. A consequence preview should not be a binary between 'here is what happens' and silence — it should show each projected consequence with its own confidence label and uncertainty, so the player is deciding under characte
  FAIL[Search and filtering too weak for the population the simulation generates] The vanilla character finder lacks filters for the tasks players actually perform. Players cannot filter by betrothal status and so repeatedly select courtiers for marriage only to discover they are already betrothed; cannot filter by heir status, education traits, claims, or guardianship willingnes | LESSON: Persistent-entity simulations create a search problem proportional to population size, and it is systematically under-budgeted because it looks like plumbing rather than a feature. MERIDIAN should treat the entity browser as a primary screen with the same design weight as the dossier, and should val
  FAIL[Screen occlusion and cramped information bars] Opening panels obscures the map; information bars and notifications compete for the same edges and crowd each other; some controls are placed by layout convenience rather than by task adjacency. Separately, some content appears in one screen and not another for the same query — one reviewer reports  | LESSON: Reserve gutters structurally so panels never occlude the operational view — this is what MERIDIAN's stable command shell (status bar / left nav / centre view / right context / bottom stream) is for, and it must be enforced rather than aspirational. The deeper lesson is the marriage-screen one: two s
  FAIL[Onboarding fires too fast and is trivially skippable] The tutorial can be accidentally skipped section by section, and even a favourable hands-on reported the advice system 'started throwing information our way a bit too quickly'. Reviews describe an incredibly steep learning curve with 'some menus that are rough' and a general sense that everything is | LESSON: Rate-limit concept introduction explicitly: a hard cap on new concepts introduced per unit of play, with a queue rather than concurrent firing. MERIDIAN's concept load (16 primitives, 8 confidence labels, 3 tiers, 4 fidelity tiers, 3 conversational modes) makes this mandatory, not optional.
  FAIL[Probability presented as a number without a model of how it resolves] Players perceive displayed percentages as misleading. A contributing factor is that CK3 pre-determines many random rolls ahead of time and consumes them as needed — the community analogy is rolling a die under a cup and lifting the cup later — so reloading a save does not reroll the same event outco | LESSON: MERIDIAN has no named RNG substreams yet, which means this failure is currently unavoidable there — determinism and replay cannot be explained to a player if the engine cannot name where randomness came from. Named substreams are therefore a UI prerequisite, not just an engineering one: they let the
  XFER(strong) Explain a computed scalar as an itemised, individually-attributed, reconciling list — each row a named mechanism with a signed contribution, evaluated relative to the observer's own values, summing vi -> The core rendering pattern for MERIDIAN's relationship rows, opinion-equivalents, consequence pricing and macro indicator movements. Extend CK3's version by attaching a confidence label to each row (not just the total) and by rendering Unknown and Restricted as present rows with the reason for their absence. Require that visible rows plus an explic
  XFER(strong) Generate the reference documentation from the same authoritative source the simulation executes, so it cannot drift. -> Build MERIDIAN's in-app reference from the consequence-primitive registry, the rule/mechanism table and the confidence-label definitions. This makes engine-computed reference categorically distinguishable from model-narrated advisory, giving the provenance rule a structural rather than editorial enforcement mechanism, and directly attacks the proje
  XFER(strong) Make the observer explicit and switchable in every view of a relationship or belief. -> The corrective to CK3's most damaging failure. MERIDIAN's four views per entity and directional relationships must be surfaced through a persistent, prominent observer control — whose knowledge am I reading, and which of the four views is this. Every relationship row carries a direction marker. 'A's assessment of B' must be a supported first-class 
  XFER(strong) Decompose a multi-factor action into separately named quantities with per-actor attribution, rather than a single success probability. -> The consequence-preview screen. Compose it from primitives — what is required, who can block, what it costs, how long, what it risks — with each contribution attributed to a named entity that clicks through to that entity's dossier. This is how the preview stays a computed artefact rather than collapsing into a pre-authored option card, which is th
  XFER(strong) Separate attention channels structurally by urgency and persistence semantics, so priority is encoded in the channel rather than in wording. -> Replace MERIDIAN's undifferentiated bottom event stream with a small fixed set of channels (must-act / standing-condition / engine-derived-opportunity / informational / significant-outcome), crossed with a provenance axis distinguishing engine-computed fact from model-narrated advisory. Users learn the semantics once and apply them to every message
  XFER(strong) Offer the same information architecture in a push mode for novices and a pull mode for experts, toggled by the user, rather than building two interfaces or picking one audience. -> Apply to MERIDIAN's three progressive-disclosure layers and to advisory density: a first-session operator gets proactive explanation; a returning operator opts into explanation on demand. Include an explicit rate limit on new concepts introduced per session, given MERIDIAN's unusually large mandatory vocabulary.
  XFER(moderate) Use camera distance as the abstraction control for the default view, and reserve explicit mode switching for genuinely orthogonal thematic overlays — exactly one active at a time. -> Make MERIDIAN's three simulation tiers perceptible through zoom in the centre operational view (macro indicators far, meso cohorts and institutions mid, micro named actors close), so the tier structure is experienced rather than asserted. Group thematic overlays by tier instead of presenting a flat list. This also gives the two open critical findin
  XFER(strong) Every element rendered in a spatial or aggregate view must resolve to an inspectable entity; anything that does not is decoration. -> Enforce click-through-to-dossier as an invariant across map, charts, event stream and consequence previews. This is what converts MERIDIAN's differentiator — that a crisis propagates through real-seeming people, firms and institutions — from a claim into something the player can verify by clicking. It also operationalises 'causal value, not fake de
  NOCOPY: Paradox's medieval-manuscript/parchment visual identity — illuminated borders, wax-seal and heraldic-shield framing, aged-vellum panel textures.; The coat-of-arms system as an identity device: shield-shaped emblems, heraldic quartering, the specific use of arms as a clickable identity token in lists and on the map.; CK3's specific trait iconography — the circular gilt-framed portrait medallions an
## Terra Invicta (Pavonis Interactive / Hooded Horse, 2022 Early Access, 1.0 January 2026)
ONELINE: Terra Invicta is the closest existing product to MERIDIAN's systemic scope — it already ships a persistent multi-tier world of nations, regions, institutions and named agents — and it is the field's most instructive UI failure: the simulation is genuinely deep, and the interface is so bad at exposing that depth that reviewers call it "less a tool and more a stress test" and players build external flowcharts to understand a single panel.
  FAIL[Information exists but is not ranked by consequence] The interface has no severity hierarchy. Critical state changes (a nuclear exchange beginning; a public-opinion swing about to cost you a nation) are rendered at the same visual weight as routine notifications and small map icons, so players discover major losses retroactively. | LESSON: For MERIDIAN: completeness is not a defence. Every surfaced fact needs an engine-computed consequence weight, and the top layer of progressive disclosure must be RANKED BY THAT WEIGHT, not by data category. MERIDIAN is in unusually good shape to do this because Eight-Questions #8 ('what future optio
  FAIL[No causal feedback on outcomes — the anti-Eight-Questions] When the player loses a space battle, the game supplies 'no meaningful combat logs' and only 'vague' feedback about why ships died. The player cannot reconstruct which decision caused the loss, so learning is by repetition rather than by comprehension. | LESSON: This is precisely the defect MERIDIAN's Eight Questions exist to prevent, and it is the strongest argument in this whole report FOR that rule. Terra Invicta demonstrates that a simulation can be world-class and still be unlearnable if it cannot answer 'what caused it' and 'which rule applied'. MERID
  FAIL[Essential mechanics documented only outside the game] Core mechanical knowledge lives in the wiki, Discord and community guides rather than in the product. Players report the choice is to spend hundreds of hours discovering mechanics or to 'blindly follow advice from veteran players from Discord/Steam'. A community complete-guide states outright: 'Much | LESSON: MERIDIAN's plain-language-first rule needs an enforcement test, not a style guide: for every screen, name the decision it supports and verify that the information required for that decision is visible WITHOUT hovering. Hover-only content is second-layer content by definition.
  FAIL[The invisible modal phase] Mission assignment is only possible during a periodic phase, and multiple players filed threads titled 'How do I assign Councilors???' and 'cant assign new counselors missions' because nothing told them a modal phase existed or that they were currently outside it. The CONFIRM ASSIGNMENTS banner is p | LESSON: For MERIDIAN's COMMAND mode: if commitment is gated (by tick boundary, by approval process, by resource availability), the gate must be VISIBLE AND EXPLAINED IN THE CLOSED STATE — showing the reason and the time-to-open. This is also a charter issue: a player who cannot tell the difference between '
  FAIL[Silent progressive unlock — the Codex nobody returns to] The Codex is reached via a small '?' in a box next to the date in the top-right corner, easy to overlook. Its pages are initially near-empty, so players flip through it once, conclude there is nothing there, and never return — even though later technologies unlock genuinely critical pages such as th | LESSON: MERIDIAN's four-views/eight-labels model implies knowledge that grows over time. Every promotion of an entity's knowledge state (Unknown→Reported, Reported→Confirmed, a Restricted row becoming visible) must generate an event in the stream that deep-links to the changed row. A reference surface that 
  FAIL[Entity identity failure — councillors indistinguishable] A professional reviewer resorted to installing a mod replacing councillor portraits with anime characters 'to make them easier to distinguish'. The base portraits do not do the one job a portrait exists to do. | LESSON: Directly relevant to MERIDIAN's portrait progression stages 0→3. Stage 0's deterministic abstract avatars may in fact OUTPERFORM stages 2-3's generated fictional faces on the only metric that matters — instant discrimination among the 6-12 entities the player handles constantly. Since portraits feed
  FAIL[Late-game volume exceeds the UI's design point] The interface holds up early and collapses in the endgame, when dozens of habitats, mining fleets, warships and points-of-interest must be tracked. Reviewers describe the war phase becoming 'a cumbersome, repetitive slog with dozens of points-of-interest to track and manage using the inadequate UI', | LESSON: MERIDIAN's world model explicitly plans population cohorts, households, named secondaries and focal individuals with PROMOTION ON TRIGGER — i.e. the entity count is designed to grow. The UI must therefore have an aggregation story from day one: cohort-level rows that expand into individuals, and a w
  XFER(strong) Propose-batch, review, single explicit commit — with the gate's CLOSED state as visible as its open state -> MERIDIAN's COMMAND mode. Assemble the parsed consequence-primitive composition into a reviewable batch; nothing touches authoritative state until one commit action. Adopt the per-entity 'pending action' badge so unassigned/undecided items are visible at a glance. Critically, fix Terra Invicta's defect: when commitment is not currently possible, sho
  XFER(strong) Itemised, signed decomposition of any single computed number the player is asked to act on -> Every priced action shows: base term, each modifier with name/sign/magnitude/source mechanism, and residual uncertainty. Extends Terra Invicta's success-breakdown by adding the mechanism identifier, which MERIDIAN's Eight Questions require and Terra Invicta lacks. This is also where the provenance tag lives: engine-computed terms and LLM-proposed n
  XFER(strong) Institutional access points as the unit of influence, with an ordering prerequisite encoded in the interaction -> Represent a state or organisation's controllable surface as a small set of NAMED institutional handles rather than an influence scalar. Encode real prerequisites structurally (you cannot attempt the executive handle without a prior foothold), which supports the charter's 'here is what is required' promise without pre-authoring a menu of outcomes. C
  XFER(strong) Relative-weight allocation with an engine-computed resolved ratio displayed alongside -> For standing policy inputs (budget posture, institutional emphasis, reserve allocation), let the player express relative intent and have the engine display the resolved absolute split. Reuse one widget everywhere. The resolved figure is engine-computed fact and can be tagged as such, while the weights remain player intent — a clean provenance bound
  XFER(strong) Spatial scene and tabular projection of the SAME entity set, both first-class and both reachable -> MERIDIAN's centre operational view gets a map and an equally prominent sortable/filterable entity table, with selection synchronised between them. Do not bury the table under an unrelated tab name as Terra Invicta buries its Solar System View under 'Intelligence'. Add a MERIDIAN-specific column: confidence label, sortable, so an analyst can interro
  XFER(strong) Map ink spent on disagreement — aggregate the homogeneous, expand the contested -> Collapse uniform state into summary glyphs and expand contested state automatically, so attention lands where the simulation is actually in flux. Mandatory corrections over Terra Invicta: the summary glyph must be visibly a summary (count badge, distinct outline), must be expandable, and must be visually distinguishable from both 'Unknown' and 'Res
  XFER(strong) Requirement set for a heterogeneous persistent entity roster (derived from what the community had to mod in) -> MERIDIAN's roster: a domain-meaningful default sort per entity type; an allegiance/ownership colour axis; a pending-action badge; per-type visibility toggles; hover preview; and inclusion of KNOWN ADVERSARY entities, not only your own. Add MERIDIAN's confidence indicator per row. Treating the Outliner Overhaul mod's feature list as a spec is a chea
  XFER(moderate) Search-by-effect over descriptions, plus isolate-the-dependency-path, as the escape hatch for any option space too large to browse -> For the composition space of the ~16 consequence primitives and for whatever prerequisite graph the engine computes, provide free-text search across effect descriptions and a view that renders ONLY the prerequisite chain relevant to the current proposal. This is the concrete UI shape for 'here is what is required' when the player proposes something
  NOCOPY: The faction identity system as a whole — the seven named factions, their ideological labels (Resistance, Humanity First, Project Exodus, The Initiative, Academy, Servants, Protectorate), their logos and their signature colour assignments. MERIDIAN's factions must be independently conceived and named.; The alien-invasion framing and any of its specific iconography — Hostility Meter, alien mission l
## Suzerain (Torpor Games, 2020; 2.0 "Amendment" 2023; 3.1 "Sovereign" 2025)
ONELINE: Suzerain is the closest existing product to MERIDIAN's player experience — govern a fictional country through adviser conversations, budgets, constitutional reform and foreign pressure — and it has already run the experiment MERIDIAN is about to run: it proves a codex-plus-ledger architecture can make an enormous fictional world comprehensible, and it proves that hiding the engine's decisive numbers converts a serious political sim into wiki-dependent trial-and-error.
  FAIL[Decisive hidden thresholds create trial-and-error and wiki dependency] The statistics that determine which of nine endings you receive — Alignment, Unrest, Opinion — are never shown numerically, and neither are the thresholds. Players report that hidden scores can deliver an ending completely at odds with the narrative direction of their choices. | LESSON: Uncertainty is legitimate; INVISIBILITY is not. If a variable determines the outcome, the player must be able to see that it exists, roughly where they stand, and what moved it — even if the precise value stays uncertain. MERIDIAN's eight confidence labels are exactly the tool Suzerain lacked: 'Asse
  FAIL[The coup arrives with no monitorable indicator] A military coup — effectively a game-over — accumulates entirely beneath the systems layer with no way for the player to monitor its approach, then fires without warning. | LESSON: For MERIDIAN: any terminal or near-terminal state transition must have a dedicated, always-available leading indicator with at least a direction of travel and a named cause — otherwise the sim reads as arbitrary and the Eight Questions are unanswerable at the exact moment they matter most.
  FAIL[Undisclosed recurring costs behind a one-off price tag] A decree presented with a one-time budget cost also silently imposed a per-turn drain. A player reports enacting a decree expecting a one-time cost of 2 budget and discovering it also carried "-2 per turn for high welfare", saying "If I would've known that I wouldn't have enacted that." | LESSON: Directly relevant to MERIDIAN's consequence primitives, which explicitly include 'create an obligation' and 'reserve resources'. A recurring or contingent liability is a DIFFERENT KIND of cost from a one-off spend and needs its own visual notation on the option — one-off / recurring / contingent — o
  FAIL[Systems explained after the player can act on them] In the Rizia DLC, players report the military/war system's rules are explained by a tutorial that arrives too late to adjust force composition, comparing it to being six sessions into a D&D campaign before surprise rules appear. | LESSON: MERIDIAN's mode structure (EXPLORE / PLAN / COMMAND) must teach a mechanism at the point of FIRST CONSTRAINT, not first use. If a turn-1 decision limits a turn-8 capability, the turn-1 screen owes the player that fact.
  FAIL[No popularity indicator at all leaves feedback unattributable] A reviewer reports being "constantly being told that apparently the people were mad at me. For what? I never learned." The absence of any happiness or popularity indicator was called out as a missing UI element. | LESSON: Removing a gauge only works if you replace it with SOURCED, ATTRIBUTED narrative feedback. Unsourced sentiment assertions are worse than a bar. For MERIDIAN this is the provenance rule in a different guise: every claim about how a population feels must carry its evidence and its confidence label, or
  FAIL[Resource scale and renewal left unexplained] A reviewer could not determine whether the budget renewed: "my budget of '3' was supposed to somehow last me throughout my entire term of what I assume was four years. Surely I get a new budget every year?" The same reviewer reports "cash injections that I never received, such as trade with a neighb | LESSON: Every resource in MERIDIAN needs three things stated on the surface that spends it: what the unit means, over what period it applies, and what replenishes it. Otherwise players cannot plan, and unexplained income makes the engine look broken even when it is correct.
  FAIL[The 2.0 UI overhaul regressed readability and was partially rolled back] The 2.0 "Amendment" redesign drew sustained complaints: the dialogue box became a full-screen black bar rather than a bordered box ("completely ruins the immersion"), reports were "double-spaced, way too many blank lines", fonts were oversized, close-button placement caused unintended map scrolling, | LESSON: Two lessons for MERIDIAN. (1) In a reading-heavy application, spacing and type scale ARE the interface; touch-first density is a regression on desktop. (2) The correct remediation was a user-selectable typography option, not a further redesign — MERIDIAN's accessibility commitment should include tex
  XFER(strong) A stable four-panel context rail — state / relationships / world knowledge / my own decision history — reachable without leaving the current decision -> Adopt as MERIDIAN's right-hand context panel taxonomy: Situation board (Overview), Entity dossiers and relationship graph (Connections), Codex, and the event-sourced decision ledger (Journal). Non-modal: opening any of them must not interrupt or advance the centre view, and per D-1/D-2 must be a pure projection consuming zero randomness.
  XFER(strong) A severity-ordered, domain-grouped, valence-coloured board of named persistent conditions, each with description and enumerated resolution paths -> Introduce a first-class Situation entity between MERIDIAN's macro indicators and its narrative layer: it is the object that carries cross-tier causation (meso condition modifying macro indicators) and gives the UI a triage surface. Sort by severity, group by domain, colour by valence only. Each entry lists engine-validated resolution routes while e
  XFER(strong) Inline hyperlinked entity references inside narrative text, opening a persistent encyclopaedia panel — lore is pulled, never pushed -> Every person, institution, state, cohort or treaty named in advisory or engine text becomes a click-target opening the dossier in the context panel at the relevant tab. This is how MERIDIAN teaches an invented world without a lore dump. MERIDIAN's version must open the PLAYER INTELLIGENCE PROFILE (view 4) with per-row confidence labels, not an omni
  XFER(strong) Price the option on the option: mechanical cost stated in engine units at the point of decision, typed by cost kind -> MERIDIAN's proposal screen should tag each option with engine-computed cost, and CRUCIALLY distinguish one-off spend from recurring obligation from contingent liability — the exact gap that produced Suzerain's most concrete player betrayal. Everything on the tag is engine-computed fact and must be visually distinct from any model-authored advisory 
  XFER(strong) Institutional disagreement rendered as named officials arguing in a fixed-layout scene, rather than as faction bars or confidence intervals -> MERIDIAN's 'who will resist' output should default to a scene or panel of named institutional actors with stated positions and stated reasons, with the layout constant across topics so the player never re-learns the screen. The difference from Suzerain must be that positions are DERIVED by the engine from institutional interest, legal authority and
  XFER(strong) Legal/procedural requirements stated as hard, knowable thresholds attached to named actors with tradeable demands -> Model MERIDIAN's approval processes the way Suzerain models constitutional amendment: an explicit requirement (supermajority, court concurrence, procurement lead time), a roster of named actors with current position, and what would move each. MERIDIAN can exceed Suzerain by adding engine-computed duration and validity-check results, which Suzerain 
  XFER(moderate) Coarse, legible, front-loaded resource scarcity with an explicit commitment horizon -> Give MERIDIAN's missing budget/cost layer a small-integer presentation scale the player can hold in working memory, with the surface stating unit meaning, applicable period and replenishment source. Make at least one allocation deliberately front-loaded and hard to reverse so later choices carry felt opportunity cost.
  XFER(moderate) Multi-source partisan reporting instead of a single neutral sentiment number -> Render public and institutional reaction through several in-world media and institutional voices with distinct positions, so the player triangulates rather than reads a gauge. This serves the differentiator (crises propagate through media systems) and the four-views model (public profile as content). Requires strict interface-level separation of in
  NOCOPY: Suzerain's specific fictional geography, nation names, ideology names (Sordland, Rizia, Merkopan, Malenyevism, Sollism, Arcasian Capitalism) or any named character — MERIDIAN's fiction must be wholly its own; The four-quadrant political-compass results screen as a visual signature; the compass-with-characters-plotted layout is strongly identified with Suzerain's ending and would read as a lift; Th
## Victoria 3 (Paradox Development Studio, 2022–present)
ONELINE: Victoria 3 is the most complete commercial attempt to render an entire simulated society — markets, tens of millions of "pops", interest groups, legislation — as a browsable interface, and its central, extensively documented failure (a rich causal model players cannot see, diagnose or trace back to a cause) is exactly the failure MERIDIAN's Eight Questions exist to prevent.
  FAIL[The signature failure: a rich causal model that cannot be traced backwards to a cause] Players routinely cannot determine why their economy collapsed. In one representative thread a player's Chile went from stable to welfare spending rising from 300k to 1.5m/year with ~20% of the workforce unemployed, with no institutional change in 15 years. The community's diagnostic advice was to c | LESSON: MERIDIAN's Eight Questions are not documentation garnish — they are the specific defence against this exact failure, and Q2 (what caused it) and Q3 (which rule applied) must be PERSISTED per outcome, not recomputed on demand. A causal explanation available only while the state still exists is not an
  FAIL[Information fragmented across screens with no place holding the whole picture] A long-form community critique states the information a player needs 'is fragmented, spread across many screens and tabs, sometimes hidden deep in nested tooltips — and at no point does the player ever have all the information they need readily at hand.' Concrete instance cited: to see a country's p | LESSON: Organise at least some of MERIDIAN's screens around QUESTIONS ('why is this intervention stalled', 'what changed in this region this month') rather than around SUBSYSTEMS. A subsystem-shaped IA guarantees that every cross-subsystem question requires manual synthesis, and manual synthesis is where pl
  FAIL[Nested tooltips used as a dumping ground for information that should be on-screen] The critique attacks a specific pattern: the Law Change screen uses 'massive buttons with wasted space' while hiding critical information behind tooltips, when it should show which interest groups support and oppose the law directly on the panel. The Naval Invasion menu does not show which generals  | LESSON: Write the rule explicitly into INTERACTION-PATTERNS: if a player needs a fact on EVERY visit to a screen, it is layout, not a tooltip. Tooltips are for the derivation of a shown number, never for the number itself. Applied to MERIDIAN: the actor-reaction list on a proposed action is layout; the modi
  FAIL[Blocked actions with no explanation of why] A published reviewer: 'there were many times where it took serious detective work on my part to figure out why I couldn't take an action.' His proposed fix is focused tooltips naming the actual blocker ('relations too high', 'existing obligations'). | LESSON: A direct hit on the charter promise 'You can attempt this. Here is what is required, what may stop it.' A greyed-out or rejected action MUST name the unmet precondition, the rule imposing it, and what would satisfy it. Treat an unexplained refusal as a charter violation, not a polish item.
  FAIL[Priority-flattened notification spam that trains players to ignore the channel] A reviewer reports that when 'a dozen or more' pop-ups arrived at once they 'took up a lot of screen space and ultimately made me ignore pop-ups altogether'. The community critique adds that toast notifications 'treat all information as equally important despite varying player priorities', and that  | LESSON: MERIDIAN's bottom event stream needs a priority model and player-editable per-class salience in v1. An event stream that cries wolf is worse than none, because it destroys the channel you later need for genuinely critical consequences.
  FAIL[Alerts hidden behind a collapsed counter that changes invisibly] 'Situations' live behind one button at the top of the screen. A new situation changes an integer from 8 to 9 — a change that does not catch peripheral vision. Understanding a single situation takes a minimum of four actions: click the button, find the item, hover it, read the tooltip. The critique:  | LESSON: Never encode 'something new demands attention' solely as a numeral increment. Reserve a persistent banner slot in MERIDIAN's shell for state changes requiring a decision, and count actions-to-comprehension for every alert class — four is too many.
  FAIL[Missing sorting and filtering makes whole game systems effectively unplayable] The trade routes tab shipped without sorting (you cannot sort by convoy usage or by trading partner), making it impossible to assess a trade agreement's value without manual tracking. The market members tab identifies countries by flag icon rather than name, forcing repeated lookups. Players also re | LESSON: The critique's own formulation belongs verbatim in MERIDIAN's design doc: 'if the UI bars people from engaging with a game system, that system is functionally prohibited from mattering.' An unsorted, unfilterable entity list means MERIDIAN's meso tier does not exist as far as the player is concerned
  XFER(strong) Colour may encode engine-computed fact (magnitude, confidence, visibility) but never uncomputed valence. When you want to draw the eye, encode HOW MUCH THIS MATTERS, not WHETHER THIS IS BAD. -> Bind into DESIGN-TOKENS as a hard rule with a review test: for every semantic colour, name the mechanism justifying the valence, or downgrade to a magnitude/neutral encoding. Governs the consequence-primitive readout, the eight confidence labels (categorical, not good/bad) and the macro indicator panels.
  XFER(strong) Persist the causal chain; do not rely on recomputing it. A model that can explain a number only while that state still exists cannot explain a collapse after the fact. -> Event-sourced history with retained causal_parents is the precondition for the Eight Questions being answerable at all. Every outcome record should store the rule that fired, the actors that reacted and the assumption set — because the question players actually ask is retrospective ('why did this happen'), not instantaneous ('what is this number').
  XFER(strong) If a player needs a fact on every visit to a screen, it is layout. Tooltips explain the derivation of a shown number; they never carry the number itself. -> Write into INTERACTION-PATTERNS. On a proposed action: who will resist, what it costs, how long, what may stop it are LAYOUT (charter-mandated). The modifier stack behind each actor's stance is drill-down. On a dossier: the confidence label is layout; the evidence trail behind it is drill-down.
  XFER(strong) Constrain the mechanism until it is explainable, rather than narrating an unexplainable mechanism. -> Paradox changed the political-movement MODEL so leader ideology 'can now actually be predicted and displayed'. Apply the same test at MERIDIAN's determinism boundary: if an engine behaviour cannot be explained from named mechanisms, simplify it until it can — do not wrap it in LLM narration. The constructive form of ADR-006.
  XFER(strong) Ship a dense, sortable, filterable table for every major entity class alongside every designed panel; players need comparison, ranking, trend and exception, and decorated panels deliver only current v -> A cohort/entity table with per-column sort and filter, plus an engine-generated exception report ('what changed, what is worsening, what resolved') for the meso tier — modelled on what the Victoria 3 community had to mod in. Add the column Victoria 3 has no analogue for: confidence, with Unknown and Restricted as present rows.
  XFER(strong) Model a contested process as visible phases with position, momentum, named opposition and a bounded failure budget — never as a single hidden probability. -> MERIDIAN's 'trigger an approval process' primitive and any contested intervention. Dock narrative events to the progress element that produced them, tagged with the outcome that spawned them, rather than dumping them in the global feed.
  XFER(strong) Prediction is cheap one hop out and expensive three hops out; state the forecast horizon explicitly rather than silently forecasting only what is easy. -> PLAN mode must distinguish engine-computed direct deltas from beyond-horizon consequences and mark the latter unknown rather than omitting them — satisfying Q6 (uncertainty) and Q7 (alternative outcomes), which Victoria 3 answers for neither.
  XFER(moderate) Promote to level one the two or three variables that actually explain an entity's next action; demote everything else. Fix over-nesting by promotion, not by adding a layer. -> Governs the person-dossier Overview tab and the entity hover card. Also the operational form of 'causal value, not fake depth': an attribute earns promotion only by naming the mechanism it drives.
  NOCOPY: The specific top-bar composition and its four named 'capacity' meters (Bureaucracy / Authority / Influence / Convoys) — this quartet is Victoria 3's signature economic-abstraction identity; The 'lens' vocabulary and the specific bottom-rail lens set (Production / Political / Diplomatic / Military / Trade) as named, iconised modes; The blue/gold imbalance-bar colour pairing and the market panel's s
## Dwarf Fortress (Bay 12 Games) — Classic ASCII and the v50 Steam UI overhaul
ONELINE: Dwarf Fortress is the only shipped product whose simulation is as deep as MERIDIAN's ambition, and it proves the central thesis empirically: for sixteen years the simulation ran fine while the *interface* failed to expose it, and a third-party ecosystem (DFHack, Dwarf Therapist, Legends Viewer) grew to become mandatory — which is the operational definition of the failure mode MERIDIAN must avoid.
  FAIL[Third-party tools became mandatory — the canonical failure] For most of DF's life, competent play required external software: Dwarf Therapist for labour and dwarf inspection, Legends Viewer for world history, DFHack for essentially everything else. DFHack now ships over 400 tools (as of v50.08-r3) and is described in the community bluntly as fixing 'all of t | LESSON: The existence of a mandatory third-party inspector is the measurable proof that a UI has failed to expose its own simulation. MERIDIAN should adopt this as an explicit test: for every simulation quantity the engine tracks, name the screen that surfaces it. Anything unnamed is a future Dwarf Therapis
  FAIL[Bulk operations were the specific missing capability] The base game exposed per-dwarf state one dwarf at a time; the community's answer was always a sortable table with mass edit. Even after the v50 redesign, the game shipped without search on major lists, and DFHack's 'sort' overlay had to add search and sort to: squad assignment, fort-wide info panel | LESSON: Damning and directly transferable: the 2022 overhaul fixed presentation and still shipped without search on its own lists. MERIDIAN must treat search, filter and sort as base infrastructure on every list surface, not as a later polish pass — and the sort criteria DFHack chose (effectiveness, stress,
  FAIL[The classic military interface — nested panes with mode-dependent keys] Three tabs, each with three panes (left/centre/right), where — per the wiki walkthrough — 'the same key will often do something different depending on what pane you're in'. Terminology is actively misleading: 'Alerts function as your effective schedules... while the schedules tab allows you to edit  | LESSON: Four named anti-patterns for MERIDIAN's own most complex screen (the command/plan composition screen, which will face exactly this pressure as the 16 consequence primitives accumulate): (a) never let an input's meaning depend on invisible focus; (b) never let two adjacent objects share a near-identi
  FAIL[Work Details confused exactly the audience it was built to serve] The system intended to make Dwarf Therapist unnecessary was itself reported as confusing, particularly by veterans. Community guides had to be written specifically to 'clear up confusion about the new system so players could do everything they want without needing extra tools'. Players continued to  | LESSON: A better abstraction is not self-evidently better at the interface. If MERIDIAN introduces standing policies over cohorts and institutions, it must (a) let the player see the concrete per-entity consequence of a policy in the same view, and (b) let the player NAME AND VISUALLY DISTINGUISH their own 
  FAIL[Keyboard-only operation regressed at Steam launch] The mouse-first redesign removed or reduced full keyboard navigation. Adams, describing what came next after launch, lists 'continuing to bring back classic functionality like keyboard support' as ongoing work. A 'Keyboard Cursor Enabled' setting exists in Settings > Game but defaults to false, and  | LESSON: Directly hits MERIDIAN's 'accessibility is not optional' commitment. A mouse-first command shell that adds keyboard operation later will ship a keyboard experience that is a translation rather than a design — and for a professional analytical tool, keyboard operation IS the expert path, not an acces
  FAIL[The announcement feed's placement undermined its own severity system] Despite the well-designed three-tier severity model, players reported that announcements are poorly integrated, with critical information (hostile attacks, workshop job failures) effectively hidden in a UI element that is barely noticeable, and no persistent on-screen feed. | LESSON: MERIDIAN's bottom event stream must be a persistent, scrollable, always-populated region — not a transient overlay. The severity model determines what ADDITIONALLY happens (focus, halt, modal); it must never determine whether the event is recorded on screen at all. Every event that occurred must be 
  FAIL[Legends mode remains a separate, mutually exclusive mode] Legends mode cannot be entered while a fortress or adventurer is active. To browse the history of the world you are currently playing in, you abandon or exit. In practice the community's answer was to export XML (up to ~1 GB for a 1000-year large world) and read it in Legends Viewer — which offers b | LESSON: Sharpest lesson in the whole study for MERIDIAN. MERIDIAN's causal history is not a post-mortem artefact — the Eight Questions demand it be inspectable DURING play. Build history browsing as an in-session panel that composes with the live run, never as a separate mode. And note that Legends Viewer's
  XFER(strong) The mandatory-third-party-tool test as an explicit design gate: for every quantity the simulation tracks, name the screen that surfaces it and the screen that lets you act on it in bulk. Any unnamed q -> Add to VISUAL-QA-CHECKLIST as a hard gate. Concretely: enumerate the ~16 consequence primitives and the world-model attributes, and require each to name (a) a dossier tab or panel that renders it and (b) whether it is filterable. MERIDIAN's own audit already found two criticals of the form 'the documentation claims a property the system lacks' — th
  XFER(strong) Search, filter and sort are base infrastructure on every list, and the sort criteria should be DERIVED, decision-relevant measures — not raw fields. -> Every MERIDIAN roster (entities, events, obligations, pending approvals, active consequences) ships with search and sort from its first version. Sort criteria should be things like 'likely to resist this action', 'exposure created', 'time-to-effect' — the analogue of DFHack's combat-effectiveness/stress/potential — because those are the criteria th
  XFER(strong) Every entity name inside generated prose is a hyperlink to that entity's canonical dossier; entity type is encoded consistently by colour on the link itself. -> Narration output — including LLM-narrated advisory text — passes through a linker that resolves entity references to dossier links. This converts flavour text into navigation, and it composes with the provenance rule: a linked, resolvable entity reference is checkable, while an unlinked proper noun in model output should be treated as suspect (poss
  XFER(strong) Cultural baseline vs personal deviation as an explicit two-class display, so the interface itself marks which attributes carry information. -> Dossier attribute rows render as 'typical for cohort' (muted, collapsible) or 'diverges from cohort' (prominent). This directly implements 'causal value, not fake depth' at the render layer and gives the meso tier a visible role inside micro-tier screens.
  XFER(strong) Notification severity as a behaviour contract (append / append+focus / append+focus+halt), configurable per event type, over a persistent always-present feed. -> The bottom event stream is persistent and complete; severity determines only what happens IN ADDITION. Ship per-event-type configuration early. Learn from DF's failure as well as its success: the tiering was right, the resting placement was reportedly too easy to miss.
  XFER(strong) History browsing is an in-session panel that composes with the live run, never a separate mutually exclusive mode requiring export. -> MERIDIAN's causal/event history must be inspectable mid-run — this is not a convenience, it is what the Eight Questions require. Note that this also constrains architecture: MERIDIAN currently discards causal_parents and has no event-sourced history, so the history panel is blocked on that engine work. Sequence accordingly.
  XFER(moderate) Named, saved policy objects with an explicit three-state (permit-selected / permit-all / FORBID-all), with the deciding evidence rendered in a column adjacent to the control. -> For standing orders over cohorts and institutions. Two mandatory additions DF lacks: the policy must display cost, lead time and expected resistance, and the player must be able to name and visually distinguish their own policy objects (DF's sequential roman numerals are a small real defect). An explicit prohibition state is worth having because 'f
  XFER(moderate) Split a combined dense page into tabs the moment two distinct question-types share it — DF's v50 explicitly separated thoughts from preferences, which had been one page. -> Concrete precedent for MERIDIAN's ten-tab dossier being the right call rather than over-engineering. But the corollary matters more: tab count should be driven by distinct question-types, so if two of the ten tabs answer the same question, merge them. Audit the ten proposed tabs against 'what question does a player open this to answer'.
  NOCOPY: Any dwarven/fantasy-fortress visual identity: the hand-drawn tile sets, stone-and-timber panel framing, or the specific pixel-art item iconography of the Premium release.; ASCII-glyph presentation used as an aesthetic signature. A dense text mode is a legitimate engineering option; DF's specific glyph vocabulary and colour conventions are its trade dress.; The name 'Legends' or 'Legends mode' for 
## Shadows of Doubt (ColePowered Games, early access 2023, 1.0 September 2024)
ONELINE: It is the only shipped product I could find that gives every simulated entity a first-class dossier window, lets the player physically assemble a causal argument from incomplete and unreliable evidence, and ships an explicit machine-readable "strength of link" model — and its failure modes are precisely the ones MERIDIAN's confidence-label system is exposed to.
  FAIL[Nothing meaningful auto-pins, and the tutorial implies otherwise] Reviewers report that the case board does not populate itself: the player must select the pin and manually place each clue. The tutorial reportedly gives an incorrect impression of how much the board does for you. | LESSON: If a surface has both automatic and manual behaviour, onboarding must state the boundary explicitly. For MERIDIAN: the player must never be unsure whether the engine has already drawn a causal link or is waiting for them to assert one. Render engine-asserted and player-asserted links in visibly diff
  FAIL[Non-unique descriptors against a single hidden correct answer] Side cases give descriptor sets ('green eyes, average height, shoe size 7'; 'green eyes, 0+ blood, first initial A') that match 5-8 citizens, but only one hidden citizen is the correct answer. Players arrest a perfectly matching citizen and are told they are wrong. | LESSON: This is the sharpest warning in the whole product for MERIDIAN. An 'Assessed' confidence label that does not disclose the size of the surviving candidate set is actively misleading. MERIDIAN's engine must compute and SHOW residual ambiguity — 'this narrows to 6 entities', not just 'Assessed'. A labe
  FAIL[No reverse index on the strongest forensic key] Fingerprints are the most probative evidence in the game and there is no fingerprint database to search. The Government Database is name-indexed only. Players resort to brute force: printing every citizen's record letter by letter, or physically scanning mailboxes hoping for a match. | LESSON: For every attribute MERIDIAN exposes on a dossier, ask which index lets a player search BY it. Attributes you can read but not query are dead ends dressed as leads. If restricting an index is deliberate (access control), the UI must say so — that is exactly the 'Restricted' label doing real work rat
  FAIL[Case boards cannot be merged, and evidence transfer is manual and lossy] If a player realises two open cases are the same case, there is no merge. Evidence must be unpinned from one board, the board switched, then repinned — and a pinned item will not unpin if you try to drag it directly to another board. Resolving one case does not apply to the other. | LESSON: MERIDIAN must make the ASSESSMENT a view over a global evidence store, never an owner of it. A player who reframes a situation ('these two incidents are one operation') must be able to re-partition without re-collecting. This is a data-model decision made at design time that no amount of UI polish f
  FAIL[The board becomes visual spaghetti and reviewers found it repetitive] Reviewers report the pinboard felt underwhelming as case count rose, and that immersion "comes crashing down when it comes to executing the necessary interface steps to make these connections." The board is a free-form 2D plane with no layout assistance, no auto-arrange, no filtering, no zoom-to-clu | LESSON: MERIDIAN should ship the same graph in at least two engine-generated layouts — chronological (the Timeline tab) and causal/hierarchical — plus the free canvas. Never make a free canvas the ONLY view of a growing graph. Also: every additional click to assert a link taxes hypothesis formation; link cr
  FAIL[Players had to invent the colour taxonomy the interface should have supplied] Community guides teach schemes like 'one colour for City Hall and official records, one for the victim's circle, one for weapon sellers, one for suspects, one for dead ends'. The game supplies colour as a raw capability with no semantics and no legend. | LESSON: Directly contradicts nothing in MERIDIAN's thesis but sharpens it: 'semantic not decorative colour' must mean the ENGINE owns the semantics. Give players annotation, but never let colour be the primary carrier of a distinction the engine already knows (confidence, provenance, authorship). Player-cho
  FAIL[Identity auto-update fires on weak evidence] An unknown card's name updates as soon as the game's matching logic is satisfied, including when the player has nothing they would regard as conclusive. The reviewer: "This 'name auto-update' mechanic is a bit annoying because it updates even if you do not have anything concrete, there were many peo | LESSON: This is the resolution-of-unknowns mechanism failing in exactly the way MERIDIAN is most exposed to. Do not let a MERIDIAN unknown-entity node resolve to a named entity silently. Resolution must be a labelled, reversible, confidence-carrying event ('provisionally identified as X — Assessed, 6 candid
  XFER(strong) Encode inferential link strength, link reliability and causal direction on three SEPARATE visual channels, all engine-computed -> MERIDIAN's intelligence-assessment tab and any causal-chain view: link weight = magnitude of the causal claim, link colour/label = confidence label, animation or arrowhead = direction of causation. Never merge them into one 'importance' visual. Engine owns all three; the LLM may narrate them and may not set them.
  XFER(strong) Unknown entities are first-class persistent nodes that accumulate assessments and later resolve in place, preserving their link history -> MERIDIAN's player intelligence profile (view 4) must support anonymous actor nodes. Resolution to a named entity is a logged, reversible, confidence-carrying engine event — not a silent relabel. This is the operational form of 'Unknown must be a present row'.
  XFER(strong) An attribute earns a dossier row only if a named mechanism CONSUMES IT AS A KEY -> Apply as a hard gate on the ten person-dossier tabs: for every field, name the query, filter or behaviour rule that reads it. Fields with no reader move below the fold or get cut. This makes 'causal value, not fake depth' testable rather than aspirational — Shadows of Doubt shipped ~23 attributes of which ~6 were actionable and players noticed.
  XFER(strong) A confidence assessment must disclose residual ambiguity, not just evidential quality -> Every 'Assessed'/'Reported' label in MERIDIAN should be able to carry a candidate-set size or a discriminating-power indicator where the assessment is an identification or attribution. Without it the label manufactures false certainty — the exact defect that produced Shadows of Doubt's most-complained-about failure.
  XFER(strong) Separate the exhaustive evidence store from the curated argument canvas, and make the canvas a VIEW not an owner -> MERIDIAN's assessment surface must be a re-partitionable projection over the global versioned-input/event store. A player who merges or splits two situations must not lose or re-collect evidence. This is a data-model commitment, and it interacts with the (currently absent) event-sourced history: without event sourcing, this pattern cannot be implem
  XFER(strong) Divergence between what an entity believes and what is true must always be produced by a NAMED mechanism -> MERIDIAN's four views: every difference between view 1 (authoritative) and views 2/3/4 must be attributable to a specific mechanism — observation channel, decay function, incentive to misreport, access restriction. Two of the eight labels ('Outdated', 'Possibly deceptive') get their mechanical grounding this way. If a divergence has no mechanism, i
  XFER(moderate) Query capability is scoped by institutional access, and query RESULTS are timestamped artefacts rather than live views -> MERIDIAN's records/search tools should return a provenanced snapshot: 'as of tick N, via channel X'. This gives 'Outdated' real semantics and makes the (currently non-existent) auth layer load-bearing rather than cosmetic. Honest caveat: MERIDIAN has no auth layer today, so this pattern is blocked until player role resolves against something.
  XFER(moderate) Commit-under-uncertainty with per-component grading against authoritative reality -> Let the player lodge a structured assessment (actor / action / mechanism / timeframe) and have the engine score each component after the fact. This turns the view-4-vs-view-1 gap into a scored artefact and gives MERIDIAN an after-action surface that is impossible to fake with narration.
  NOCOPY: The corkboard/pinboard surface itself, and the detective-noir skeuomorphic framing (manila case folders, thumbtacks, pins); Red string / physical twine as the link visual, including the string-width and fraying vocabulary; The pseudo-Windows desktop chrome inside the world view (title bars with an 'X', minimise-to-taskbar behaviour, window shadows on cork); Yellow numbered crime-scene placards; Th
## Command: Modern Operations (WarfareSims / Matrix Games, 2019; successor to Command: Modern Air/Naval Operations, 2013)
ONELINE: CMO is the only mass-market product that has fully solved the problem MERIDIAN calls "the intelligence product": it maintains a rigorous, permanently visible distinction between an entity that exists and a *contact* — the observer's uncertain, provenance-tagged, decay-prone belief about that entity — and it makes that uncertainty numerically actionable by letting the player set policy on how much ambiguity is tolerable before acting.
  FAIL[Irreducible map clutter — density with no editorial hierarchy] CMO renders essentially everything on one surface: unit icons, contact icons, datablocks, movement vectors, sensor range rings/arcs, weapon range rings, AoUs, plotted courses, mission areas, targeting vectors, datalinks, contact emissions, reference points, sonobuoys, LOS overlays and message balloo | LESSON: Layer toggles are not information hierarchy. MERIDIAN must ship curated VIEW PRESETS (named, switchable, each answering one question) as the primary control, with per-layer toggles as an advanced escape hatch — not the reverse. MERIDIAN's stated 'three layers of progressive disclosure' must be enfor
  FAIL[Doctrine/ROE is powerful but effectively undiscoverable — the community had to write a troubleshooting genre] Units silently fail to act, and the cause is almost always a doctrine/ROE/WRA/EMCON/altitude/loadout constraint several dialogs away from where the player is looking. The manual devotes a section titled '9.2.8 My @#%^@#% weapon won't fire!!!' to this. A widely-used player guide exists solely to expl | LESSON: A brilliant explanation the user must know to ask for is a failed explanation. MERIDIAN must PUSH blocker state: when an ordered action is blocked or degraded by a constraint, that must surface unprompted in the event stream and on the entity/order itself, with the named constraint and its owning le
  FAIL[The refusal window reports one blocker at a time] When several conditions simultaneously prevent an action, the manual weapon allocation window surfaces only one, so the player fixes it, retries, and hits the next — an iterative guessing loop. | LESSON: MERIDIAN's validator must collect and return the FULL blocker set for a proposed composition, not the first failure. 'What may stop it' is a list, and a partial list is worse than useless because it implies a false remaining path.
  FAIL[Widget vocabulary inherited from business software — the developers' own diagnosis] CMANO v1 'appeared as a "traditional" business application like MS Office, rather than a bonafide game': light grey Windows chrome, dense tabbed property dialogs, sortable grids. CMO's fix was substantially a re-skin to a dark palette plus a colour-coded shortcut bar. | LESSON: Directly validates MERIDIAN's '70% conventional usability underneath, near-future styling on top' thesis — but with a warning. CMO's experience shows a dark theme buys real goodwill (reviewers consistently praised it) and changes nothing structural. MERIDIAN must not let the cinematic 10% substitute
  FAIL[Runaway simulation under time compression (dev-acknowledged, subsequently fixed)] In CMANO v1, high time acceleration combined with no message pop-ups let the simulation 'advance beyond the player's control' — the player would fast-forward and discover the situation had resolved without them. | LESSON: For MERIDIAN this is a named, avoidable failure. A crisis sim whose selling point is delayed second- and third-order effects is MAXIMALLY exposed to it: the interesting consequence arrives at t+6 months and the player fast-forwarded past it. Ship bounded advance and event-triggered auto-pause from d
  FAIL[Message deluge (dev-acknowledged, partially fixed)] CMANO v1 presented 'a deluge of information being presented to (some would say "poured upon" or "flooded") the player.' | LESSON: The event stream will drown the user by default. MERIDIAN must design the taxonomy, grouping, read-state and routing matrix BEFORE the stream ships, because retrofitting a taxonomy onto an existing free-text log is the expensive path CMO had to take.
  FAIL[Tutorials reported as unhelpful and in places inaccurate] Multiple player accounts report that the tutorials do not adequately bridge to competent play, and reviewers describe the learning curve as extreme. | LESSON: MERIDIAN's conceptual model (belief vs truth, primitives vs actions, the determinism boundary) is at least as unusual as CMO's. Onboarding must teach the MODEL, not the controls. The specific technique worth stealing is CMO's opposite — teaching by refusal messages that name the governing rule, whic
  XFER(strong) Belief objects are separate from truth objects, with their own identity generator that encodes exactly the known attribute set (CMO: 'Skunk #4' vs 'TAKR Admiral Kuznetsov'). -> Every MERIDIAN entity gets a parallel per-observer contact record. The dossier renders the contact record. Unidentified entities get engine-generated placeholder identities describing only known attributes. Never render an unknown entity as a real entity with fields greyed out — render it as a distinct object class.
  XFER(strong) Uncertainty is a QUANTITY with spatial or numeric extent that grows with time-since-observation, not merely a categorical label. -> Pair each of MERIDIAN's eight confidence labels with an engine-computed dispersion for continuous attributes (±£, ±months, ±strength), rendered as a visible range, and decay it with elapsed time absent new evidence. 'Assessed: 40-70% capacity (last corroborated 11 weeks ago)' rather than 'Assessed'.
  XFER(strong) A standing, inheritable player policy that sets how much uncertainty is tolerable before acting, with defined arithmetic (CMO's Engage Ambiguous: Ignore / Optimistic ≤3× tolerance / Pessimistic ≤1× to -> An evidence-threshold setting in PLAN/COMMAND mode, inheritable from world → institution → operation, that the engine checks during validation and cites by name when it blocks an order. This is MERIDIAN's answer to 'what happens when the player commands an action against a merely Reported entity'.
  XFER(strong) Refusal as structured data: named blocker type + both sides of the failing comparison with real numbers + the named setting/authority that imposed it + a remedy + the remedy's cost. -> The exact output contract for MERIDIAN's engine validator. Define a closed blocker enumeration (authority absent, budget insufficient, lead time, prerequisite unmet, actor resistance, evidence threshold) in the engine; the LLM may only render blockers the engine raised. Return the FULL blocker set, never just the first — CMO's one-at-a-time behavio
  XFER(strong) Provenance is a navigable panel row, bidirectionally linked: to the claimant and to the claimant's instrument/capability to know (CMO: 'Classification by: Fishbed #1 [Sensor: Mk1 Eyeball] at 3.6 nm',  -> Every non-Confirmed dossier row carries a source link and a source-reliability link. Strengthens MERIDIAN's PROVENANCE RULE from 'tag AI text' to 'make every fact's origin walkable'. Hard prerequisite: stop discarding causal_parents; ship event sourcing.
  XFER(strong) Evidence and inference rendered as separate panes: observed indicators on one side, the enumerated candidate interpretations on the other, each candidate linked to its implied entity type. -> The dossier's 'Intelligence assessment' tab renders an engine-computed hypothesis SET, not a conclusion. Gives a clean determinism boundary: engine computes candidates, model narrates them. Also satisfies eight-questions item 7 (alternative outcomes) applied to identity.
  XFER(strong) Time control split into two orthogonal controls: rate (multiplier) and bounded duration (run-then-pause), plus per-event-class auto-pause. -> Global status bar gets a rate control AND a bounded-advance control ('advance one week', 'advance to next decision point', 'advance until event of type X'). Non-negotiable for a sim whose value is delayed second/third-order effects. Requires an interruptible bounded-advance API with stop-condition predicates.
  XFER(strong) Per-event-class × per-channel routing matrix (CMO: Message Type / Show in log / Raise pop-up + pause / Show balloon), with a taxonomy granular enough to be worth filtering. -> MERIDIAN's event stream gets the same matrix, with channels stream-only / surface in context panel / interrupt-and-pause. Requires defining the event taxonomy at player-meaningful granularity ('regulator opened inquiry', not 'institutional reaction') before the stream ships.
  NOCOPY: NATO APP-6 / MIL-STD-2525 joint military symbology and CMO's NTDS icon set, and the 'Directional Stylized' alternative — MERIDIAN must invent an original symbol grammar for entity class, affiliation and confidence.; The Skunk / Bogey / Goblin contact-naming vocabulary — steal the PRINCIPLE (a generated name encoding exactly what is known) and invent MERIDIAN's own civil-domain lexicon.; The dark c
## Power & Revolution / Geopolitical Simulator 4 (Eversim) — with predecessors Rulers of Nations / Masters of the World and successor Geo-Political Simulator 5 (2024/2026 Editions)
ONELINE: Eversim has already built the "everything simulator" MERIDIAN describes and shipped it for eighteen years; its interface is the clearest available demonstration of MERIDIAN's named failure mode — enormous causal depth rendered as an undifferentiated menu tree, governed by a hero metric that cannot explain itself, with the true causal variables sold separately as a paid debug add-on.
  FAIL[Everything is a menu, and depth is navigational rather than conceptual] The entire action space — advertised by Eversim as over one thousand playable actions across 175 playable countries with over 600 variables each — is reached through one uniform path: seven fixed buttons along the bottom of the screen (budget and finances; national economy including energy and envir | LESSON: MERIDIAN must not let the domain model dictate the navigation model. If the simulation has N systems, the shell must expose a small fixed number of goal-shaped entry points and reach depth by context and search, not by an Nth menu. The owner's stable command shell is the right instinct; the risk is 
  FAIL[A single hero metric that does not explain, and does not even predict, the terminal outcome] Popularity is rendered in large numerals beside a stream of smiley faces at top centre and is described by the manual as 'the most important number in the game', with the warning that if it approaches zero the player will probably lose. Yet Steam discussion threads exist with the titles 'Deposed wit | LESSON: MERIDIAN should be extremely reluctant to crown any single scalar. If the global status bar carries headline indicators, each must be tied to the specific mechanisms that can end or transform the run, and the interface must never present an indicator as dominant unless the engine can show it dominat
  FAIL[Sentiment tokens substituted for causal explanation] Public reaction is conveyed by smiley faces streaming across the top centre of the screen, showing daily approval or disapproval by theme (security, health, education and so on). The manual concedes that a popularity change of exactly 0% can still be accompanied by smileys, because positive and nega | LESSON: This is precisely MERIDIAN's Eight Questions rule, validated negatively by a shipped product. Sentiment iconography without an attached causal chain is flavour text. Under MERIDIAN's own doctrine such output must not be permitted to imply it modified the simulation — and if the engine cannot answer 
  FAIL[The causal truth is a paid add-on] The variables that would answer 'why did that happen' are exposed by God'n Spy, a separately purchased add-on that surfaces internal hidden engine variables through buttons bolted onto roughly twenty-eight different screens. | LESSON: For MERIDIAN this is close to an existential warning. If explanation is architecturally separable from the main interface, it will be separated — into a debug view, a log tab, a documentation footnote. MERIDIAN's provenance rule already anticipates this by demanding tagging 'at the interface level a
  FAIL[Redesigning the surface without fixing the architecture, then shipping it as the headline feature] Geo-Political Simulator 5 (released 22 October 2024, £49.99) advertises that 'The interface has been completely redesigned and revamped to be more dynamic and user-friendly', adapting to 4K and dual-screen setups, with user-selectable interface scale, pre-positionable game windows, and 'a generalize | LESSON: The most important single finding for MERIDIAN. The owner's thesis of 'near-future command-system styling on top' is only safe if the layer underneath is genuinely solved first. A visual system laid over an unresolved hierarchy is not 70% conventional usability — it is 0% usability with a good skin,
  FAIL[Duplicate action paths with no canonical home] Right-clicking the map opens context menus that vary by what is under the cursor — a foreign country offers an appointment with its head of state, one of your own regions offers construction, a city offers creating a festival. The manual states outright that 'This right-click menu is a shortcut: all | LESSON: MERIDIAN's contextual affordances (map selection, entity right-click, conversational COMMAND mode) will multiply the routes to any given action. Each action needs one canonical location that the interface can always name, with every other route presented as a shortcut TO that location — and the prim
  FAIL[Perishable, unstable information] The left edge of the screen carries a stack of requests from national and international figures. The manual states that requests have a limited life and that if the player does not read them they disappear after a few days. Overflow is handled by a bolt-on: when too many requests exist to display, a | LESSON: MERIDIAN's bottom event stream must not be the only home of any consequential item. Persist everything into an addressable, sortable, filterable inbox; let the stream be a view onto that store, never the store itself. Nothing that can influence an action should expire silently — MERIDIAN's replay-pr
  XFER(strong) Show institutional resistance as a named, per-actor forecast attached to the specific proposal, before commitment, with reversal offered at that moment and a declared political cost for reversing -> MERIDIAN's PLAN-to-COMMAND transition should render, for any composed action, an engine-computed panel of which institutions and cohorts will resist, at what strength, and via which named mechanism — with an explicit cost attached to withdrawing a proposal already tabled. This is the interface expression of the charter promise 'who will resist', an
  XFER(strong) Separate edit, preview and commit, with the preview visually marked as a projection and the delta shown continuously at the point of edit -> All parameter manipulation in MERIDIAN happens in a provisional layer where indicators reposition live and the aggregate cost delta is always on screen; only an explicit Confirm crosses the determinism boundary into engine mutation. This is a direct interface encoding of ADR-006 and makes the boundary visible rather than documentary.
  XFER(strong) A hero metric that cannot predict the outcome it is supposed to summarise is worse than no metric at all -> Audit MERIDIAN's global status bar candidates against the question 'can the engine show this indicator driving a named terminal or transformative outcome?' If not, it does not get top-of-hierarchy placement. This constrains the macro-indicator layer that already has an open critical finding for monotonic saturation.
  XFER(strong) Every event item in a stream should be traversable upward to its causal parent, and downward to the affordances that respond to it -> MERIDIAN's bottom event stream items each expose (a) 'what caused this' navigating to the parent event and (b) the actions available in response. This is Eight Question #2 rendered as navigation. It requires MERIDIAN to stop discarding causal_parents — currently a known gap — so it is a design commitment with an engine prerequisite.
  XFER(strong) Encode the player's knowledge state in the same visual system as world state, so both are read in one pass -> MERIDIAN's dossier renders confidence labels and visibility state as first-class row-level furniture in the same register as the facts themselves, rather than as a separate legend or metadata panel. Eversim's investigation and file glyphs are a two-state ancestor of MERIDIAN's eight labels on two axes.
  XFER(strong) Enumerate plural mechanically distinct routes to the same goal, each with its own prerequisite, cost and failure mode -> PLAN mode should show the composition SPACE — several distinct primitive compositions that reach the target state, priced differently and gated by different prerequisites — rather than a single LLM-chosen composition. This is what stops the interface collapsing back into a menu of pre-authored options while still being legible.
  XFER(strong) Make composed actions first-class, player-named, persisted objects, and express legality as the presence or absence of an affordance derived from the world's own rules -> A player-named package of consequence primitives becomes an inspectable object in MERIDIAN with its own history, forecast and provenance. Constitutionally unavailable routes simply do not render, with an inspectable reason available on demand rather than an error on attempt.
  XFER(moderate) Give every displayed indicator a default baseline (value at scenario start) so magnitude is legible without opening a chart -> Every macro, meso and micro indicator in MERIDIAN renders as current value against its scenario-start value by default, with the time series one click deeper. Cheap to build, and it makes the three-layer disclosure model work at layer one instead of forcing users to layer two.
  NOCOPY: The desk/office metaphor as a navigation device — clicking your own portrait frame to open a 'desktop' whose bottom edge carries an F1–F8 icon row (welcome, score, agenda, newspaper, budget, graphs, sound, saves, exit).; The stream of smiley faces across the top centre of the screen as the primary readout of public sentiment, and the large popularity numeral sitting beside it.; The 'teleprinter' a
## Football Manager (Sports Interactive / SEGA) — series, with emphasis on FM22–FM24 (mature legacy UI), FM25 (cancelled), FM26 (Unity rebuild)
ONELINE: FM is the only mass-market product that has shipped and refined over 30 years the exact interface MERIDIAN needs — a dossier system over hundreds of thousands of persistent people where the screen renders what your staff believe, never ground truth, with uncertainty encoded as a first-class visual channel rather than a footnote caveat.
  FAIL[FM25 was cancelled outright, explicitly over interface quality] After multiple delays, SI and SEGA cancelled Football Manager 25 entirely — breaking a release streak of roughly three decades — with the interface named as a primary cause. | LESSON: For MERIDIAN: the interface is not a layer on top of the simulation, it IS the product, and a company with 30 years of domain expertise and SEGA's resources could not rebuild theirs on schedule. MERIDIAN should treat the visual system documents (VISUAL-CONSTITUTION, DESIGN-TOKENS, etc.) as load-bear
  FAIL[FM26 shipped the rebuild and was rejected by the existing user base] The successor release launched to heavily negative user sentiment specifically citing the interface. | LESSON: A correct redesign delivered without a transition path reads to existing users as a regression. MERIDIAN has no legacy user base yet, which is an advantage to spend deliberately: establish the navigational model EARLY and treat it as an API with a deprecation policy, because the cost of changing it 
  FAIL['Fewer screens' increased effective click-depth] SI's stated goal was to reduce the volume of screens and make navigation easier. Users reported the opposite: facts that were previously one click away now require several, with league tables and statistics 'buried several screens deep'. | LESSON: This is the most important single warning for MERIDIAN's three-layer progressive disclosure model. Measure INTERACTIONS-TO-FACT for a defined set of expert tasks, not screen count. Progressive disclosure must be additive (a summary that expands) rather than substitutive (detail relocated behind the 
  FAIL[Console-first interaction patterns imposed on desktop] Panels built as carousels are reported as far easier to manipulate with controller triggers than with mouse and keyboard, despite developer statements that console was not prioritised. | LESSON: MERIDIAN is desktop-first professional software. Design for the precise pointer, the scroll wheel, right-click context menus, multi-select and keyboard traversal. Do not adopt a component vocabulary constrained by input modes MERIDIAN does not need to serve.
  FAIL[Inconsistent information density — crammed and empty on the same product] Some screens display data 'crammed so tightly you can't even read half of it' while others waste large areas on empty space. A specific example: in press conferences, the options to refuse a question or leave are buried at the bottom behind 'pointless pop-up menus' and 'vast expanses of wasted space | LESSON: MERIDIAN's DESIGN-TOKENS must define at least two explicit density modes (a dense data-grid mode and a comfortable reading mode) with rules for which surfaces use which, rather than one spacing scale applied uniformly. And the primary action on any screen must never be positioned below secondary con
  FAIL[Attribute-number reductionism and false precision] The 1-20 attribute grid encourages users to optimise numbers rather than evaluate footballers, and creates an impression of precision the simulation does not support. Two players with nearly identical grids can perform very differently, and the interface offers no way to see why. | LESSON: This is a direct threat to MERIDIAN's charter. If entity dossiers expose comparable numeric attribute vectors, players will optimise vectors and the 'causal value not fake depth' rule will be defeated at the interface layer even if the engine is sound. Mitigation: prefer qualitative bands with an in
  FAIL[The confidence encoding is powerful but not self-explaining] The star-colour system carries three or four distinct semantics (value / uncertainty / range / scale-band) and users demonstrably do not agree on what they mean. In a fetched Steam thread a player confidently explains black stars as 'the estimated potential', which conflicts with the specialist-site | LESSON: For MERIDIAN this is decisive: encode confidence visually for scanning, but ALWAYS pair the visual with a text label on hover/expansion, and make the label the authoritative form. MERIDIAN's eight labels are named for exactly this reason — do not let a colour channel become the primary carrier of a 
  XFER(strong) Render ignorance in-place at equal visual weight: the degraded form (range, 'Unknown', 'Restricted') occupies the same cell geometry and same row position as the confident form. -> Implement directly in the entity dossier and all entity tables. A row whose confidence label is Unknown renders as a full row with the label in the value slot, not as a hidden or dimmed row. This makes MERIDIAN's 'absence of knowledge is content' rule mechanically enforceable and gives it a concrete acceptance test: for any dossier, the set of rows
  XFER(strong) Two-channel encoding of value and confidence in a single glance-readable widget, so confidence survives compression into dense tables. -> Design a MERIDIAN-original (non-star) widget carrying an assessed value plus an engine-computed confidence channel, embeddable in a table cell. Keep the eight named labels as the authoritative semantic and always expose the label as text on hover/expansion — never let the visual channel be the only carrier. Distinguish epistemic uncertainty (we hav
  XFER(strong) Authoritative ground truth is computed, drives everything, and is structurally unreachable from any player-facing surface. -> Treat view 1 (authoritative reality) exactly as FM treats CA/PA. No debug toggle in the shipped build should reveal it, because a toggle that exists will be used and will then be relied upon. Enforce architecturally: the projection layer that produces view 4 should be the only code path any UI endpoint can call. This also gives dossier property D-1
  XFER(strong) Every assessment is attributed to a named, persistent, fallible assessor whose own competence is inspectable, and improving your assessors improves your whole information layer. -> MERIDIAN's intelligence profile should name its sources as simulated entities with their own reliability attributes, clickable through to their own dossiers. This grounds the 'Disputed' and 'Possibly deceptive' labels in mechanism rather than authoring, satisfies the provenance rule with a stronger artefact than a badge, and turns epistemics into a
  XFER(strong) Assessments are expressed relative to the observer's own situation rather than on an absolute universal scale. -> Never present an entity's capability or influence as an absolute score. Present leverage, exposure and access relative to the player's current actor and current objective. Besides being more decision-useful, this materially reduces the dual-use surface: a relative 'this actor constrains your current option set' is not a targeting metric in the way 
  XFER(strong) The assessment document is DATED, FROZEN and ATTRIBUTED — a historical artefact, not a live readout. -> MERIDIAN's Intelligence assessment tab should render dated report objects, each capturing the assumptions and confidence at the moment of production, with a visible age. Stale reports should visibly age (feeding the 'Outdated' label). This answers Eight Questions 5, 6 and 7 almost for free, and creates a natural home for model-narrated prose that i
  XFER(strong) Search results are gated by what the observer's collection actually covers, so the search interface cannot leak omniscience. -> MERIDIAN's entity search must operate over the player intelligence projection, not the authoritative entity store, and must be incapable of returning or ranking on attributes the player has not established. Standing collection instructions (FM's Recruitment Focus) map exactly onto MERIDIAN's intelligence model and give the player a legitimate lever
  XFER(strong) A hard, engine-determined blocking class in the event stream that prevents time advance until resolved, distinct from informational items. -> Classify MERIDIAN's bottom event stream into informational / consequential-optional / blocking. Reserve blocking for events where advancing would silently forfeit an option — computable from the 'enable/remove future options' primitive. This lets the stream carry high volume without losing consequential items, and it is enforcement by mechanism rat
  NOCOPY: The star-rating widget in any form — gold/silver/black/white stars are FM's signature visual identity for graded assessment. MERIDIAN must invent an original geometry for its value+confidence encoder.; The 1-20 attribute scale rendered as a three-column Technical / Mental / Physical grid — this specific layout and scale are FM trade dress and also carry the reductionism failure mode.; The A+ to E-
## Shadow Empire (VR Designs / Slitherine)
ONELINE: Shadow Empire is the closest existing product to MERIDIAN's actual problem — an engine-authoritative simulation whose interface must expose named persons, institutional factions, budgets, logistics and second-order consequences without lying about what the player knows — and it has already solved several of MERIDIAN's specific problems (unknown-as-rendered-content, engine-computed suitability scores, pre-commitment odds preview reconciled with resolution, post-hoc roll disclosure) while failing visibly at exactly the places MERIDIAN is most at risk (causal opacity, report findability, and a predictive display that players stopped trusting).
  FAIL[Combat odds that players learned not to believe] The displayed odds for a battle are frequently and dramatically wrong relative to outcomes, and the post-battle Details screen does not adequately explain the gap. | LESSON: An estimate the player cannot reconcile with the outcome is worse than no estimate — it converts a legible system into a capricious one. If MERIDIAN shows a cost/probability/timeline projection for a proposed action, the resolution must be reconcilable to it *item by item* in the after-action view. 
  FAIL[Causal opacity in logistics — the overlay shows the symptom, not the cause] Players can see that supply is failing but not why. The Explorminate reviewer says outright there 'just isn't enough obvious in-game feedback as to exactly what is happening to cause blockages in your supply chain', and that new players 'will fall foul of this more often than they'll like'. This is  | LESSON: Rendering state is not explaining state. The user-facing question is never 'what colour is this hex' but 'which constraint is binding, and what is the cheapest way to relax it'. MERIDIAN's propagation views must name the binding constraint (which approval, which budget line, which lead time, which i
  FAIL[Reports as a findability problem] The report estate is simultaneously the game's best feature and a maze. Critical numbers are reachable but not locatable. | LESSON: MERIDIAN will generate more report surfaces than Shadow Empire has, faster, because it has three tiers and four views per entity. Two counters are needed from day one: (a) a global search/command palette over every named engine quantity and entity, and (b) a rule that any number shown anywhere is de
  FAIL[No deep links out of decision text] Decisions present consequential choices but do not link to the entities they reference. The reviewer's specific complaint: clicking a city's name in a decision should take you to that city, and being asked a question about a political party should not require exiting the menu to go research the fore | LESSON: This is a data-model failure surfacing as a UI failure, and it is the exact failure MERIDIAN is set up to repeat if narration emits prose rather than prose-with-entity-references. MERIDIAN's rule should be: **narrated text is a typed document containing entity references, and every reference renders
  FAIL[The leader system's effect on the world is invisible] A reviewer states that information about 'how much impact leaders actually have on their area' is 'almost completely absent'. Leaders are the game's headline differentiator and the player cannot read their contribution. | LESSON: The most direct warning in this report for MERIDIAN. MERIDIAN's entity model is the whole differentiator and its named failure mode is already 'enormous biographies that never affect the simulation'. Shadow Empire shows the adjacent and equally fatal variant: **attributes that do affect the simulati
  FAIL[Hover and click deliver different payloads, undocumented] 'Mouseover often hands you different information than left-clicking on the same menu item' — and the manual does not say so. Some clickable elements have no visual affordance (technologies in the Technology Overview are clickable but not underlined, whereas underlined words elsewhere signal a toolti | LESSON: MERIDIAN's three-layer progressive disclosure needs a written, enforced contract: layer 1 is the value, layer 2 is always the derivation, layer 3 is always the full surface — the same everywhere, with a single affordance vocabulary. Belongs in INTERACTION-PATTERNS as a hard rule, and in VISUAL-QA-CH
  FAIL[Inconsistent scroll and input behaviour] Some scrollbars respond to the mouse wheel, some must be dragged. No click-and-drag map scrolling. No WASD (A is bound to artillery, S to strategic movement). No rebindable keys. | LESSON: Low-glamour, high-cost. Every one of these is a per-interaction tax paid thousands of times per session, and they are the complaints that appear in *every* review of the product. For MERIDIAN: one scroll container component, one keymap registry, rebinding from v1. These are cheap before there is a U
  XFER(strong) Preview and resolution must share one code path; divergence is a defect class, not a tolerance -> MERIDIAN's charter promises a pre-action briefing (what is required, what may stop it, what it costs, how long, what consequences). Bind that briefing to the same engine functions that later validate, price and resolve the action. Add a correctness property alongside D-1/D-2/D-3: 'P-x: the pre-commitment projection for an action and its post-resolu
  XFER(strong) Visibility is enforced once, at a projection chokepoint, and reports leak before the main view does -> Apply the observer's visibility filter in a single projection function that every renderer — dossier, list, report, export, narration prompt — must call. Shadow Empire spent years patching fog-of-war leaks specifically in *reports* (battle reports, trade-house overviews, minimaps) long after the map was correct. MERIDIAN should assume the same and 
  XFER(strong) Unknown gets a reserved visual treatment and an occupied slot, never a blank or a plausible default -> MERIDIAN's rule that Unknown and Restricted render as present rows needs a concrete visual contract. Reserve exactly one treatment for 'not knowledge' (Shadow Empire uses white text in a field that is otherwise coloured), use it nowhere else, and never substitute an estimate for an unknown without labelling it as a guess (Shadow Empire renders enem
  XFER(strong) Composite scores must carry a written exclusion list, and capability must be orthogonal to loyalty -> Any single-number index MERIDIAN shows (fit for a role, institutional capacity, threat) must be engine-computed with a documented, UI-inspectable definition of what it excludes. Shadow Empire explicitly removed Relation and one-off advisor bonuses from Suitability so the number means capability and only capability. For MERIDIAN this also protects t
  XFER(strong) Every attribute in an entity profile must name the mechanism that consumes it — and the profile must show what the entity is currently doing to the world -> Shadow Empire passes the first half (40+ skills, all consumed) and fails the second (reviewers cannot tell what leaders actually do to their zones). MERIDIAN should require both: a machine-checkable link from each dossier attribute to a named mechanism, AND a panel on the Overview tab listing the entity's current signed contributions to the simulat
  XFER(strong) Narrated text is a typed document containing entity references, not a string -> Every entity, place, institution, obligation or figure mentioned in generated or authored text renders as an inspectable reference into the dossier. This fixes the concrete usability failure reviewers named in Shadow Empire (a city name in a decision that you cannot click), and independently it constrains the LLM: if narration must emit references 
  XFER(strong) Render the flow, name the binding constraint, preview the next step -> For MERIDIAN's propagation views (budgets, procurement lead times, approval chains, institutional capacity, media reach), the overlay is the cheap part. The valuable parts are the Bottlenecks equivalent — 'this proposal is blocked by approval X held by institution Y, whose lead time is Z' — and the Preview equivalent — 'here is next tick's state if
  XFER(moderate) Advisory text is attributed to a named in-world source whose reliability the player can independently inspect -> Beyond MERIDIAN's mandatory provenance tag, route advisory/LLM narration through an in-world advisor entity carrying a visible track record and known biases. Shadow Empire's Vidcom attributes advice to a specific leader with a visible Relation score and faction colour, which lets the player discount it appropriately. For MERIDIAN this makes provena
  NOCOPY: The retro late-1990s/early-2000s visual treatment — flat grey panels, dense unstyled tabular text, bevelled buttons. Reviewers identify it as a primary entry barrier and MERIDIAN's credibility depends on looking as rigorous as it claims.; The specific five-bar chrome signature (top bar + top tab row + left bar + right bar + multi-row bottom tab stack) as a *layout copy*. MERIDIAN's shell should be
## Old World (Mohawk Games / Soren Johnson, 2021–present)
ONELINE: Old World is the closest existing proof that a simulation of opinionated, related, mortal characters can be made legible without a Paradox-style wall of modifiers — and its two defining mechanisms (nested/pinnable tooltips as an infinite drill-down, and event choices whose mechanical consequences are shown before commitment) map almost one-to-one onto MERIDIAN's dossier drill-down and its "here is what is required, what may stop it, what it will cost" charter requirement.
  FAIL[Tooltip-first information architecture with no equivalent character sheet] Critical detail about characters lives in hover tooltips rather than in a persistent, scannable, clickable panel. A reviewer reports being unable to click a character element at all — only hover it — and being buried in stacked tooltips. | LESSON: MERIDIAN must NOT make tooltips the primary dossier. Build the ten-tab dossier as a real, persistent, keyboard-navigable, copyable, comparable screen — and use nested drill-down only as the SECOND layer for causal chains. If a fact answers one of the Eight Questions, it belongs on a surface, not on 
  FAIL[No visual hierarchy — everything rendered at the same weight] Text and icons are uniformly small and uniformly weighted, so the interface provides no signal about relative importance. | LESSON: MERIDIAN's owner thesis already says 'semantic not decorative colour' and 'three layers of progressive disclosure' — this is the empirical case for why. Concretely: the dossier's top strip must be an engine-ranked 'what changed and why it matters now' set of no more than about three items, and the r
  FAIL[Event volume producing noise rather than narrative] The sheer number of events and choices, many of them low-stakes, is experienced by some players as fatigue and dilution rather than richness. | LESSON: MERIDIAN must gate interruption on computed consequence magnitude, not on event occurrence. An outcome that cannot answer the Eight Questions is by charter 'flavour text' that must not modify the simulation — the corollary is that it also must not take a modal. Route low-magnitude outcomes to the ev
  FAIL[No per-character event history at launch — the dossier's memory was missing] Characters accumulated Memories, Traits and Relationships from events, but for years there was no place in the game to see the events a given character had been involved in. The Timeline screen logged national-level events (diplomacy, techs, wonders) but not per-character history. | LESSON: This is the most important failure for MERIDIAN. The dossier's Activity and Timeline tabs are not decoration — they are the audit trail that makes the other eight tabs credible. Build them first, not last, and build them as a screen. Also note the shape of the fix: even when the studio agreed, they 
  FAIL[Orders system required roughly 99 iterations before it worked] The scarcity mechanism that is now the game's signature took extraordinary iteration; earlier variants (buyable/sellable orders, stockpiling) failed as core mechanics. | LESSON: MERIDIAN's open critical finding — no cost, cooldown, decay, budget, prerequisite or revert mechanism anywhere — will not be closed by picking a scarcity model on paper. Budget for many iterations, instrument the engine so saturation is measurable, and expect the first two or three cost models to be
  FAIL[Default key mappings churned after release] Update #100 changed default key mappings: 'Q/E now cycles tabs, with arrow keys or A/D cycling cities instead.' Public hotkey aggregators disagree with each other and with older developer statements about which F-key opens which screen. | LESSON: Minor for MERIDIAN but real: do not commit to a keyboard map until the screen inventory is settled, and publish the map from a single source the UI itself reads, so documentation cannot drift from behaviour. Given MERIDIAN's defining defect is documentation claiming properties that do not exist, gen
  FAIL[Worker automation as an admitted design smell] Johnson identifies the industry-standard need for worker automation in 4X games as evidence of a broken core loop rather than a convenience feature. | LESSON: Direct diagnostic for MERIDIAN: if a future MERIDIAN screen makes players want an 'auto-resolve' button, that screen is generating decisions with no real tradeoff — which is the same disease as the macro-saturation critical finding. Treat requests for automation as bug reports against the cost model
  XFER(strong) Pre-commitment consequence disclosure docked to the option itself: every selectable action carries its concrete, mechanically-derived effects in the same visual block as the choice, before the click. -> The PLAN and COMMAND modes must render the engine's priced composition — required authority, blocking conditions, resisting actors, cost, lead time, likely consequences — as a structured block attached to each proposed action, not as prose below it. Old World proves the pattern reads well and does not kill drama. MERIDIAN's inversion: those effects
  XFER(strong) Nested, pinnable drill-down over hyperlinked keywords as the SECOND layer, letting the player expand a causal chain to arbitrary depth with all intermediate steps kept on screen. -> Answering the Eight Questions on demand: every claim in a dossier or outcome report exposes a keyword link into the mechanism that produced it, and the chain stays visible so the player can compare links. Must be implemented as a pure projection — zero randomness, zero mutation (D-1, D-2) — and every terminal node must be either an engine-computed 
  XFER(strong) Role-contextual attribute filtering: show only the attributes whose mechanism is active in the current context; collapse the rest. -> The direct enforcement of 'CAUSAL VALUE, NOT FAKE DEPTH'. Each dossier attribute row renders alongside the named mechanism it currently feeds; attributes with no currently-active mechanism are collapsed by default. This also gives a testable invariant: every visible attribute row must resolve to a named mechanism, or it is a bug.
  XFER(strong) One quantity, three densities: glyph on the entity's representation → itemised contributor breakdown → time-series graph. -> The Relationships tab and any trust/legitimacy/exposure value. Glyph in list views and on the map; itemised breakdown on inspect (which specific evidence or event contributed how much); graph over the run's history. MERIDIAN adds a fourth element Old World has no need for: the confidence label and visibility axis, since the player sees an ASSESSED 
  XFER(strong) Separate transient contextual explanation (hover) from stable reference documentation (a keyed encyclopedia deep-link), and make the reference link available from every element. -> Question 3 of the Eight Questions — 'which rule/mechanism applied' — is reference, not context. Give every rule a stable article and a stable keyboard route from any element that rule touched. Generate the articles from the engine's rule registry so documentation cannot claim mechanisms that do not exist.
  XFER(strong) A scarce, always-visible global action budget that every proposed action visibly draws against. -> Closes MERIDIAN's macro-saturation critical finding at both engine and UI level. The domain-correct analogues are institutional capacity, legal process throughput, budget and political capital — never a copy of 'Orders'. The UI requirement is that the budget lives in the persistent global status bar and that the PLAN-mode forecast shows the draw be
  XFER(moderate) Unlimited undo built on deterministic replay, extending even to narrative/decision commitments. -> Only viable once MERIDIAN is event-sourced and has named RNG substreams. Its real value is as a forcing function: implementing undo proves the determinism boundary (ADR-006) holds, because a run that cannot be replayed exactly cannot be undone exactly. Treat 'can we undo turn N' as an executable test of the project's central claim rather than as a 
  XFER(moderate) Persisted verbosity preference (advanced help text) layered on top of per-element expansion. -> Implements the 'three layers of progressive disclosure' as both a durable preference and a per-element control. Hard constraint MERIDIAN must add: provenance tags, confidence labels, and the fictional-world disclosure are NEVER verbosity-gated — they sit in the always-visible layer regardless of setting.
  NOCOPY: The ancient-Mediterranean visual identity: parchment/bronze/laurel framing, the classical portrait art style, the family crest iconography and the archetype icon set.; The specific vocabulary as product naming: Orders, Legitimacy, Ambitions, Cognomen, Archetypes, Memories, Opinion-as-a-branded-stat. MERIDIAN needs original command-system language for its scarcity, authority and objective concepts.
## Frostpunk (2018) and Frostpunk 2 (2024), 11 bit studios
ONELINE: Frostpunk is the strongest commercial demonstration that an irreversible decision can be given procedural ceremony — a dedicated instrument, a preview of costs, a signing act, an enforced cooldown, and a later reckoning — which is precisely the "commitment sequence" MERIDIAN needs for engine-authoritative irreversible commands; Frostpunk 2 then shows both how to render multi-party institutional resistance as a bargaining interface and how that interface becomes unreadable when the causal chain outruns the feedback.
  FAIL[Moral weight evaporated when the simulation moved from people to populations] FP2 replaced FP1's intimate, individually-legible suffering with faction-scale politics, and multiple reviewers report the ethical charge dropped sharply as a result. | LESSON: THIS IS MERIDIAN'S CENTRAL RISK, and it is the strongest empirical support for the four-fidelity-tier population model. MERIDIAN's stated differentiator is that a crisis 'propagates through real-seeming people, families, companies, institutions' — Frostpunk 2 is the proof that if the interface only 
  FAIL[Overlay tooltips, shifting panels and delayed feedback broke the causal chain] Players could not connect what they did to what happened. The specific failure repeatedly cited is being unable to diagnose why citizens were freezing despite apparently sufficient heat and shelter. | LESSON: A simulation whose whole value proposition is 'you can inspect WHY' cannot deliver causality through transient hover states. MERIDIAN's Eight Questions imply a PERSISTENT, ADDRESSABLE causal record — a 'why did this happen' surface you can open, keep open, and link to — not a tooltip. Also: panel po
  FAIL[Tutorialisation by encyclopedia — non-interactive reference pages instead of guided practice] FP2's onboarding was delivered as encyclopedia-style pages, and reviewers found it inadequate to the interface's complexity. | LESSON: Directly cautionary for MERIDIAN, whose failure mode is documentation that describes properties the software does not have. A doc page asserting that the interface explains causality is not the same as the interface explaining causality. Onboarding for the commitment sequence must be a live, reversi
  FAIL[Hidden effects on unresearched options destroyed forward planning] The effects of locked technologies in FP2's idea tree are not shown, so the player cannot plan a route through the tree on the basis of what it will actually do. | LESSON: MERIDIAN's charter demands the player be told 'what is required, what may stop it, what it will cost' for things they have NOT yet done. Any option the engine can price should display its price before it is available. Where the engine genuinely cannot know, that must render as an explicit Unknown ro
  FAIL[The moral judgement at the end felt arbitrary because the causal trace was missing] FP1's epilogue judges the player, but players could not determine WHICH of their acts produced the judgement, and many rejected it as unearned. | LESSON: THE MOST IMPORTANT NEGATIVE LESSON HERE FOR MERIDIAN. A reckoning screen without a causal trace is exactly the 'flavour text' the charter forbids from influencing the simulation — and it damaged player trust in a well-reviewed game. MERIDIAN's chapter-close view must be assertion-plus-citation: ever
  FAIL[The promise web became an unreadable debt ledger] Accumulated Promises spiral into interlocking obligations that override the player's own plan, and the interface does not make the resulting constraint set easy to hold in mind. | LESSON: MERIDIAN needs an explicit obligations surface, not just obligation creation. Every active commitment should appear in the right-hand context panel with counterparty, deadline, default penalty, and — the part Frostpunk lacks — a conflict indicator when a newly proposed action would breach an existin
  FAIL[Institutional cooldowns read as dead time rather than pressure] The 10-week Council recess after every vote, plus law-signing cooldowns in FP1, create windows where the primary decision instrument is simply unavailable. | LESSON: If MERIDIAN adopts cooldowns to fix macro saturation (which it should), the locked-out window must remain productive: browsing, planning, and PLAN-mode costing should stay fully available during it, exactly as FP2 permits browsing laws during recess. Rationing commitment is defensible; rationing inf
  XFER(strong) THE COMMITMENT SEQUENCE: dedicated instrument → engine-computed consequence preview with signed, colour-valenced deltas → explicit signing act → enforced cooldown surfaced on the instrument → permanen -> This is the spine of MERIDIAN's irreversible-command flow. Concretely: COMMAND mode opens a distinct full-attention surface (pause + de-emphasise the shell, per FP2's blur-and-pause); the engine returns requirements / blockers / resisting actors / cost / lead time / likely consequences as a compact bullet set with provenance tags separating engine-
  XFER(strong) Render an institution as a composition of disagreeing blocs on a single bar, with a first-class UNDECIDED band and the decision threshold drawn in place. -> Institution and state dossiers get a support/opposition bar assembled from meso cohorts and micro key actors, weighted by the institution's actual modelled decision rule (which must be inspectable). The undecided band is rendered width, not whitespace — the direct analogue of MERIDIAN's rule that Unknown and Restricted are present rows. Higher-stak
  XFER(strong) Make negotiability a TYPE with visible affordances rather than a hidden persuadability scalar: if a party cannot be moved by a given verb, that control is absent and its absence is explained. -> For each actor in a proposed action's resistance set, MERIDIAN shows which influence channels are live (bargain / concede / compensate / compel / change a material precondition) and greys the rest with the engine's reason. Prevents the player from probing by failure and satisfies 'causal value, not fake depth' at the interface layer — every afforda
  XFER(strong) Priced authority: never refuse an override outright; make it available with an itemised, relationship-specific invoice, and asymmetric — small cost globally, large with the parties actually overridden -> Directly serves the charter's 'you can attempt this' promise. When a player commands something the normal process would block, the engine offers the override path with its computed cost decomposed by affected actor, plus which future options it removes. Extends naturally to MERIDIAN's 'change legal status' and 'enable/remove future options' primiti
  XFER(strong) Obligations as a capped, persistent, conflict-aware ledger — created in one screen, enforced in another, and checked against every subsequent proposal. -> MERIDIAN's 'create an obligation' primitive gets a right-panel ledger: counterparty, term, deadline, engine-computed default penalty. New proposals are checked against it and any breach is surfaced INSIDE the commitment preview, not discovered later. Adopt FP2's hard cap (a small number of concurrent obligations) so the ledger stays renderable — bu
  XFER(strong) Bounded-scalar-with-named-tiers plus a SPEND path, instead of an unbounded score. -> A direct structural fix for the macro-saturation critical: give key indicators a bounded range, named tiers that change behaviour at boundaries, an enumerated list of what raises and lowers them (visible on hover as a contributing-factors breakdown), and at least one sink where the resource can be spent on a real capability. Plus a designed floor e
  XFER(strong) Status-on-the-gateway: every navigation entry carries the state of the system behind it, so the shell answers 'is there anything for me here' without navigation. -> MERIDIAN's left nav entries display pending approvals, instrument cooldowns, in-flight operations and unread consequence reports as compact indicators — the same idea as FP2's radial carrying research, recess and expedition progress. Cheap to build, and it is what keeps a stable shell from requiring constant exploratory clicking.
  XFER(moderate) Ambient state rendering: map a global severity variable onto the operational substrate the player is already looking at, giving magnitude and locality simultaneously. -> Crisis severity tints the operational map's base layer regionally rather than only filling a status widget. HARD CONSTRAINT, learned from FP2's failure mode: ambient styling is a REDUNDANT channel only — never the sole carrier of a critical fact, never allowed to reduce legibility (reject the 'UI covered in black liquid' escalation outright), and a
  NOCOPY: The physical leather-bound book-of-laws metaphor with circular wax-seal law nodes and a page-turning signature animation — this is Frostpunk's single most recognisable UI signature.; The frozen-orb / filling-liquid status globe, and the escalation in which black or red liquid creeps across the interface chrome.; Recolouring city roads red as a global unrest readout — a specific, identifiable Frost
## Evidence, deduction and document-verification interfaces (Papers Please; Return of the Obra Dinn; The Case of / Rise of the Golden Idol; Orwell; Her Story / Immortality; The Roottrees are Dead)
ONELINE: This cluster is the only body of shipped, playtested UI work that has solved MERIDIAN's hardest interface problem — letting a person assert an original conclusion from messy evidence, and having a deterministic engine (not a model, not a menu) adjudicate it — and it supplies concrete, transferable mechanisms for confidence rendering, absence-as-content, provenance-by-construction and commit gating.
  FAIL[Obra Dinn's confirmation guard degrades as the pool shrinks] Once a player has two entries they are confident about, they can brute-force the third by cycling names and fates until the batch validates — and the endgame relaxation to sets of two makes this cheaper still. | LESSON: For MERIDIAN: a confirmation surface that leaks one bit per attempt leaks the whole model given enough attempts. If the four-view epistemic model is load-bearing, the UI needs either no per-attempt oracle at all, or a cost/rate limit on probing. Assume adversarial players; the honest ones will find 
  FAIL[Obra Dinn ships no note-taking tool] The logbook records only committed answers. There is nowhere in the game to record an in-progress inference, a hunch, or an eliminated candidate, so a large share of players work with an external spreadsheet or paper notes beside the screen. | LESSON: MERIDIAN must ship a first-class scratchpad that is explicitly NOT simulation state — player annotations, hypotheses, watchlists, links between entities the engine has not confirmed. It must be versioned as external input if it can influence an action (per the conversational-interface rule), and it 
  FAIL[Papers, Please: playtesters did not discover the rulebook] Testers reported that 'Nothing made sense before realizing that there is a rulebook' — the single object that makes the entire game legible was missable. | LESSON: MERIDIAN's equivalent of the rulebook is the mechanism/rule explanation behind any outcome — the 'which rule applied' element of the Eight Questions. If it lives behind a hover, a footnote, or an unlabelled panel, the simulation will read as arbitrary numbers to most users and the entire causal-hone
  FAIL[Papers, Please: the core verb was invisible] Testers reported 'I didn't know that you could actually highlight rules in the rulebook to get a denial' — i.e. the central assertion mechanic was undiscovered. | LESSON: If MERIDIAN builds a contradiction-assertion interaction, it will be undiscoverable for exactly the same reason. Budget for an explicit, scripted first-use demonstration and a persistent affordance cue, not a tutorial tooltip that fires once.
  FAIL[Papers, Please: failure feedback initially taught nothing] Early builds denied or penalised the player without saying what the mistake was; testers were confused about what they had missed. Pope changed it so the warning slip lists the mistake, and shipped citations arrive by in-game fax after the applicant leaves. | LESSON: MERIDIAN's bottom event stream is the citation fax. Every consequence line must name the rule/mechanism and link back to the originating decision. A stream of 'Indicator X fell 4%' lines with no attribution is the exact failure Pope fixed.
  FAIL[Papers, Please mobile: highlight state desynchronised from document position] On the phone port, highlighted elements remained partially on screen after their documents had scrolled away, and the carousel and page-turn buttons stayed active during inspect mode. | LESSON: Concrete implementation warning for MERIDIAN's comparison/assertion UI: selection must be owned by the data object and travel with it, and entering an assertion mode must disable navigation that can invalidate the selection. This is the class of bug that makes an evidence interface feel untrustworth
  FAIL[Orwell: prescriptive task lists were cut for feeling linear] Early builds told the player what to investigate. Testing showed it felt restrictive and turned an investigation into a checklist. | LESSON: This is MERIDIAN's charter failure mode observed in the wild and reversed by a shipping team. Any 'recommended actions' panel is the same mistake. Mark what is actionable; never rank what to do.
  XFER(strong) Composable assertion grammar: the player builds a claim from two or more engine-addressable referents; the engine adjudicates and cites the mechanism (Papers Please inspect mode). -> A contradiction-assert interaction on the dossier and Intelligence Assessment tab: select two rows/claims/clauses, assert conflict, engine returns CONFIRMED CONTRADICTION with both provenance chains and the named rule, or NO CORRELATION. Unbounded player expression over a bounded validatable grammar — the determinism boundary made visible in a sing
  XFER(strong) Assembled dossier: evidence is dragged from a source into a profile field, carrying its provenance, with a commit preview and a warning when the evidence set is known to be incomplete (Orwell). -> Build view #4 (player intelligence profile) as constructed, not delivered. Every row shows its source and its engine-computed confidence label; promoting an observation into an assessment is an explicit, previewed, warned commit. Discharges the PROVENANCE RULE structurally rather than by adding a badge.
  XFER(strong) Absence as a typed, addressable slot rather than a hidden or greyed field (Roottrees nodes, Obra Dinn blank fates, Golden Idol blanks). -> Render Unknown and Restricted as present rows that state the SHAPE of the missing thing and the reason it is missing (no evidence vs no access). Satisfies the stated requirement directly and converts absence into a specific next question.
  XFER(strong) Non-colour epistemic encoding: unconfirmed content is typographically distinct from confirmed content and converts on validation (Obra Dinn handwritten to typeset). -> A redundant weight/style channel for the three-way engine-fact / model-advisory / player-hypothesis split, with the eight confidence labels carried in text. Accessible, survives at high row density, and makes the provenance distinction pre-attentive.
  XFER(moderate) Distance-not-diagnosis feedback with lock-in on settled parts (Golden Idol's 'two or fewer wrong'; Obra Dinn's rule of three). -> Pre-commit plan validation returns a count of failing preconditions before naming them; settled elements of a composition lock and leave the working set. Post-resolution, the Eight Questions apply in full — the gradient is for planning, not for outcomes.
  XFER(strong) Coverage meters that are explicitly not correctness meters (Her Story's red/green checker, Obra Dinn's back-page diagram, Golden Idol's used-hotspot recolouring). -> Show 'you have inspected 12 of 41 implicated entities' and per-section read state, and never show anything that implies the player's interpretation is right. Read state must live in the observer session, not simulation state, to preserve dossier properties D-1 and D-2.
  XFER(strong) Entity-as-hyperlink associative traversal of a large corpus (Immortality match cut, Roottrees search engine, Her Story user tags). -> Every entity mention anywhere in the shell — event stream, advisory text, relationship rows — opens that entity's dossier and its occurrence list. Add a breadcrumb trail; associative traversal without history is where players get lost. Cheapest available expression of the 'inspect why' differentiator.
  XFER(moderate) Result caps and limited pools as design tools, not limitations (Her Story's capped result list — number unverified; Golden Idol's per-scenario phrase pool). -> Cap EXPLORE-mode query results and force refinement rather than returning a haystack. Bound the candidate sets the player reasons over so that reasoning beats enumeration.
  NOCOPY: Obra Dinn's 1-bit dithered monochrome rendering, sepia parchment logbook and nautical-ledger typography — the most imitated trade dress in this entire category.; The word 'logbook' (and 'Memento Mortem'-style device naming) as a UI object name; MERIDIAN needs original command-system vocabulary.; Papers, Please's grimy Eastern-bloc booth, the passport-stamp motif, the red APPROVED/DENIED stamp, and
## RimWorld (Ludeon Studios / Tynan Sylvester)
ONELINE: RimWorld is the closest shipped analogue to MERIDIAN's hardest interface problem — it renders every emotional state, every derived statistic and every attention-worthy event as an itemised, signed, drill-downable causal ledger that points back at the exact entity that caused it, which is precisely what MERIDIAN's Eight Questions demand.
  FAIL[Per-pawn tab silo with no colony-wide comparative view] All meaningful character information is locked behind a per-individual inspect pane with a long tab strip — the shipped tab set includes Character, Health, Needs, Social, Gear, Log, Records, Training, Prisoner, Guest and more. To compare a single attribute across twelve colonists the player must sel | LESSON: Drill-down and comparison are two different jobs. Shipping only the drill-down guarantees users will rebuild the comparison view themselves.
  FAIL[Basic overview functionality delegated to mods] Widely-used community mods exist purely to surface information the base game already computes but does not display conveniently — most prominently RimHUD, which replaces the vanilla inspect pane with a dense at-a-glance readout of needs, health, skills and activity. | LESSON: When a community independently builds and translates a replacement for your primary information panel, the panel's density ceiling is set too low. MERIDIAN should ship the dense expert view as a first-class mode rather than assume the default serves everyone.
  FAIL[Alert stack fires too eagerly and cannot be tuned] The base alert system has no per-alert delay, no per-alert severity override, and no per-alert mute. Transient conditions that resolve themselves within seconds still generate a stack entry, and low-value alerts (notably 'Colonists idle') sit alongside genuinely urgent ones. | LESSON: Any always-on alert system needs hysteresis at the engine level and user-controllable severity at the interface level. MERIDIAN, whose crisis model will produce far more simultaneous conditions than a colony sim, must build debounce and per-class muting into the alert contract from the start rather 
  FAIL[The opinion breakdown lives in a transient hover tooltip] The itemised derivation of one character's opinion of another surfaces as a hover tooltip. It cannot be pinned, scrolled comfortably when long, compared side by side, or kept open while the player looks at anything else. | LESSON: Explanation surfaces must be as persistent as the decisions they inform. MERIDIAN's relationship derivations must be pinnable panels, not hover states — especially since MERIDIAN expects the player to reason across several entities at once.
  FAIL[Storyteller pacing is qualitative-only; the actual arithmetic is invisible] The player chooses a pacing regime from prose descriptions alone. The quantities that actually drive event selection and difficulty are not exposed anywhere in the interface, so players routinely resort to external wikis and mods to understand why raids scale the way they do. | LESSON: This is where RimWorld's design goals and MERIDIAN's charter are genuinely opposed. RimWorld hides the model to protect the illusion; MERIDIAN's Eight Questions forbid that. MERIDIAN must copy the plain-language pacing contract and then ADD the inspectable parameters — otherwise it inherits the opac
  FAIL[The thought ledger degrades under length] A colonist in a bad state can accumulate a thought list far longer than the small pane allocated to it, forcing scrolling inside an already-cramped inspect pane. The largest-magnitude contributors are not necessarily at the top of the visible region, so the player scrolls to find the thing worth fix | LESSON: An exhaustive causal ledger must be sortable by contribution magnitude, or its exhaustiveness becomes a burden. MERIDIAN should default its ledgers to descending absolute impact with a 'top N + show all' fold, so the actionable term is always above the fold.
  FAIL[The work grid does not scale past a modest population] With a large colony and the full set of work types, the priority matrix exceeds comfortable screen width and height, and column headers become hard to associate with distant rows. | LESSON: Matrix views need grouping and filtering designed in from the beginning. MERIDIAN's institution/mechanism matrices will be larger than RimWorld's on day one, so aggregation by category with drill-down must be part of the initial specification, not an optimisation.
  XFER(strong) Exhaustive signed causal ledger: any composite state renders as a complete list of named contributors with signed magnitudes, no hidden residual, split into decaying memories and standing situational  -> The core rendering for the Motivations, Beliefs-and-knowledge and Public-perception dossier tabs, and for cohort disposition. The no-hidden-residual property is what makes it an audit trail rather than flavour, and it operationalises 'causal value, not fake depth': any attribute that cannot appear as a ledger row has no mechanism and should be dele
  XFER(strong) Universal derivation drill-down with a fixed section order: base value → typed modifier groups (offsets vs multipliers) → clamping → post-process curve → final value, applied identically to every numb -> The answer to Eight Questions #3 and #5 for every priced consequence primitive — cost, lead time, success probability, resistance. One learnable format for all numbers. It is also the most persuasive visible demonstration of the ADR-006 determinism boundary, because the derivation is manifestly engine arithmetic that no model produced.
  XFER(strong) Two-field alert (terse label + engine-computed explanatory body) bound to a live entity reference, so clicking navigates to the culprit and the alert self-clears when the condition clears. -> The bottom event stream and global status bar. Every entry carries a mechanism/entity reference opening the relevant dossier or rule view. Alerts derived from live state predicates cannot go stale — which also protects dossier property D-1, since navigation is a projection, not a mutation.
  XFER(strong) Group N instances of one condition into a single row with a pluralised label and an enumerated culprit list in the expansion, so stack length tracks problem TYPES not instance counts. -> Essential for MERIDIAN's cohort-scale propagation, where one macro shock produces thousands of same-type consequences. Group by mechanism type with a count; defer actor enumeration to expansion. Without this the event stream floods precisely during the crises the product exists to portray.
  XFER(strong) One authoritative fact, multiple presentation strings selected by observer context (RimWorld's label vs labelSocial). -> The shipped precedent for MERIDIAN's four-views-per-entity requirement. Store one fact record in authoritative state; attach renderings for self-understanding, public profile and player intelligence profile; let the observer select. Only view #1 is authoritative. A small schema cost for a large correctness gain.
  XFER(strong) Render impossibility as a present, visibly disabled element with the blocking mechanism named — never omit the option (RimWorld's struck-out 'Unchangeable' work cells). -> Generalises MERIDIAN's existing 'Unknown and Restricted must be present rows' rule from knowledge to capability and to action. When the player proposes something an actor cannot legally or institutionally do, show it present, disabled, and attributed to the blocking mechanism. This is what keeps the charter promise ('here is what may stop it') inta
  XFER(strong) Staged, engine-selected plain-language wording keyed to magnitude bands, so readable prose is produced deterministically without a model. -> Implements 'plain language first, technical detail second' without crossing the determinism boundary. Each primitive defines stage wording per magnitude band; the engine picks the stage. Any LLM narration then sits in a clearly separate, provenance-tagged layer beneath text the engine authored.
  XFER(strong) A single explicit, persistent, reversible mode toggle that re-renders an entire view at a different fidelity (RimWorld's Manual Priorities checkbox). -> Gives MERIDIAN's 'three layers of progressive disclosure' a concrete control. One visible toggle per major view beats scattered per-panel expanders, is discoverable, and lets novice and expert modes coexist without either paying for the other.
  NOCOPY: The storyteller characters themselves — the names Cassandra Classic, Phoebe Chillax and Randy Random, their personas, and the conceit of an authorial AI character with an attitude. Take the plain-language pacing contract; invent MERIDIAN's own framing with no anthropomorphised narrator.; RimWorld's specific vocabulary as user-facing labels: 'mood', 'thought', 'break risk', 'mental break', 'colonis
## Hearts of Iron IV (Paradox Development Studio) + operational wargame cluster
ONELINE: This cluster is the only body of shipped software that has actually solved MERIDIAN's hardest operational problem — showing a player that an action is possible-but-constrained (readiness, logistics, detection, ROE, chain of command, weather, time) by attaching the constraints to the order control itself and by rendering uncertainty as explicit numeric error bands rather than as absence.
  FAIL[The battle planner delegates execution but not judgement — and punishes you for taking it back] Drawing a plan and pressing Execute hands divisions to an AI that advances along the drawn arrows. When the player disagrees with a move and issues a manual order, that division 'breaks from battle plan AI', and the accumulated planning bonus then decays three times faster (3%/day vs 1%/day). The pl | LESSON: MERIDIAN must make intervention *composable* with delegation: a correction should amend the plan object rather than terminate it. If MERIDIAN prices micromanagement (and it should, to make chain-of-command real), the price must attach to bypassing an echelon, not to disagreeing with an AI's pathfind
  FAIL[Map-mode proliferation outruns memory] HOI4 ships roughly fifteen mutually exclusive map modes, each with its own colour language (supply's five-band blue→red ramp, resistance's yellow→red, compliance's blues, air's red/green/yellow, naval trade routes' white/blue/cyan/green/red). Several use the same colours for unrelated meanings — red | LESSON: MERIDIAN's DESIGN-TOKENS and VISUAL-CONSTITUTION documents must define ONE semantic colour system used identically across every lens (e.g. one hue always means 'engine-computed stress', one always means 'contested/disputed'), with a persistent on-screen legend. Fewer lenses, each stable, beats many 
  FAIL[The intelligence ledger is buried behind diplomacy] HOI4's richest uncertainty model — the graded ± bands, the per-category thresholds, the decay — lives in an 'Intel Ledger' the wiki says is 'accessible through diplomacy', i.e. two navigations away from the map where the decisions are actually made. The map itself shows enemy counters without their  | LESSON: MERIDIAN's confidence labelling must be attached to the datum wherever the datum appears, not concentrated in one screen. If a division count appears on the map, its band and its age appear with it. Otherwise the honest model exists and nobody reads it.
  FAIL[Complexity acknowledged, onboarding not solved] Reviewers praised the surface and condemned the ramp. GameSpot identified the tutorial as 'the only weak point'. Kotaku found the game 'overwhelming in both its depth and, more importantly, its complexity'. IGN called it 'an incredibly complex World War II simulation that will require potentially hu | LESSON: MERIDIAN's differentiator is inspectable causality, so the tutorial problem IS the product problem. The mitigation is not a tutorial mode but making every outcome self-explaining at the point of occurrence — the Eight Questions as a hover, on every event, from the first minute.
  FAIL[Design-space editors degenerate into spreadsheet meta-solving] HOI4's division designer presents a 25-battalion grid with live derived stats — an editor that looks like creative expression but which, because the combat model has sharp breakpoints, resolves to a small set of externally-computed optimal templates that the community shares and everyone copies. | LESSON: MERIDIAN's action composer must avoid a single scalar objective. Because MERIDIAN prices actions across multiple incommensurable axes (political capital, legal exposure, institutional resistance, public perception, foreign reaction) and resolves stochastically, no dominant composition should exist —
  FAIL[Live-service divergence: overall approval can hide current dissatisfaction] NEBULOUS: Fleet Command shows 'Very Positive' overall (81% of 3,475 reviews) but 'Mixed' recent reviews (52% positive over 190 reviews in the last 30 days) — a large gap between accumulated and current sentiment. | LESSON: Methodological, for MERIDIAN's own research doc: aggregate review scores are not evidence about an interface. Only specific, dated, attributable complaints are. Do not import 'Very Positive' as a design endorsement.
  FAIL[Store-page descriptions are near-useless as design evidence] Fetching the official Steam/Matrix pages for WARNO, Sea Power, NEBULOUS and Command Ops 2 yielded marketing feature bullets with almost no interface detail — WARNO's page 'doesn't explicitly detail mechanics like fog of war or morale systems', and the Command Ops 2 product URL resolved to a catalogu | LESSON: For the remaining operational-wargame research, MERIDIAN's document should cite game manuals, dev diaries and detailed reviews. Specifically still MISSING and worth chasing: Combat Mission's command-and-control / information-sharing model (the CMx2 manual), Graviteam Tactics' command-delay model (ev
  XFER(strong) Every uncertain figure renders as value + error band + age + collection channel, and the band narrows as a named investment rises. The estimate is always shown, never suppressed. -> Implement MERIDIAN's eight confidence labels this way in the dossier and on the map: the engine computes the label from evidence count/recency/corroboration, and the row shows the label, the age since last confirmation, and the named channel (which observer, which event) that produced it. 'Unknown' and 'Restricted' are rows with that same structure
  XFER(strong) Two-stage commit: a plan is drawn as an inspectable, editable, costed object; nothing mutates state until an explicit, unambiguously-placed Execute control is pressed; the plan object persists afterwa -> This is MERIDIAN's determinism boundary made visible. The LLM's proposed composition of consequence primitives IS the drawn plan — visible, priced by the engine, inert. The Execute control is the only path to state mutation, sits in one fixed location, and carries the engine's full precondition summary. The persisted plan object then supports the a
  XFER(strong) Preconditions are enumerated on the order control as named gates with current value vs required value, so 'you cannot do this' always decomposes into 'this specific gate is short by this much'. -> The direct implementation of the charter sentence about what is required and what may stop it. Critical constraint: the gates must be DERIVED by the engine from the primitive composition and current world state, not authored per action type — otherwise the requirements panel becomes the menu of pre-authored options the charter forbids. Each gate sh
  XFER(strong) Commit → resolve a bounded, variable-length interval → scrub the recorded interval freely. The replay is a pure projection of the same resolved data, not a separately-authored report. -> Satisfies dossier properties D-1 (projection never mutation) and D-2 (consumes zero randomness) by construction, and gives MERIDIAN its after-action review for free. Resolve until the next decision-worthy event rather than on a fixed tick, following Flashpoint's variable-length WEGO. Attach the Eight Questions to each recorded event in the scrubbed
  XFER(strong) One geometry, several named lenses with a single shared semantic colour system, including a distinct band for 'degrading but not yet harmful'. -> MERIDIAN's crisis propagation across people, firms, institutions, markets and media is a lens problem. Ship a small fixed set (legal authority / budget stress / institutional compliance / public sentiment / logistics / information reach), one colour language across all of them defined in DESIGN-TOKENS, a persistent legend, and — copying HOI4's purp
  XFER(strong) Standing behaviour (ROE, doctrine, posture, pre-granted approvals) is authored on a surface separate from one-off orders, and the engine cites the standing rule when it shapes an outcome. -> Give MERIDIAN a persistent posture surface: legal authorities currently in force, escalation thresholds, disclosure policy, delegated approval scope. The engine resolves most routine events against this without a player turn, and when an outcome is blocked or shaped by a posture setting the event card names that setting and links to it — a clean, h
  XFER(strong) Composition on the left, engine-computed consequences on the right, recomputed live, with cost shown as a min/max range rather than a false point estimate. -> The action composer screen. Left: the primitive composition. Right: engine-computed political capital cost, elapsed time, approvals required, expected resistance by actor, and consequence forecast — as ranges. This also makes the LLM's role visible and bounded: it may populate the left panel; only the engine writes the right panel, and the right pa
  XFER(moderate) Delegation is priced. Operating below your delegated echelon carries a visible, decaying effectiveness cost with a named cause. -> Makes MERIDIAN's chain of command a felt constraint rather than an org chart, without simulating staff work. Render as a decaying indicator with the causal text attached ('direct intervention at ministry level — institutional coordination degrading 3%/day'). Cheap to implement, answers Question 8 (what future options changed), and creates real stra
  NOCOPY: HOI4's visual identity wholesale: the tan/parchment WW2-era chrome, art-deco framing, ornamental corner brackets, portrait frames and the specific top-bar icon set. MERIDIAN's brief is near-future command-system styling; this is period pastiche.; Paradox's naming vocabulary. 'National focus', 'political power', 'war support', 'world tension', 'battle plan', 'planning bonus', 'command power', 'nati
## Disco Elysium (primary) — with Pentiment, Citizen Sleeper and Suzerain as comparative cases
ONELINE: Disco Elysium is the only shipped product that solves MERIDIAN's two hardest advisory problems at once — it makes every adviser an openly interested party with a standing agenda, and it itemises the full provenance of a probability before the player commits — and its failure modes (save-scumming, off-screen modifier sourcing, opaque Thought Cabinet payoffs) map directly onto risks in MERIDIAN's Eight Questions and provenance rules.
  FAIL[Full odds disclosure plus free saves collapses into save-scumming] Because the exact success percentage is shown before commitment and the game permits manual saves anywhere, a substantial part of the audience reloads until checks pass, which nullifies the red/white distinction and the 2d6 uncertainty entirely. | LESSON: For MERIDIAN: showing odds is safe ONLY if the consequence of a bad draw is durable. MERIDIAN's event-sourced history (once built) should make outcomes part of the run's identity, and any 'rewind' affordance must be an explicit, labelled counterfactual branch rather than a silent undo. Alternatively
  FAIL[Modifier sourcing lives off-screen, so optimising means leaving the conversation] Over fifty clothing items grant skill bonuses with offsetting penalties, and modifiers are inspected in the inventory rather than in the check panel. The result: 'Every Skill check requires exiting a conversation, swapping on the best items, and re-initiating the conversation.' Players eventually st | LESSON: MERIDIAN's commit screen must let the player act on a modifier row in place — 'this row is negative because you lack X; here is what obtaining X costs' — otherwise the itemised breakdown becomes read-only trivia. An explanation the player cannot act on from where they are standing is documentation, 
  FAIL[Commitment with a hidden payoff reads as obscurity, not mystery] Thought titles are oblique and the mechanical effect is concealed until internalisation completes, so a large part of the audience treats the Thought Cabinet as flavour. One analysis criticises it directly 'for obscure naming and negligible mechanical impact.' | LESSON: MERIDIAN can have uncertain outcomes but must not have unnamed mechanisms. If adopting a doctrine or committing to a policy has an unknown magnitude, the CHANNEL through which it will act must still be stated ('this will change procurement lead times'). Unknown magnitude is legitimate uncertainty; u
  FAIL[Adviser volume scales with player investment, producing choice overload] The better the character gets, the more passive checks succeed, and the more skill voices interject per conversation. The game's late-game difficulty is substantially self-inflicted internal noise. | LESSON: MERIDIAN's advisory interjections need engine-computed salience — relevance to the active decision, and the adviser's stake in it — plus a mute/collapse control per adviser. The right panel should default to the two or three most interested parties with the rest collapsed behind a count, not stream 
  FAIL[Consequence-free dialogue trains exhaustive clicking] Because nearly all dialogue options carry no cost and many grant XP, the dominant strategy is to click every option in every tree. The few options that genuinely end a conversation then feel arbitrarily punitive against that trained expectation. | LESSON: MERIDIAN's EXPLORE / PLAN / COMMAND mode split is exactly the right structural answer — but only if the mode is unmistakable at all times and if COMMAND-mode options are visually and interactionally distinct from EXPLORE-mode ones. If a player can drift from asking into acting without noticing, the 
  FAIL[A UI overhaul that optimised for touch destroyed information density (Suzerain 2.0)] The 2.0 redesign moved to a mobile-oriented layout: full-screen dialogue that 'blocks the game board entirely,' vertical scrolling replacing a horizontal bar, oversized fonts with heavy spacing, 'rubbery' overscroll, and a close button positioned so it triggers unintended map scrolling. Players desc | LESSON: Two lessons for MERIDIAN's stable command shell: (1) the centre operational view must never be fully occluded by the conversational surface — dialogue is a panel, not a takeover; (2) notification volume was the single biggest accessibility complaint and the single most effective fix, so build the su
  FAIL[An accusation/commitment mechanic whose stakes were not legible (Pentiment)] The player must ultimately accuse someone 'based on either who they think committed the crime or who most deserves punishment,' and can never gather enough evidence to be certain — Sawyer: 'you never have enough time to investigate every lead. It's just impossible … you often fail checks when you're | LESSON: This is MERIDIAN's sharpest risk. The system deliberately gives the player an intelligence product rather than omniscience — so it MUST make the boundary of its own knowledge explicit. That is exactly why 'Unknown' and 'Restricted' have to be rendered as present rows: they are the mechanism that sto
  XFER(strong) Render a probability as a sum of named, signed, individually-sourced contributions, with the conclusion (the percentage) and the argument (the rows) on the same surface at the moment of commitment. -> The action-commit panel for a composed set of consequence primitives: each row states a rule, an actor or a resource constraint, its signed effect on cost / time / likelihood, and a provenance tag (engine-computed vs LLM-proposed). Directly discharges Eight Questions #3, #5 and #6 before the act rather than after.
  XFER(strong) Bound the disclosed probability away from certainty by a structural rule, so full information and real jeopardy coexist. -> An engine invariant that no displayed outcome probability may render as 0% or 100%, backed by named RNG substreams so the draw is reproducible in replay. This is also a partial answer to the macro-saturation critical finding: unbounded certainty is what lets indicators ratchet.
  XFER(strong) Mark reversibility at the point of choice, and let the small irreversible set carry the tension budget for the large reversible set. -> An engine-derived reversibility flag on every proposed action, computed from its primitive composition (legal-status change, public exposure, asset seizure ⇒ presumptively irreversible), rendered on the commit control alongside the named future options it closes.
  XFER(strong) Advisers are persistent named entities with agendas that are the SAME attributes driving their behaviour elsewhere in the simulation — bias is demonstrated by conduct, not declared by a label. -> Advisory voices are reads on real world-model entities (institutions, factions, individuals) with their own motivations, resources and relationships. An adviser's recommendation must be derivable from its entity record, and the dossier for that entity must be reachable in one click from its advice.
  XFER(strong) Advisers must be observably wrong and observably self-serving over time, or the player will treat them as an oracle regardless of any disclaimer. -> Track and expose per-adviser history: what it recommended, what it predicted, what actually happened. A visible track record turns the provenance tag from an ignored badge into a live judgement. MERIDIAN's own honesty doctrine argues for this — a system that claims its advisers are interested but never shows one being wrong is making an unverifiabl
  XFER(strong) Rendering encodes the OBSERVER's epistemic state, and visibly re-renders when that state changes. -> Dossier fields animate their transition when a confidence label changes (Assessed → Confirmed, Reported → Disputed), because the revision is itself intelligence. Preserves D-1 (projection, never mutation) and D-2 (zero randomness): nothing in world state changed, only the projection of it.
  XFER(strong) Absence is content: unavailable options and unknown facts are present rows naming their missing prerequisite, not omissions. -> Already MERIDIAN doctrine for Unknown/Restricted dossier rows; extend it to PLAN mode so blocked actions render with the specific authority, budget or relationship they lack — and what obtaining it would cost. Also the defence against Pentiment's 'unclear choice mechanic' failure.
  XFER(strong) Pending consequences are persistent inspectable objects with elapsed/remaining state, not messages that scroll past. -> Procurement lead times, approval processes, legal challenges and delayed operations become durable tracked items in the bottom event stream, each openable to its causal chain. This is also the missing cost/cooldown/prerequisite layer flagged as a critical finding.
  NOCOPY: The white/red colour language for check classes, and the term 'check' itself as a UI label — this is Disco Elysium's most recognisable signature. MERIDIAN needs its own reversibility vocabulary.; The name 'Thought Cabinet' and the cabinet-of-drawers metaphor for belief commitment.; The 24 named skills, the four attribute names (Intellect / Psyche / Physique / Motorics), and the practice of naming 
## Cities: Skylines 1 & 2 (primary subject), with Tropico 6 and Democracy 4 as comparators
ONELINE: Cities: Skylines is the most mature answer in games to MERIDIAN's hardest rendering problem — one shared spatial substrate re-interpreted through dozens of switchable data lenses — and CS2's publicly admitted central failure (a simulation that produces numbers whose causes the UI cannot explain) is exactly the failure MERIDIAN's Eight Questions exist to prevent.
  FAIL[Info-view sprawl with no cross-view correlation] CS1 ended its life with 37 info views and CS2 shipped with 33, all mutually exclusive. Diagnosing a real problem almost always requires correlating two or three of them — land value against noise pollution against workplace availability — and the interface offers no supported way to do that. The pla | LESSON: MERIDIAN needs an explicit, designed answer to two-lens correlation before the layer count grows. Candidates: a designated secondary lens rendered in a reserved channel; a small-multiples grid of the same map under three lenses; or a split view. Decide this at the SCREEN-SPECIFICATIONS stage, not af
  FAIL[Showing the score while hiding the cause (Cities: Skylines 1 happiness)] CS1's Citizen Happiness info view colours buildings on a red-to-green gradient and stops there. The wiki documents no per-factor breakdown, no tooltip enumerating penalties, and no ranked list of what is hurting a building — only 'Happiness can be viewed from the happiness overlay represented by the | LESSON: This is exactly MERIDIAN's charter failure mode in miniature: a number that modifies the simulation but cannot answer question 2 or question 3. MERIDIAN's rule should be stronger than CS1's practice and stronger than CS2's: if a value has no renderable causal decomposition, it does not get a colour 
  FAIL['Why is nobody moving in?' — the aggregate with no drill-down] CS2's demand bars are an output of a large multi-factor computation, presented as a level with no exposed reasons. When residential demand sits at zero, the interface does not tell the player which of the documented inputs — job availability, housing occupancy, education opportunity, city attractive | LESSON: The single most important negative lesson for MERIDIAN. Every aggregate in the status bar must expose a ranked contributor list naming the mechanism behind each contributor. Making an unexplained number more visible makes the problem worse, not better.
  FAIL[Chirper — narrative flavour decoupled from authoritative state] Chirper is a social-media-style feed of short messages from citizens. Much of it is generic or ambient, and its relationship to the underlying simulation is loose enough that players cannot reliably act on it. In CS1 it became one of the most commonly disabled or modded-out elements of the interface | LESSON: This is MERIDIAN's provenance rule stated as a cautionary tale. A stream of model-authored text sitting in the interface next to engine-computed fact will either be trusted when it should not be, or distrusted and ignored wholesale. The bottom event stream must carry visible provenance tags per item
  FAIL[Developer-acknowledged communication failure at the interface layer] Roughly two years after launch, Colossal Order publicly stated in a dev diary that 'the UI can sometimes be a bit confusing when it comes to communicating things' and shipped a makeover: streamlined onboarding, icons 'more expressive and easier to understand the context from them', a restyled toolba | LESSON: Restyling does not fix explicability. If MERIDIAN cannot answer 'why did this happen' structurally, no amount of icon work or a manual will substitute. Build the causal decomposition into the outcome renderer, not into an encyclopaedia the player has to leave the situation to consult.
  FAIL[Players report a beta-quality interface at launch] CS2's launch reception treated the whole product, interface included, as unfinished. IGN's Leana Hafer wrote that it 'feels like playing a beta, or a very early Early Access game'. PCGamesN's Ed Smith wrote that 'Something personal is lost in its larger scale while performance problems spoil the bea | LESSON: Directly relevant to MERIDIAN's two open criticals. A more elaborate world model with no cost, cooldown, decay or revert mechanism will make outputs LESS explicable, not more. Explanatory surface must be treated as a scaling constraint on simulation depth.
  FAIL[The economy needed a documented rework because its behaviour was not believable to players] CS2 shipped and then required 'Economy 2.0', a multi-diary rework of the economic simulation, roughly eight months after launch. | LESSON: Explicability is a trust mechanism, not a convenience. MERIDIAN's determinism boundary (ADR-006) is only credible to a user if the interface demonstrates it on every outcome. An engine that is genuinely deterministic but renders like a black box will be accused of being an LLM guessing.
  XFER(strong) A named, mutually-exclusive lens register over one shared substrate, with an exclusive active lens named in persistent chrome. -> Define MERIDIAN's map/graph layers as a register of read-only projections (legal authority, budget exposure, institutional resistance, information asymmetry, public salience, retaliation risk). Exactly one is active; its name sits in the global status bar; each satisfies dossier properties D-1 and D-2 — a projection, never a mutation, consuming zer
  XFER(strong) Separate visual channels within one lens for institutional reach, entity state, the institution itself, and directional flow. -> Map MERIDIAN's three tiers onto fixed channels: macro field on the terrain/background, meso institutional reach on the relationship network, micro named actors as objects, directional asymmetric relationships as arrow glyphs. This makes 'authority exists but produces no effect' visible in a single view rather than requiring inference across two.
  XFER(strong) Two-tier progressive disclosure of causation: aggregate always visible, ranked contributor breakdown one deliberate gesture away. -> Every aggregate in the status bar and every score in a dossier gets a click-to-expand (NOT hover — hover is inaccessible, unlinkable and cannot carry provenance) causal breakdown listing contributors ranked by magnitude, each naming the mechanism and carrying its own confidence label and provenance tag. This is the concrete rendering of Eight-Quest
  XFER(strong) Contextual auto-activation of the lens that governs the action currently being composed. -> In PLAN and COMMAND modes, composing an action auto-switches the operational view to the lens matching its dominant consequence primitive — 'trigger an approval process' surfaces the veto-points lens; 'create public exposure' surfaces the salience lens. The switch is a projection and must never consume randomness or alter state.
  XFER(strong) Discrete named condition badges as a channel parallel to, and never merged into, continuous scores. -> Render categorical entity states (under investigation, under sanction, detained, in exile, subject to legal challenge, under obligation) as a badge row under the dossier header, each linking to the event and mechanism that set it. This is also the honest rendering surface for 'Restricted' — a present row stating the observer may not see this, satis
  XFER(strong) Composite score decomposed into a small number of separately-named orthogonal axes rather than one scalar or a 40-attribute grid. -> Validates the two-axis VISIBILITY / CONFIDENCE design: never collapse them into a single bar. Also a template for the dossier Overview tab — one headline reading, two or three named axes, then defer everything else to the nine remaining tabs.
  XFER(strong) Data-type-matched colour encoding: three distinct ramp families, with the valence ramp reserved for variables that genuinely have good and bad. -> Write into DESIGN-TOKENS: categorical hue set, sequential magnitude ramp, diverging valence ramp, plus a dedicated non-reusable scale for the eight confidence labels. Prohibit red-green on neutral quantities. Vector-valued and asymmetric relationships get glyphs, never colour alone. Every ramp must pass a colour-vision-deficiency check, since acces
  XFER(strong) Opt-in per-entity following that upgrades a member of the mass into a tracked narrative timeline. -> Player-pinned entities complement engine-triggered promotion in the fidelity-tier system, and directly populate the dossier Timeline tab. Hard constraint absent from CS2: pinning must not change simulation fidelity in any way that alters outcomes, or observation would mutate the world — which would break the replay guarantee.
  NOCOPY: Chirper — the bird-branded social-media feed, its mascot, its name, its visual treatment and its position on screen. The transferable idea is a provenance-tagged event stream; the bird is trade dress.; The advisor-icon-in-the-top-left-corner placement and the specific info-view icon grid layout of Cities: Skylines II. Adopt the register concept, place the control according to MERIDIAN's own comman
## Link-analysis and investigative graph tooling: IBM/i2 Analyst's Notebook, Maltego, Linkurious Enterprise, Neo4j Bloom
ONELINE: This product family has spent thirty years solving exactly MERIDIAN's hardest unsolved interface problem — letting a human interrogate a large directional, asymmetric, time-stamped, unevenly-evidenced relationship network without drowning in it — and its solutions are mechanical (thresholds, counters, drill-down breadcrumbs, re-encoding views, filter-as-first-class-panel) rather than aesthetic, which makes them transferable without touching trade dress.
  FAIL[The hairball: node-link readability collapses above roughly twenty vertices] Node-link diagrams stop being readable for most analytic tasks at surprisingly small sizes — far smaller than practitioners assume. Ghoniem, Fekete and Castagliola's controlled experiment found that 'matrix based visualization performs better than node-link diagrams on most tasks' for graphs exceedi | LESSON: MERIDIAN must decide its node-link budget NUMERICALLY and design the fallback before the graph ships. Twenty is the evidence-based order of magnitude at which a naive node-link view stops earning its place. Every one of MERIDIAN's relationship views should have a documented node ceiling and a define
  FAIL[Edge density, not node count, is what actually makes a picture feel complex] Teams optimise for node count and are then blindsided when a modest graph reads as impenetrable. | LESSON: MERIDIAN's graph budget must be expressed in EDGES per unit of screen area, not nodes. A ten-actor cabinet with every pairwise relationship drawn is worse than forty actors in a sparse hierarchy. This argues for showing only the strongest-k relationships per node by default, with the rest available 
  FAIL[Force-directed layouts overlap unrelated structures and are non-deterministic] The default layout in nearly every graph tool produces drawings where visually adjacent clusters have no relationship, and where re-running the layout produces a different picture from the same data. | LESSON: This is a HARD CONSTRAINT for MERIDIAN, not a preference. Dossier property D-2 requires a dossier to consume ZERO randomness, and D-1 requires byte-identical simulation across open/close. A randomly-seeded force layout in the Relationships tab would violate the spirit of both and destroy reproducibi
  FAIL[Supernode expansion is the specific interaction that detonates the display] A single innocuous double-click on a well-connected node can add thousands of nodes and destroy the user's mental model and spatial memory in one frame. | LESSON: Never ship an unbounded expand. Show the neighbour count on the affordance before the click; intercept above a threshold with a chooser; and forbid multi-supernode expansion outright as Linkurious does. In MERIDIAN, states, ministries and national media systems will all be supernodes on day one.
  FAIL[Matrix views are not a free substitute — they lose exactly the task analysts most need] Teams reading the matrix-versus-node-link literature sometimes conclude matrices are simply better at scale and switch wholesale, then find their users cannot trace a chain of connection. | LESSON: MERIDIAN cannot solve its hairball by replacing the graph with a matrix. It needs BOTH, plus a dedicated path query (i2's Find Path is the model). If MERIDIAN's Eight Questions #2 ('what caused it') is answered by tracing a causal chain, that is a path-finding task, and it needs a node-link or a pur
  FAIL[Vendor documentation teaches WHERE to click, not WHEN to use which measure] i2's page on selecting centrality measures is purely procedural: it tells the analyst that betweenness, closeness, degree and eigenvector exist and where the Options tab is, but provides no guidance on which question each measure answers or when to prefer one. The interpretive burden sits entirely o | LESSON: MERIDIAN's audience is NOT trained analysts. Every engine-computed measure it surfaces must ship with an inline, plain-language statement of what question it answers and what it does not mean — at the interface level, in the same visual register as the PROVENANCE RULE's tags. A number the player can
  FAIL[Drill-down that silently disables itself under a legitimate user state] In i2's histogram, 'if you have more than one selection range in the Histogram window, the Drill Down command is unavailable.' A user who has built a two-range selection — a perfectly reasonable thing to do — finds their next action greyed out, with the reason not necessarily co-located with the dis | LESSON: MERIDIAN will hit this constantly, because a simulation has far more invalid-state combinations than a database browser. Adopt a hard rule: no control is ever merely disabled — every disabled control states its unlock condition in place. This is the interface-level expression of the charter's promis
  XFER(strong) Never surface a single scalar 'importance'. Force the choice of a NAMED measure, expose its normalisation basis, and expose whether direction was honoured. -> The Relationships tab computes betweenness / closeness / degree / eigenvector-equivalents in the ENGINE, and the UI always shows measure name + normalisation mode + directionality. This satisfies the determinism boundary (deterministic graph maths, never LLM-produced) and Eight Questions #3 ('which rule/mechanism applied') simultaneously.
  XFER(strong) Missing, invalid and unavailable values are rendered as first-class rows with live counts, not omitted. -> Directly implements MERIDIAN's existing rule that Unknown and Restricted must be present rows. Build the confidence-label filter panel with all eight labels as rows carrying counts, so filtering by confidence makes the SHAPE of the player's ignorance visible. Linkurious has already shipped this pattern for missing/invalid.
  XFER(strong) Filtered-out elements dim rather than disappear, and an 'Applied Filters' strip makes the current view permanently self-describing. -> A MERIDIAN hard invariant: no view is ever filtered without an on-screen statement of the filter. Prevents the player from mistaking a filter for a fact about the world — the interface-level analogue of the provenance rule.
  XFER(strong) Expansion is bounded at the moment of the click: neighbour count shown on the affordance, threshold interception with a chooser, ordering made explicit, multi-supernode expansion forbidden. -> Every expand control in MERIDIAN's relationship graph shows its count; expansions above a threshold route through a chooser offering relationship type, direction (in/out, honouring asymmetry) and ordering (tie strength / recency / centrality). Silent truncation is banned.
  XFER(strong) Layout and encoding are ORTHOGONAL controls, both labelled by the question they answer rather than the algorithm they run. -> Separate 'arrange by' from 'size/colour by'. Label both in plain language ('Show who is central', 'Show the factions'), with algorithm names as secondary detail — matching the owner's plain-language-first principle and delivering command-system feel with zero borrowed trade dress.
  XFER(strong) Deterministic layout as the default; any stochastic layout must be seeded from a named RNG substream keyed on stable entity identity. -> Required to satisfy dossier properties D-1 (byte-identical across open/close) and D-2 (zero randomness consumed). Makes the currently-missing named RNG substreams a blocker for the graph UI, not only for the world model. Prefer hierarchical/circular/grouped layouts; make organic opt-in and reproducible.
  XFER(strong) Drill-down always leaves a clickable breadcrumb path describing the steps taken, with a return-to-all option. -> The route from macro indicator → cohort → named secondary → focal individual must be a breadcrumb the player can walk back. Build the breadcrumb BEFORE the drill; it is the component that makes tier traversal defensible, and it directly serves the two open critical findings about tier decoupling.
  XFER(moderate) Time is a filter with three distinguishable playback semantics — sliding window (state at each moment), expanding window (accumulation), narrowing window (convergence) — plus a nameable, switchable co -> Temporal scrub over the event-sourced history with all three modes named. The switchable-scale rule bears directly on the open 'macro indicators saturate monotonically' finding: log and linear scales over a saturating indicator tell opposite stories, so the scale must be visible and chosen, never hard-coded.
  NOCOPY: i2 Analyst's Notebook's ribbon-plus-chart-surface application shell and its overall Windows-desktop visual identity; The i2 law-enforcement/intelligence icon glyph library (the specific pictograms for Person, Vehicle, Bank Account, etc.) — build an original entity-class symbol set; i2's distinctive feature naming: 'Peacock' and 'Compact Peacock' layouts, 'Time Wheel', 'Activity View', 'Analyst's N
## Palantir Gotham and Foundry (plus adjacent entity-resolution / link-analysis platforms)
ONELINE: Palantir is the only widely-deployed production system that has solved the exact interface problem MERIDIAN faces — making a heterogeneous, uncertain, access-controlled world of persistent entities searchable, inspectable and actionable while keeping "what the data says" separate from "what a model inferred" — and its public failure record is an equally precise warning about demo-vs-daily-use, configuration burden and unproven value.
  FAIL[Very low actual adoption despite enormous contract value] The interface and platform were bought at scale and then largely not used. Of England's 215 hospital trusts, only 45 were actively using Palantir's platform by the end of 2024, against a £330 million contract. | LESSON: MERIDIAN must be fully usable with ZERO scenario-author configuration. Every entity type must render an honest default dossier, and every screen must degrade gracefully to a generic projection of the type system. Curated layouts are an enhancement. If MERIDIAN's ten-tab person dossier requires per-s
  FAIL[Incumbent tools were better at the specific job] Leeds Teaching Hospitals Trust concluded of the Palantir care-coordination product: "We would lose functionality rather than gain it by adopting them." NHS Greater Manchester found "no products designed or produced by Palantir as part of the FDP programme that exceed the NHS Greater Manchester local | LESSON: MERIDIAN's differentiator must be the thing generality *uniquely* enables — inspectable propagation through people, institutions, markets and media — and not a marginally-better version of something a purpose-built tool already does. Do not compete on map quality, chart quality or timeline quality. 
  FAIL[Read-only insight that cannot be actioned in the systems people actually work in] Foundry could extract and analyse but struggled to write results back into legacy systems. Palantir EVP Louis Mosley: write-back "is more challenging than reading from these systems, especially in systems that are very old," and "if ultimately the end step of that process is a manual write back, whe | LESSON: MERIDIAN has a structural advantage here — it *is* the system of record, so writeback is free — and should exploit it aggressively. Every inspection surface should be an action surface: from any dossier row, any lineage node, any event-stream line, the player should be able to open a PLAN compositio
  FAIL[Claimed benefits withdrawn under scrutiny] NHS England rewrote its own website to remove assertions that the platform cut waiting lists or boosted procedure volumes, replacing them with language conceding it "cannot therefore draw conclusions about cause and effect as other variables have not been controlled for." A parliamentary health comm | LESSON: Direct reinforcement of MERIDIAN's private-until-honest doctrine. Never let the UI imply a causal claim the engine cannot substantiate. Given the two open critical findings — tiers not causally coupled, macro indicators saturating monotonically — the current engine literally cannot answer Eight Ques
  FAIL[User revulsion and deliberate non-adoption for non-usability reasons] NHS staff resisted the software on ethical and trust grounds independent of its capability. Reported quotes include an official calling the vendor "ethically bankrupt" and a professional saying "It makes me feel sick every time I log into the thing and I know I'm not alone in that." Another said the | LESSON: MERIDIAN's dual-use constraint and mandatory fictional-world disclosure are load-bearing product features, not compliance overhead. The disclosure must be persistent, visible in the command shell (not a splash screen), and present in API responses — because the moment a MERIDIAN screenshot circulate
  FAIL[Vendor lock-in and no ownership of the resulting system] An NHS watchdog raised vendor lock-in concerns over the platform deal; a later report noted the £330M deal leaves the service without software ownership; and a committee recommended preparing alternatives well in advance of a February 2027 break clause rather than waiting. | LESSON: Not a UI lesson directly, but a scoping one for MERIDIAN as a portfolio project: the ontology/type system is the crown jewel and should be expressible as data (a versioned schema document) independent of the rendering layer. That also protects the design — if the type system is a document, the UI ca
  FAIL[Cost model excludes casual and exploratory use] Foundry "is not an off-the-shelf SaaS product with pay-per-use pricing. Instead, it requires expensive licenses that may be out of reach for some organizations." The same practitioner notes "Palantir hasn't always been transparent about its product capabilities." | LESSON: MERIDIAN's UI must be learnable by someone who has never used an intelligence tool, in a single session, without training. 'Design for recognition not memorisation' in the owner's thesis is exactly right and should be treated as a hard acceptance criterion: a first-time player must be able to reach 
  XFER(strong) Declare a semantic layer (what exists) and a kinetic layer (what can be done) in one published type system, and render every screen from it. -> Promote the ~16 consequence primitives from engine internals to a published, typed vocabulary alongside the entity type system. Add an 'interface'/shape concept so a primitive can be offered against any entity satisfying a declared shape. This is the mechanism that keeps PLAN mode from degenerating into a menu of pre-authored options — the charter'
  XFER(strong) Offer-but-block-with-reason: an unavailable action is rendered with its unmet declared preconditions enumerated, never hidden. -> The composer shows every primitive the player could in principle compose, each with a live list of unmet submission criteria — legal authority, budget, lead time, prerequisite, cooldown. This is a near-literal implementation of the charter sentence 'You can attempt this. Here is what is required, what may stop it...' and it requires the criteria to
  XFER(strong) Two form factors per entity: a full dossier and a compact panel projection, with an auto-generated default that needs no authoring. -> Every MERIDIAN entity type gets (a) the ten-tab full dossier and (b) a 5-8 row panel used wherever the entity is referenced from elsewhere. Both are pure projections, preserving properties D-1/D-2. The auto-generated default guarantees a new entity type is inspectable before anyone designs a screen for it.
  XFER(strong) Lineage is a destination application AND an in-place popover — the same graph read backward (provenance) and forward (impact preview). -> Build one causal graph over persisted causal_parents. Backward reader answers Eight Questions 2, 3, 5. Forward reader powers PLAN-mode consequence preview and Eight Questions 7, 8. Every rendered number carries a 'why this' affordance opening the compact reader, with a link to the full graph. Guarantees preview and post-hoc explanation cannot disag
  XFER(strong) Paint epistemic and access state onto the lineage graph itself — staleness, health and permission-check in Foundry become confidence label and visibility status in MERIDIAN. -> Each causal-lineage node renders its confidence label (the seven) and its visibility status (Restricted) as node state. From any node the player can ask 'why can I see this / why not more', which is the VISIBILITY axis made interactive. Unknown and Restricted appear as present coloured nodes, satisfying the 'absence of knowledge is content' rule in
  XFER(strong) Mandatory access markings propagate along data dependency, so derived artifacts cannot launder a classification. -> Attach visibility markings to state values and propagate them through the projection step that builds the player intelligence profile (view 4). A computed assessment inherits the most restrictive marking of its inputs. Without this, the four-views model leaks: a summary would reveal what its sources would not.
  XFER(strong) The unit of work is a saved, re-evaluating entity SET, not a screen — narrow it, view it several ways, drill into a member, act on the whole set. -> EXPLORE mode centres on named saveable entity sets that re-evaluate each tick. Sets are diffable across ticks (Palantir's 'compare and contrast object sets'), which is the cheapest honest way to render second- and third-order propagation. A set is also a valid target for a PLAN primitive, giving bulk action for free.
  XFER(moderate) Distribution-as-filter (histogram click-to-narrow) as the bridge between aggregate and individual. -> Render meso-tier cohorts as distributions the player can click to promote a slice into an entity set, then promote a member to a full dossier. This makes the macro/meso/micro tiers navigable in the interface as a single continuous drill, and it is a strong candidate for the first screen that makes tier coupling legible once the engine actually coup
  NOCOPY: Product and application names: Gotham, Foundry, AIP, Ontology-as-a-brand, Object Explorer, Object View, Workshop, Quiver, Contour, Vertex, Notepad, Code Workbook, AIP Logic, Nexus Peering. Use original command-system language — e.g. call MERIDIAN's screens something of its own, and prefer 'world model' or 'entity model' over 'Ontology' as a user-facing label given how strongly that word is now bra
## Military & emergency command-and-control interfaces: ATAK/WinTAK/CivTAK, MIL-STD-2525D / NATO APP-6 symbology, and COP/emergency-management displays
ONELINE: This is the only domain that has already solved, in a published and auditable standard, MERIDIAN's hardest rendering problem — how to show identity, status, confidence, staleness and "we do not know" as first-class visual properties of an object rather than as prose — and its documented failure mode (operators drowning in layers and features they then switch off) is precisely the failure MERIDIAN's dossier and event stream will hit.
  FAIL["WAY too many features" — the feature set outruns the users] In the only controlled user feedback I could obtain, TAK's ease of use scored 3.67 out of 5 (1 = "impossible to figure out", 5 = "no problem at all") from 14 firefighter respondents — a 50% response rate against 28 issued devices. One free-text response reads in full: "The app has WAY to(o) many fea | LESSON: For MERIDIAN: the charter's demand that a player can attempt anything must not be met by exposing every mechanism as a control. It must be met by ONE input surface (natural language intent) whose results are validated by the engine. Every mechanism that gets its own panel is a mechanism the player m
  FAIL[The core loop is learnable in minutes; the product is not] The same deployment reports that training consisted of "3–5 minutes of in-person training on the operation of the TAK app", covering only how to change the basemap, how to recenter by tapping the coordinate display bar, and how to drop and share points — plus a trifold pocket guide. Users with no An | LESSON: MERIDIAN should identify and protect its five-minute core — most plausibly: read the situation, propose one action in plain language, see what it would cost and who would resist. Everything else should be discoverable but never on the critical path. The 'surprised at what users discovered' finding a
  FAIL[Naive realism — richer, more realistic representations perform WORSE while being preferred] Smallman and St. John's work on 'naive realism' reports that 3-D realistic icons are harder to identify than conventional 2-D military symbols, and that users nonetheless PREFER the realistic versions. The stated mechanism is a "misplaced faith in human perceptual systems to extract information from | LESSON: This is a direct challenge to MERIDIAN's '10% cinematic presentation' allocation. The evidence says cinematic realism is exactly the thing stakeholders will ask for and exactly the thing that degrades identification performance. The defensible position is that cinematic treatment may be used for TRA
  FAIL[COP clutter is severe enough that automated decluttering measurably improves performance] In a naval air-defence simulation with 27 experienced US Navy personnel monitoring a cluttered airspace, an algorithm that continuously evaluated aircraft for threat level and dimmed the symbols of less threatening ones improved response timeliness to threatening aircraft by 25% versus an undeclutte | LESSON: Two lessons for MERIDIAN. (1) De-emphasis (dimming) beats deletion: nothing was hidden, so operators could still spot-check, and they did. MERIDIAN's event stream should rank and DIM low-relevance events rather than filter them out — filtering breaks the honesty doctrine, dimming does not. (2) The r
  FAIL[The symbology standard explicitly does NOT guarantee legibility — it delegates that to the implementer] MIL-STD-2525D repeatedly declines to specify the things that actually determine whether a symbol can be read. On line width: "The optimum line width may differ depending on frame size and be affected by whether the frame is filled or unfilled and displayed in color or black/white. Usability testing  | LESSON: Directly relevant to MERIDIAN's VISUAL-CONSTITUTION and DESIGN-TOKENS backlog. Adopting a semantic standard buys you interoperability and rigour, not legibility. MERIDIAN must own the perceptual layer itself — minimum sizes, contrast ratios, dot diameter, line weights — and must not assume that a we
  FAIL[Status is undisplayable for three of the seven identities — a channel collision the standard cannot resolve] "When the frame is assumed friend, suspect, or pending, the status will not be displayed." So for exactly the three identities where you are least certain what you are looking at, you lose the ability to say whether it is present or merely anticipated. The dashed-line channel is consumed twice over. | LESSON: MERIDIAN's eight confidence labels plus the separate visibility axis (Restricted) will hit this at the glyph level. The lesson is not 'invent more line styles'. It is: decide which attribute owns the channel, document the collision explicitly in the visual constitution as a known limitation, and ren
  FAIL[Inconsistent persistence — some artefacts silently do not survive a restart] The ATAK manual carries scattered notes that certain objects are ephemeral: "Note: The Red X is not persistent. When ATAK is closed and then reopened, the Red X will no longer be present." And: "Note: Viewsheds and Contour lines do not persist upon ATAK quit/restart." Meanwhile markers, routes, shap | LESSON: MERIDIAN has an exact analogue waiting in the dossier-is-a-projection rule (D-1/D-2). Projections are regenerated; player annotations are stored. If those two look identical on screen, the player will lose work and will not know why. Projected content and persisted content need a visible, consistent
  XFER(strong) Encode the most critical attribute in SHAPE, use colour as a redundant second channel, and vary only luminance between light and dark themes -> Entity glyphs and dossier row markers distinguish confidence and affiliation by outline shape first. MERIDIAN's theme system stores one hue per semantic and two luminance values, so light/dark is one design rather than two — and no meaning is ever carried by hue alone.
  XFER(strong) Solid versus dashed as the universal present-versus-projected distinction, with an explicit documented rule about which attribute owns the channel when two attributes collide -> PLAN-mode consequences, projected indicator trajectories and anticipated actor reactions render dashed; COMMAND-mode resolved outcomes render solid. The VISUAL-CONSTITUTION documents the collision between the confidence axis and the visibility axis and names the winner, rather than inventing a hybrid.
  XFER(strong) Two-axis confidence encoding with explicit 'cannot be judged' terminators on each axis, rendered in ~2 characters -> Re-examine MERIDIAN's eight confidence labels as source-reliability × corroboration, with freshness (Outdated) and visibility (Restricted) pulled out as separately-rendered properties. Gives the engine a far more testable computation and gives the dossier a compact per-row badge.
  XFER(strong) Render uncertainty as geometry in the measured quantity's own units, with the glyph form determined by the EVIDENCE TYPE rather than chosen by an author -> Eight Questions item (6) 'what uncertainty existed' becomes an engine-sized band on the indicator, whose visual form differs for single-report vs corroborated vs modelled evidence. The object itself stays crisp; only the claim about its value is qualified.
  XFER(strong) Show staleness as a visible GAP between last observation and current inference, not as a timestamp -> In the player intelligence profile, every extrapolated attribute shows last-observed value, the engine's current inference, and the elapsed distance between them as one connected visual. Resolves 'Outdated' as a magnitude rather than a label.
  XFER(strong) One object, N display fidelities, with fidelity selectable BY RULE per category rather than per object -> The four population fidelity tiers become one entity component rendered at four levels, with the player setting rules like 'cohorts render as dots unless inside the active consequence chain'. Preserves D-1 because fidelity is a view concern, not a state concern.
  XFER(strong) De-emphasise rather than delete; keep the ranking engine-computed and inspectable so operators can spot-check it -> The bottom event stream dims low-relevance events instead of filtering them. The relevance score is engine-computed and its inputs are viewable, so dimming never becomes silent suppression — which is what makes it compatible with the honesty doctrine.
  XFER(strong) Make the world's fictional/exercise/simulation context a mandatory FIELD of the entity identifier, not a banner -> Satisfies the dual-use disclosure constraint structurally: the API cannot emit an entity whose fictionality is unstated, and the UI banner becomes a rendering of that field rather than a separate promise that could drift out of sync.
  NOCOPY: MIL-STD-2525/APP-6 frame shapes and their colour assignments as a literal symbol set — the rectangle/diamond/square/quatrefoil vocabulary and blue-friendly/red-hostile/green-neutral/yellow-unknown mapping is recognisable trade dress of real military symbology and would also falsely imply MERIDIAN emits real-world-interoperable tracks; TAK's tool naming — Bloodhound, GeoChat, Red X, Point Dropper, 
## Bloomberg Terminal (with Refinitiv Eikon / LSEG Workspace and Koyfin-class challengers as counter-cases)
ONELINE: The Terminal is the only mass-deployed proof that a deliberately dense, keyboard-first, aesthetically "archaic" interface can be the preferred tool of tens of thousands of experts — and it works because command grammar, persistent context, colour-as-affordance, and in-product help form a single learnable layer system, not because ugliness is a virtue.
  FAIL[No back button — navigation is a stack the user has to keep in their head] There is no general 'back' affordance. The MENU key goes *up* to a menu of related functions, which is not the same as returning to where you were; the History key repopulates the command line with prior commands, which is recall, not navigation. Users report this directly as a defect. | LESSON: An addressable command grammar does NOT give you navigation history for free. MERIDIAN must build an explicit, first-class session history — which it needs anyway, because every state the player viewed is part of the replay/provenance record. Build back/forward on top of that record from the start, 
  FAIL[Discoverability collapses at vocabulary scale] Users report a 'huge learning curve and hard to find what you need'. The command grammar scales beautifully for functions you know and poorly for functions you do not know exist. Autocomplete, SEARCH, BU/BPS cheat sheets and a 24/7 human help desk are all compensations for this single structural wea | LESSON: MERIDIAN's vocabulary problem is worse, because its nouns (consequence primitives, mechanisms, institutions) are invented and the player has no prior domain knowledge to lean on. Every typed capability must have a browsable, previewable, recognition-based twin. The command line is the accelerator; i
  FAIL[Accessibility retrofit is incomplete — semantic colour without full palette coverage] The deuteranopia/protanomaly schemes are 'supported by a limited number of functions at this time', and specific critical controls — Buy and Sell buttons — are explicitly not yet compatible. | LESSON: MERIDIAN's DESIGN-TOKENS document must precede SCREEN-SPECIFICATIONS, not follow it. Since MERIDIAN is at zero screens today, this failure is entirely avoidable — and the eight confidence labels are exactly the kind of high-stakes semantic encoding that would become unfixable if hard-coded. Also not
  FAIL['Religiously consistent' becomes an argument against ever improving] Bloomberg's stated commitment to consistency for the sake of user attachment, combined with an IDEO redesign proposal in 2007 that did not result in a visible overhaul, is read by designers as evidence the interface is frozen rather than merely stable. | LESSON: MERIDIAN should adopt the consistency obligation *narrowly and explicitly*: freeze the grammar (command syntax, key bindings, colour semantics, panel zones) and declare everything else improvable. That gives you Bloomberg's stickiness without Bloomberg's paralysis. Write the frozen list into VISUAL-
  FAIL[The 'difficulty as status symbol' defence is a trap, and probably a misdiagnosis] The popular explanation — that traders defend the ugly UI because mastering it signals expertise, and 'the more painful the UI is, the more satisfied these users are' — is repeated widely, weakly sourced, and if believed leads designers to conclude that gratuitous difficulty builds loyalty. | LESSON: Do not let 'experts like density' authorise unexplained difficulty in MERIDIAN. The legitimate version of the claim is narrower: experts tolerate density and reject *slowness*. Every element of density must pay for itself in throughput or comprehension, and MERIDIAN's charter already has the right t
  FAIL[A more modern UI at a lower price did not displace the incumbent] Refinitiv Eikon / LSEG Workspace competes explicitly on 'modern UX', Office integration, browser and iOS access, and AI-powered personalised alerts — and has not dislodged Bloomberg, which had roughly 325,000 subscribers as of 2022 at roughly $24k–$30k per user per year, with terminal sales accounti | LESSON: Two-edged for MERIDIAN. Defensively: MERIDIAN has no data moat and no network effect, so usability IS its binding constraint and the Bloomberg licence to be difficult does not transfer. Offensively: this is the strongest available evidence that switching cost is generated by *workflow embedding* — s
  FAIL[Attacking the lock-in directly failed: the Symphony consortium] In 2014 fifteen major financial institutions put $66 million into Symphony (built on Perzo, led by Goldman Sachs, with Bank of America, BlackRock and JPMorgan among the investors) explicitly to replace Bloomberg's messaging and break the terminal's grip. Bloomberg's subscriber base and revenue conce | LESSON: Integration is the defensible asset, not any single feature. For MERIDIAN this is a warning about its own architecture: if the dossier, the command line, the event stream and the planning surface are separately good but not *linked* (shared loaded entity, shared history, shared provenance), the prod
  XFER(strong) Persistent loaded subject + bare-verb commands. State the noun once; it stays visible in a fixed shell location; every subsequent action is a verb applied to it until explicitly replaced. -> Put the loaded entity in the global status bar as a persistent, clickable token showing name, type, fidelity tier, and which of the four views is active. Make the ten dossier tabs addressable as bare verbs from the command line. Replacing the token is the only context switch; everything else is an interrogation of the standing subject. This also gi
  XFER(strong) One input field serving navigation, command and search, with autocomplete as the permanent training wheel — so novice and expert use the same control and differ only in whether they read the dropdown. -> MERIDIAN's EXPLORE/PLAN/COMMAND modes should be one field with a mode indicator, not three surfaces. The autocomplete must visually separate engine-resolvable completions (real entities, real views, real primitives — deterministic, zero randomness) from free-text destined for LLM interpretation, which must carry the provenance tag before submission
  XFER(strong) Explicit named linking groups across panels, with an instance-vs-copy distinction, so the user chooses which panels follow a selection and which stay pinned. -> The mechanism for making propagation legible. A player selects an actor in a relationship graph; the timeline, beliefs and event stream follow because they are in the same named group; a second actor's dossier and the macro indicator strip stay pinned for comparison. Show a link badge on every following panel. This is how MERIDIAN's core differenti
  XFER(strong) Closed, exceptionless colour legend encoding interaction affordance as well as meaning — learned once, applied to screens the user has never seen. -> Define a small closed legend in VISUAL-CONSTITUTION covering: editable/parameterisable, engine-computed fact, model-proposed text, navigable reference, and each of the eight confidence labels plus the Restricted visibility state. Never violate it. Because it is closed and exhaustive, a player can parse an unfamiliar institution dossier on first sig
  XFER(strong) Separate the ephemeral investigation surface from the standing-watch surface, and give the standing surface a browsable component catalogue with live previews and popularity ranking. -> Command-line + dossier = investigation. A Launchpad-equivalent = the crisis-in-progress watch: indicator strips, institutional faction balance, actor watch-lists, filtered event feeds. Build the catalogue with real preview thumbnails, because MERIDIAN's component names ('cohort pressure index', 'faction balance') mean nothing to a new player. Enfor
  XFER(strong) Graded one-keystroke help escalation where each level is deeper rather than elsewhere. -> MERIDIAN's escalation ladder should terminate in the engine, not a help desk: press once for 'what is this screen', press again for the full Eight Questions trace on whatever is focused. This turns the help system into the transparency system and makes the charter's explainability requirement a permanently available keystroke rather than a report t
  XFER(strong) Move persistent system-level verbs out of the content area into a permanent, never-moving control strip, so the content area can afford to be dense. -> MERIDIAN's stable command shell should permanently host: cancel, back (real back, from the session history record), help, search, cycle panel, command history recall, and the fictional-world disclosure required by the dual-use constraint. Command history recall in particular is cheap to build and is the difference between a text-first interface tha
  XFER(strong) Add generative AI as a named function inside the existing grammar, returning structure-plus-narrative with a source link on every response — rather than replacing the interface with a chat window. -> Validated commercially by ASKB. MERIDIAN's version is stronger: the link target is the engine trace (rule fired, primitive composition, RNG substream, assumptions), not a document. Render the engine-computed table and the model-authored narrative as visually distinct blocks with the provenance tag on the narrative block, satisfying the PROVENANCE R
  NOCOPY: Amber/orange-on-black terminal palette — Bloomberg's most recognisable trade dress; described in sources as 'the same old orange and black screen' and as a recognisable trait of the service. MERIDIAN must derive its dark palette from its own token system, not from this signature.; The <GO> execution token and angle-bracket/curly-brace command syntax ({VOD LN Equity GO}). The grammar concept transf
## Intelligence-analysis tradecraft standards for expressing uncertainty (ICD 203 / ICD 206, Sherman Kent's Words of Estimative Probability, the PHIA probability yardstick, the Admiralty/NATO A–F / 1–6 source grading system, Heuer's Psychology of Intelligence Analysis and Structured Analytic Techniques, and the finished-intelligence-product form)
ONELINE: This is the only mature body of practice that has already fought and largely settled MERIDIAN's hardest dossier problem — how to render a claim together with how well it is known, who says so, when it was true, whether someone is lying, and whether you are allowed to see it — and measured against it MERIDIAN's "eight confidence labels" is not a confidence scale at all but a single flattened enum spanning five orthogonal axes, with the primary axis of real tradecraft (likelihood) entirely absent.
  FAIL['Serious possibility' — the founding failure] Kent's canonical anecdote: a phrase in a national estimate (NIE 29-51, on Yugoslavia) that different senior readers understood as radically different probabilities, despite all reading the same sentence. | LESSON: Any MERIDIAN label that is not bound to an engine-computed range in a legend the player can open will be silently re-interpreted by every player differently. This is the argument for making the eight labels engine-derived AND publishing their definitions in-product.
  FAIL[The 2007 Iran NIE: 'confidence' used to mean probability] Friedman and Zeckhauser count 'confidence' used 19 times in the Key Judgements of the 2007 Iran NIE to express probability itself, rather than to qualify a separately stated probability. The document defined its estimative terms on a spectrum from 'remote' to 'very likely' but never reported the und | LESSON: This is the precise failure MERIDIAN is currently set up to reproduce. Its eight labels contain no likelihood axis, so 'Assessed' and 'Confirmed' will inevitably be read as probability statements. A lexicon does not enforce a distinction — separate structured FIELDS with separate rendering do.
  FAIL[Verbal-only probability destroys decision-relevant resolution] A judgement rendered only as 'unlikely' cannot distinguish a 1% threat from a 0.01% threat — a thousandfold difference that can invert the correct policy. | LESSON: MERIDIAN should band by default and reveal the number on demand (progressive disclosure layer 2/3), and should tighten bands in the tails for anything catastrophic. Never make the band the only representation the engine can express.
  FAIL[Admiralty two-axis grading collapses onto its diagonal in practice] Baker et al. (1968) found ~87% of ratings fell on the diagonal — A1, B2, C3 and so on — meaning the reliability and credibility scales are not used independently. The nominally two-dimensional system behaves as one dimension. | LESSON: Two axes are only worth building if they are driven by genuinely different inputs. MERIDIAN's engine can guarantee this in a way humans cannot — source-reliability from the entity's historical accuracy record, item-credibility from independent-channel corroboration in the same tick. If a proposed ME
  FAIL[Undefined grading vocabulary: 'authenticity', 'trustworthiness', 'competency'] The Admiralty definitions rest on terms that are never operationalised, and the boundary between 'reliable' and 'usually reliable' has no numeric anchor, producing subjective and inconsistent application across NATO users. The system also does not distinguish primary from secondary sources, or subje | LESSON: Every MERIDIAN label must have a machine-evaluable predicate, not a prose gloss. If a designer cannot write the boolean or the threshold that produces the label, the label is not engine-computed and violates the project's own 'computed by the engine, never authored' rule.
  FAIL[Consequence neglect: filtering out low-credibility reports that matter most] Source-grading frameworks downgrade information that is unconfirmed, illogical, or inconsistent with other information. But reports that contradict prior beliefs are precisely the ones with the highest potential impact. Friedman and Zeckhauser cite Pearl Harbor and the Cuban Missile Crisis as cases  | LESSON: Direct UI consequence for MERIDIAN: NEVER sort, collapse or hide dossier rows by confidence. A low-confidence, high-consequence row must be surfaceable — arguably promoted. This also gives 'Unknown' and 'Possibly deceptive' rows a positive reason to occupy screen space rather than being treated as c
  FAIL[ACH does not scale, and disorients analysts at realistic sizes] Tim van Gelder's critique: ACH demands too many discrete judgements, treats hypotheses as a flat list with no hierarchy or sub-argumentation, cannot represent subordinate reasoning, and leaves analysts disoriented or confused at realistic scale. It is also widely described as time-consuming and cumb | LESSON: If MERIDIAN builds an alternatives widget, do not build a raw grid. Show only the top few discriminating rows (highest diagnosticity), collapse the rest behind a count, and let the engine pre-rank — the engine knows which inputs actually flipped the resolution, so it can present three rows instead o
  XFER(strong) Split the single 'confidence' concept into orthogonal, separately-rendered axes, and never merge them into one badge -> MERIDIAN's eight labels should become a tuple on every dossier row, each element engine-computed: (1) VISIBILITY — visible / restricted / existence-concealed, styled in a distinct family (this absorbs 'Restricted'); (2) DERIVATION — observed / reported / inferred (this absorbs 'Reported' and 'Assessed', which are derivation modes, not confidence le
  XFER(strong) Bind every verbal label to a published numeric range with deliberate gaps between bands -> Ship an in-product uncertainty legend as a real screen. Band engine probabilities into seven non-contiguous bands with hysteresis at the gaps so labels do not flicker between ticks. Reveal the underlying number at progressive-disclosure layer 2. Tighten bands in the low tail for high-consequence judgements.
  XFER(strong) Portion marking: granular per-item provenance, with document-level indicators derived as an aggregate rather than authored -> Every dossier row carries its own provenance/visibility chip. Tab-level and dossier-level indicators are computed as the aggregate over rows and are visibly marked as derived. This satisfies MERIDIAN's PROVENANCE RULE at interface level and prevents the 'one header disclaimer covers a screen of mixed-provenance content' failure the rule exists to s
  XFER(strong) Confidence must decompose into named factors on demand -> The confidence slot expands into its inputs — evidence count, channel independence, recency, adversarial-control status, and simulation volatility of the variable. This is the same test as MERIDIAN's 'causal value, not fake depth': if a factor cannot be named and shown, the grade is decorative.
  XFER(strong) Key judgements first, individually graded, above the detail -> The Overview tab of a person/organisation dossier leads with 3–5 falsifiable propositions about the entity, each carrying its own likelihood band and confidence grade, above the fold. The other nine tabs are the supporting body. This is the concrete answer to the biography-bloat failure mode and to the charter's 'plain language first, technical det
  XFER(strong) An evidential-spine panel per tab, distinct from per-row grading -> A short engine-generated structural statement: which observations carry the key judgements, which conflict, and where source independence collapses (e.g. 'four of five items on this entity trace to one channel controlled by X'). Per-row chips cannot express this and no amount of chip polish will.
  XFER(strong) Institutional disagreement rendered as dissent attached to a specific judgement, not as an averaged position -> MERIDIAN's requirement that a state dossier make internal disagreement visible maps onto the NIE dissent-footnote form: the organisation's key judgement carries named constituent factions that assess it differently, each with its own likelihood band, and the aggregate position is shown as emerging from competing pressures rather than as a single nu
  XFER(moderate) Pre-registered diagnostic indicators — 'what would change this judgement' -> In PLAN mode, a proposed action shows a short strip of engine-derived observables that would confirm or refute each projected consequence, filtered for uniqueness so that non-discriminating indicators are dropped. Answers Eight Questions #8 and gives the player a reason to re-open a dossier later.
  NOCOPY: Real classification banner aesthetics — SECRET//NOFORN-style top-and-bottom banner lines, red/amber classification bars, cover sheets. MERIDIAN is fictional-worlds-only and must DISCLOSE fictionality; borrowing real classification chrome manufactures false authority and cuts against the dual-use constraint.; Real dissemination-control caveat words verbatim: NOFORN, ORCON, NOCONTRACTOR, REL TO, TS/
## Security operations, observability and incident-response consoles studied as one class: Microsoft Sentinel + Defender XDR (incl. Security Copilot), Elastic Security (incl. Attack Discovery), Prometheus Alertmanager + Grafana Alerting, PagerDuty, Datadog Watchdog
ONELINE: This is the only shipping software class that already solves MERIDIAN's actual hard problems — an unbounded event stream that must be ranked without hiding evidence, entity dossiers that are intelligence products rather than encyclopaedias, and AI narrative that must sit visibly outside the authoritative record.
  FAIL[The investigation graph: the vendor documents its own failure] Sentinel's node-link 'investigation graph' — the visually impressive artefact — is described by Microsoft's own documentation with an explicit caveat: 'The downside of the graph is that you end up having to switch contexts a great deal more.' It also cannot be used at all unless the analytics rule o | LESSON: MERIDIAN will be tempted to build a beautiful causal-propagation graph as its hero screen. A major vendor built exactly that, then demoted it and made a widgetised page the default. Build the graph as a secondary lens on a structured page, budget for its context-switch cost, and never let it render 
  FAIL[Truncation-by-widget, repaired with tooltips] The Sentinel incident page's Incident timeline and Similar incidents widgets truncate text because of the widget's limited width, and the documentation's remedy is instruction: 'Hover over any icon or incomplete text element to see a ToolTip with the full text' — stated twice, for both widgets, with | LESSON: If documentation has to teach hovering, the layout has failed. MERIDIAN's stream and dossier rows will carry long entity and mechanism names; design for the actual string lengths from the first mock, and if truncation is unavoidable, truncate in the middle so the discriminating tail survives.
  FAIL[The audit trail silently eats itself] Sentinel incident comments are capped at 30,000 characters per comment and 100 comments per incident, but the harder limit is that the incident record in the SecurityIncident table is 64 KB — and if exceeded, 'comments (starting with the earliest) are truncated'. Also: images cannot be uploaded to c | LESSON: Two lessons for MERIDIAN. First: if the player's own reasoning is worth capturing, give it a real store with a real schema, not a field appended to a run record — and never truncate the earliest entries. Second: an absent capability does not vanish, it becomes a workaround, and the workaround is usu
  FAIL[Search that gets worse the more precisely you search] Sentinel incident search defaults to only Incident ID, Title, Tags, Owner and Product name. Turning on advanced parameters triggers three regressions at once: auto-refresh becomes unavailable, only 50 results are shown at a time, and cross-workspace views are unsupported. Search strings are also cas | LESSON: A mode that silently changes correctness guarantees must announce itself in words, not hue — especially given MERIDIAN's own accessibility commitment and its rule that colour must be semantic rather than decorative. Where MERIDIAN's queries or filters become approximate, the UI must state 'showing f
  FAIL[Two live investigation experiences, plus a portal migration deadline] The docs open by telling users that if they are 'currently viewing the legacy incident investigation experience' they must toggle the new experience on at the top-right to follow the article. Simultaneously, entity pages differ between the Azure portal and the Defender portal (timeline and insights  | LESSON: MERIDIAN is pre-UI, so the transferable lesson is preventative: define the command shell's regions and their invariants *once*, in the VISUAL-CONSTITUTION document, before any screen is built. Two coexisting layouts for the same object is the single most expensive UI debt in this product class.
  FAIL[AI summarisation defaulted to always-on and had to be throttled] Copilot 'generates a summary for each incident the user opens' by default. Microsoft subsequently shipped a preview setting to restrict this: Auto-generate (Always / only above a chosen minimum severity) vs Generate on demand — and the severity picker had to display both an estimated number of incid | LESSON: MERIDIAN should not LLM-narrate every event. Make narration a thresholded, player-controlled, cached behaviour from the start; the engine-computed record must be complete and legible *without* any narration, with narration as a strictly optional layer over it. This also protects the determinism boun
  FAIL[The AI triage layer has an undisclosed sampling window] Elastic's Attack Discovery analyses, by default, up to 100 alerts from the last 24 hours. Copilot's incident summary covers incidents 'containing up to 100 alerts'. These caps are stated in documentation but there is no indication in either doc that the resulting UI surface tells the analyst which a | LESSON: For MERIDIAN this is close to a charter violation waiting to happen. Any generated narrative must render its coverage as engine-computed fact adjacent to the prose: 'synthesised from 100 of 412 events in this window'. Coverage is a confidence axis, and MERIDIAN already has the vocabulary for it.
  XFER(strong) Never render a group without rendering the joining predicate ('Similarity reason', group_by keys, suppression fields). -> Every collapsed cluster in MERIDIAN's event stream carries a machine-generated joining line — 'grouped because: shared causal parent E-441 + same institution' — sourced from the causal_parents the engine currently discards. This converts grouping from a display trick into an answer to Eight-Questions #2 and #3.
  XFER(strong) Suppression must be counted, hoverable and reversible to the raw constituents; never a silent drop. -> Stream rows show '×37' with a hover count and a drill-through to the 37 constituent entity-level events. This is what lets meso-cohort summarisation exist without becoming the 'fake depth' the world model forbids.
  XFER(strong) Three distinct noise semantics — group (same shape), inhibit (this one explains those), silence (I know, stop until X) — kept separate rather than merged into one mute. -> Inhibition is the mechanism that expresses MERIDIAN's differentiator: while a causal parent is unresolved, its downstream consequence events are inhibited and shown as a count on the parent. The player sees a causal story rather than a flood, and expanding the parent restores the full list.
  XFER(strong) AI output is quarantined by container and lifecycle controls, not by adjective — its own pane, its own regenerate/rate/show-working affordances, its own coverage statement. -> Advisory text lives in a persistently distinct region with a 'show proposed composition' control that renders the LLM's proposed consequence primitives as inspectable rows the engine then prices. The determinism boundary becomes a visible interaction rather than a documented claim. Coverage ('synthesised from 100 of 412 events') is rendered as engi
  XFER(strong) Absent and out-of-scope knowledge is rendered as a present row, differentiated redundantly (desaturated glyph + dotted rule + presence/absence of an action affordance), not by colour. -> Direct implementation route for the eight confidence labels and the visibility/confidence split: Restricted rows carry a 'request access' affordance, Unknown rows carry a 'task collection' affordance, Confirmed rows carry neither. Redundant encoding also satisfies the accessibility commitment.
  XFER(strong) Drill-to-mechanism from any summary claim, in a non-destructive in-place overlay that returns you to your position. -> Every engine-computed number opens an overlay showing the rule, the primitive, the inputs and the RNG substream draw — implemented as a pure projection so it consumes zero randomness and cannot mutate state (dossier properties D-1 and D-2 extended from dossiers to the whole UI).
  XFER(strong) Two densities of the same entity view — an in-context card scoped to the current window, and the full dossier — with distinct affordances for 'stay' vs 'leave', and the scope stated in words. -> Replaces the ten-tab dossier as the default. Three cards (identity+provenance / activity timeline / assessed anomaly and motivation) in the right context panel scoped to the current crisis window; row-click stays, name-click opens the full four-view dossier. 'This view covers turns 41–47' is always visible.
  XFER(strong) Actionability, not severity, decides whether something interrupts; informational items are a separate, non-interrupting class. -> Two visually and behaviourally distinct stream classes. A micro-tier event about a named family may enter the stream only if it changes an available option or explains a macro movement — giving the project a hard editorial test for its differentiator that also enforces the causal-value-not-fake-depth rule.
  NOCOPY: Microsoft's incident-page widget names and the 'attack story' framing, and Sentinel's specific Overview/Entities two-tab signature with its four named widgets (Incident timeline / Similar incidents / Entities / Top insights).; The MITRE ATT&CK matrix as a visual artefact, and tactic/technique chips — a real-world framework, and MERIDIAN's charter forbids real-world operational content.; Security C
## Safety-critical control-room interface design canon (Ecological Interface Design / Endsley SA / EEMUA 191 + ISA-18.2 alarm management / high-performance process displays / ATC and aviation alerting / NASA mission control)
ONELINE: This is the only body of interface knowledge that has been forced to answer MERIDIAN's exact three problems — how to make causality navigable on screen, how many alerts a human can actually act on, and when colour is allowed to mean something — under conditions where getting it wrong killed people, and it answers them with numbers rather than taste.
  FAIL[Texaco Milford Haven refinery explosion, 24 July 1994 — the founding alarm-flood case] In an 11-minute window, an alarm system emitted more alerts than two human beings could physically process, while the graphics gave them no way to understand the plant state. | LESSON: 275 alerts in 11 minutes is roughly one every 2.4 seconds — far beyond acknowledgement, let alone action. The failure was not any single bad alarm but the absence of a rate budget and an admission test. For MERIDIAN: an event-generating simulation will produce this by default unless throttling is an
  FAIL[Three Mile Island — the undifferentiated alarm cacophony and the lying lamp] Operators could not distinguish signal from noise among the alarms, and the one indicator that would have revealed the loss-of-coolant reported a proxy state and misled them. | LESSON: Two independent failures compound: no prioritisation destroys the alert channel, and a mislabelled proxy destroys the diagnostic channel. MERIDIAN's exposure is the second one — rendering a player-intelligence-profile value with the styling of authoritative reality is the same class of defect, and i
  FAIL[Qantas Flight 32 — a modern, well-designed alerting system still floods] An uncontained engine failure cascaded into an alert volume that consumed the crew for over an hour, requiring one pilot to be dedicated full-time to sifting checklists. | LESSON: Tiering and prioritisation are not sufficient. Without causal grouping, a correlated event produces N correct alerts and one overwhelmed human. MERIDIAN needs root-cause collapsing built in from the start: alerts sharing a causal parent must render as one expandable item, which is only possible if c
  FAIL[STCA nuisance alerts when the prediction horizon outruns prediction quality] Extending the conflict-prediction look-ahead beyond roughly two minutes causes 'more and more nuisance alerts' rather than more warning. | LESSON: Predictive reach is a confidence-bounded quantity, not a feature to maximise. MERIDIAN's projection surface must shorten its horizon when engine confidence falls, and must never predict further than it can defend. A long confident-looking forecast is the fastest route to the project's own named defe
  FAIL[HAZOP-driven alarm proliferation — the governance failure that creates floods] Safety studies routinely close out findings by adding an alarm, so alarm counts grow monotonically with no one owning the aggregate operator burden. | LESSON: This is a process defect, not a design defect, and it is exactly how MERIDIAN will acquire an alert flood: each new mechanism ships with its own notification, each locally reasonable. Charge every new alert against a fixed global budget and require the aggregate rate to be reported when the alert is
  FAIL[The cost of retrofitting alarm rationalisation] Fixing an alarm system after the fact is enormously expensive per alarm. | LESSON: More than one shift of skilled work per alarm. MERIDIAN has no alert model yet, which is a rare and closing opportunity: define the Alert schema with mandatory defined_response, causal_parent, priority and provenance fields BEFORE any alerts exist, rather than rationalising hundreds later.
  FAIL[Ecological Interface Design's own limitations] EID assumes the designer can analytically identify the information the operator needs from the work domain, and building a full abstraction hierarchy for a genuinely complex system is very costly. | LESSON: Do not adopt EID as MERIDIAN's whole method. Use the abstraction hierarchy to decide what CAN be explained and how explanation is structured, but keep conventional usability testing for whether players actually use it. This is direct support for the owner's '70% familiar professional software' thesi
  XFER(strong) Alert-rate budget as an engine invariant: at most 1 attention-demanding alert per 10 minutes of nominal operation, at most 10 in the first 10 minutes after a major event. -> Enforce in the engine, not the UI, since MERIDIAN generates its own events. Instrument the rate and assert it in tests over long simulated runs. Overflow goes to a browsable log, never the alert channel. This doubles as a detector for the known macro-saturation critical finding — an engine with no decay or cooldown will blow the budget immediately 
  XFER(strong) Admission test: nothing becomes an alert without a named operator response; status indicators are not alerts. -> Make defined_response a non-nullable field on the Alert type so an alert is unconstructable without it. Indicator movements, tier promotions and narrative colour render as status. Corollary that falls out for free: the LLM can never mint an alert, because it cannot author an engine-honoured response — it can only annotate one.
  XFER(strong) Three priorities, distributed roughly 5% / 15% / 80%, with priority defined by consequence-of-non-response rather than magnitude-of-change. -> Compute priority from the consequence primitives — an unmet obligation, an expiring approval window, a triggered retaliation — not from indicator deltas. Measure the realised distribution across a run and treat drift toward high-priority as a regression. This gives semantic colour an engine-computed source and prevents priority becoming an authored
  XFER(strong) Escalating multi-channel signatures with a deliberately silent lowest tier (ECAM levels 3/2/1). -> P1 = colour + persistent shell indicator + sound; P2 = colour + indicator + single sound; P3 = colour in event stream only, silent. Map ECAM's 'loss of system redundancy' tier onto MERIDIAN's 'removed a future option' primitive and give it the silent tier — it matters, it is never urgent, and it is exactly Eight-Questions item 8.
  XFER(strong) Awareness tier and action tier are separated, and acting from the awareness tier is structurally prohibited (TCAS TA vs RA). -> Alerts surfaced in EXPLORE and the bottom event stream carry no action affordances at all; acting requires a deliberate transition into PLAN or COMMAND. Promote between tiers by engine-computed time-to-consequence in simulated turns. Also gives the conversational interface a safety property: a natural-language turn can raise an advisory but can nev
  XFER(strong) Predictive horizon bounded by prediction confidence, and visibly shortened when confidence drops (STCA's ~2 minute ceiling). -> The Level-3 projection surface states its own reliable horizon and contracts it as engine confidence falls. Never project further than the engine can defend. This is the eight-confidence-label honesty posture applied to the time axis, and it protects against the project's defining defect on the one surface most tempting to oversell.
  XFER(moderate) Colour reserved as an exception channel; the nominal display is achromatic and the null state is verifiable at a glance. -> Neutral achromatic shell and operational view when nominal. Render the eight confidence labels typographically and positionally rather than by colour, keeping the colour channel free for priority and abnormality. Spend the 10% cinematic budget on motion, scale and typography, not saturation. Resolve deliberately: 'Unknown' and 'Restricted' rows mus
  XFER(strong) The go/no-go poll: force each affected domain to compress its full analysis into one binary verdict plus a one-line reason at a defined decision moment. -> Build this as the PLAN→COMMAND transition. The shell polls each affected institution and faction, returning GO / NO-GO with a single-line reason. This is a compact non-LLM rendering of 'which actors reacted' and 'who will resist', and a split verdict within one state's constituent bodies makes internal disagreement visible exactly as the world mode
  NOCOPY: Airbus ECAM's signature two-screen identity (Engine/Warning Display above System Display), its MEMO/STATUS page vocabulary, and its specific master-caution/master-warning lamp-and-chime trade dress. Take the three-level tiering and the silent lowest tier; invent MERIDIAN's own surfaces and sounds.; TCAS's aural phraseology ('Traffic, traffic', 'Climb, climb') and the green-arc vertical-speed indic
## C2PA / Content Credentials, plus the AI-labelling and over-reliance research corpus
ONELINE: This is the only body of work that has already attempted MERIDIAN's charter provenance rule at scale, and it supplies both a proven layered-disclosure mechanism and hard experimental evidence that naive labelling manufactures false confidence in the UNLABELLED engine output sitting next to it.
  FAIL[The implied truth effect] Labelling a subset of content raises the perceived accuracy of the UNLABELLED remainder above what it would have been with no labelling at all. | LESSON: Never label one pole only. If MERIDIAN tags AI text and nothing else, it has silently promoted every unlabelled number — including saturating macro indicators and scenario-authored seed values — to apparent verified status. Label engine-computed content explicitly too, and define a visible third sta
  FAIL[Inverse implied truth — labels licence dismissal of true labelled content] AI labels made participants MORE credulous of false claims illustrated with unlabelled human-made images, and MORE hesitant to believe TRUE claims illustrated with labelled AI images. | LESSON: A provenance tag must not be readable as a quality verdict. MERIDIAN's tag should answer 'how was this produced' and nothing else, and the L2 card should say so in words. Otherwise players will discount a correct AI-proposed composition and accept a wrong unlabelled one — the precise inversion of th
  FAIL[Explanations do not reduce over-reliance, and can worsen it] Adding explanations to AI recommendations 'does not appear to reduce the overreliance' relative to no explanation. | LESSON: MERIDIAN's Eight Questions are, in interface terms, an explanation panel — and this literature says an explanation panel alone will not produce careful players. The Eight Questions must be an interaction, not a wall. At minimum, one of the eight (assumptions) should require engagement before a COMMA
  FAIL[The interventions that work best are the ones users hate most] 'people assigned the least favorable subjective ratings to the designs that reduced the overreliance the most.' | LESSON: Do not A/B a forcing function on satisfaction — it will lose. Budget the friction deliberately and spend it only at the PLAN-to-COMMAND boundary, where the stakes justify it. This is also why the 70/20/10 thesis is sound: friction must be rationed, so the rest of the interface must be frictionless.
  FAIL[Cognitive forcing benefits are inequitably distributed] An intervention-generated-inequality audit found forcing functions benefit users with higher Need for Cognition more, potentially disadvantaging those with lower intrinsic motivation for analytical thinking. | LESSON: MERIDIAN's accessibility-is-not-optional stance must cover COGNITIVE access, not only sensory. A design that only protects the most analytical players from over-trusting the model is a design that fails the median player. Pair any forcing function with a passive safeguard that needs no effort — e.g.
  FAIL[Trust drops, behaviour does not] A claim-evidence verification interface 'significantly lowered participants' trust compared to the baseline', but 'this increased caution did not translate to behavioral changes, as people continued to rely on LLM-generated scholarly edits to avoid a cognitively burdensome task'. | LESSON: Measuring MERIDIAN's provenance system by self-reported trust or comprehension will produce a flattering and meaningless result. Measure BEHAVIOUR: did the player open the L2 card, did they change the composition, did they catch a seeded wrong advisory. And attack the cost side — make verification c
  FAIL[Verification cost, not bias, is the binding constraint] Null results for explanations 'could be due in part to the explanation not sufficiently reducing the costs of verifying the AI's prediction'. Task difficulty, explanation difficulty and incentives all moved whether people verified. | LESSON: The design target is not 'more provenance', it is 'cheaper verification'. One click from a narrated sentence to the exact engine value and rule identifier that produced it. If MERIDIAN's L2 card requires the player to read a paragraph and cross-reference a tab, it will be ignored regardless of how c
  XFER(strong) Label both poles, never one. Every meaningful surface element carries one of exactly three provenance states — engine-computed, model-proposed-engine-validated, model-narrated — and a fourth explicit  -> Amend the charter provenance rule. As written ('every AI-generated advisory text carries a visible provenance tag') it produces the implied truth effect on every untagged engine number. The rule should read: every meaningful element carries a provenance state; AI-generated text is one of the states, not the only tagged one.
  XFER(strong) Four-tier progressive disclosure with a fixed job per tier, and validation status pushed down to the cheapest tier. -> L1 glyph = provenance state, always visible, never hover-only. L2 hover/click card = producer, inputs it was permitted to see, simulation tick. L3 = the Eight Questions plus the structured composition. L4 = replay and versioned external-input forensics. Write these four tiers into SCREEN-SPECIFICATIONS as a global pattern, not per screen.
  XFER(strong) Provenance markers are spatially bound to the content they qualify, overlapping or adjacent, never a page-level banner. -> Per-block tagging inside dossier tabs and the bottom event stream. A single session-level 'AI in use' notice does not satisfy the charter, because the charter's whole point is distinguishing adjacent elements from each other.
  XFER(strong) Optimise verification COST, not disclosure VOLUME. One gesture from any narrated sentence to the engine value and rule identifier behind it. -> The L2 card must resolve the question in one glance without navigating away. If reading the provenance costs more than re-deriving the fact, players will do neither.
  XFER(moderate) Spend friction only at the authority boundary. One cognitive forcing step at PLAN-to-COMMAND, requiring engagement with the assumptions, and none elsewhere. -> Fits the 70/20/10 thesis exactly: the forcing step is the moment that earns cinematic and interruptive treatment. EXPLORE stays frictionless. Expect users to rate the forcing step poorly and do not remove it on that basis.
  XFER(moderate) Render disagreement as a first-class competing assessment with its own provenance, rather than a consensus line with a caveat. -> Serves charter Question 7 (alternative outcomes), the world-model requirement that organisations show internal factional disagreement, and the dissenting-explanations over-reliance finding, with one mechanism. The state dossier as portal-into-constituents is already the right shape for this.
  XFER(strong) Fail to a neutral named state, never an accusatory or silent one. Missing, broken and restricted provenance are each present rows with distinct wording. -> Extends the existing 'Unknown and Restricted are present rows' rule to provenance itself. Add 'Provenance unavailable' as a rendered state and strip authority styling from any block in it.
  XFER(strong) Freeze the user-facing vocabulary and ban internal schema nouns from the surface. -> The eight confidence labels become frozen content tokens in DESIGN-TOKENS with one canonical string each. The sixteen consequence primitives are schema and need separate player-facing phrasings, decided once.
  NOCOPY: The C2PA pin-shaped glyph containing lowercase 'cr' in an outlined circle with a 90-degree corner — this is a registered visual mark of the standard with construction rules and forbidden modifications; MERIDIAN needs an original glyph; The name 'Content Credentials' and the phrase 'Signed by' as system nomenclature; Adobe / Content Authenticity Initiative visual identity and the specific L2 card l