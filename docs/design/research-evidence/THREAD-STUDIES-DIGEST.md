## Uncertainty visualisation research and its practical encoding rules, applied to MERIDIAN's eight confidence labels, uncertain map contacts, forecast ranges on indicators, and "Unknown" rows
SUMMARY: PROCESS DISCLOSURE FIRST: the session's WebSearch budget was already exhausted (200/200) before my first query, so this synthesis was built from ~16 direct fetches of primary sources whose canonical URLs I could reach, plus PDF text extraction. Every claim below is tagged as verified-in-session or unverified. Three sources I wanted and could NOT obtain are flagged explicitly: ICD 203 (US IC analytic standards probability table — dni.gov and FAS mirrors returned 403/empty), Budescu et al. on cross-national interpretation of IPCC probability words (Nature paywall), and Sherman Kent's original CIA essay (403; I have his table only via Wikipedia, a secondary source).

The literature converges on four things that are directly load-bearing for MERIDIAN. (1) Point estimates and capped error bars 
  FIND(well-established) Confidence intervals and error bars are robustly misinterpreted, including by trained researchers; experience does not help. | WHY: MERIDIAN's default instinct for a forecast band will be a capped error bar or a '±' range. The evidence says that mark is not a neutral container — it reliably transmits a Bayesian reading the engine cannot honestly supp
  FIND(well-established) The bar-plus-error-bar idiom changes decisions about uncertain data, and gradient/violin encodings align human reasoning better with the underlying statistics. | WHY: Gives MERIDIAN a directly substitutable mark: replace capped intervals with gradient strips in dense tabular contexts where a dotplot will not fit.
  FIND(well-established) Quantile dotplots — discrete frequency framing — reduce the variance of users' probabilistic estimates versus continuous density displays, on small screens and for untrained users. | WHY: This is the specific, citable basis for making a dotplot MERIDIAN's default forecast mark rather than a density curve, a fan chart, or an error bar.
  FIND(contested) Hypothetical outcome plots are NOT uniformly superior — they are markedly better for relational/probability judgments and markedly WORSE for estimating a mean when variance is high. | WHY: This is the finding that should stop MERIDIAN shipping animated 'possible futures' as the house style. It is a task-conditional tool. Reported as contested because the two strongest papers point different directions on d
  FIND(well-established) Explicitly highlighting and annotating missing/imputed data improves BOTH perceived data quality and interpretation accuracy; downplaying or removing it degrades both, and removal that breaks visual continuity produces outright wrong answers. | WHY: This is the empirical charter for MERIDIAN's 'Unknown must be a present row' rule, and it extends the rule from dossier tables to indicator time series. It also rules out the seemingly-honest option of simply not drawing
  FIND(well-established) Within the missing-data result there is a sharp sub-finding: annotation only helps if it preserves continuity. Disconnected error bars performed badly. | WHY: Prevents MERIDIAN from 'fixing' a gap by dropping in floating interval marks. The correct pattern is a continuous but visibly different segment, not a hole with decorations around it.
  FIND(well-established) Value-suppressing uncertainty palettes (VSUPs) budget the colour channel non-uniformly: more distinguishable value steps where uncertainty is low, fewer where it is high — and they measurably increase how much people weight uncertainty in decisions. | WHY: This is the correct encoding for MERIDIAN's regional/choropleth indicator layers. It makes an uncertain region physically unreadable for fine value differences, which is an honesty mechanism enforced by the encoding rath
  FIND(well-established) Sketchiness/hand-drawn rendering damages quantitative judgment and is perceived inconsistently across individuals, though it can increase engagement and willingness to critique. | WHY: Kills the intuitively appealing idea of rendering low-confidence MERIDIAN objects as 'sketchy'. It may be admissible for non-quantitative objects (an unconfirmed boundary line) but never for a mark whose size or area is 
  FIND(well-established) The IPCC treats confidence and likelihood as two orthogonal metrics, publishes fixed numeric bands for its seven likelihood words, and explicitly forbids using the mid-range word to express ignorance. | WHY: This is the closest institutional analogue to MERIDIAN's eight-label scheme and it independently validates MERIDIAN's two-axis (visibility vs confidence) design. Its 'do not use about-as-likely-as-not for lack of knowled
  FIND(well-established) The IPCC also warns that framing changes interpretation and that readers rescale likelihood language according to perceived consequence severity. | WHY: Directly relevant to a crisis simulator: the same '20% chance' chip will be read differently on a casualty indicator than on a procurement-delay indicator. MERIDIAN cannot assume a label means the same thing everywhere, 
  REC[Eight confidence labels; dossier rows; e] Encode each of the eight labels on THREE redundant channels, with colour as an optional fourth: (1) a text word, always spelled out, never abbreviated or truncated; (2) a glyph drawn from an 8-member shape set that stays discriminable at 12px — proposed assignment: Confirmed = filled square, Reported = square filled from the left half, Assessed = hollow square, Disputed = square split by a diagona
  REC[Eight confidence labels; player intellig] Split the eight labels into TWO CHIP FAMILIES that are visually non-comparable, because they are not on one scale. Family A (visibility): Restricted only — rendered as a neutral grey chip with a solid diagonal bar, positioned where the value would be, replacing it. Family B (confidence): the other seven, rendered in the confidence chip style at the END of the value row, never replacing the value. 
  REC[Eight confidence labels; Eight Questions] Make every confidence chip a control, not a decoration. Clicking it opens an evidence receipt popover containing, in fixed order: (a) the label and its one-sentence definition; (b) the count and type of supporting observations; (c) the age of the most recent observation in days; (d) the named rule/threshold that produced this label from that evidence; (e) what would change it ('two independent obs
  REC[Forecast ranges on macro/meso indicators] Ban capped error bars and '±' notation from engine outputs entirely. Where a full distribution can be shown, use a quantile dotplot of 20 dots (one dot = one twentieth = 'one run in twenty') with a median tick. Where vertical space is under ~24px (dense tables, inline text), use a horizontal gradient interval strip whose opacity ramps to the tails — not a capped bar. Every interval mark carries a 
  REC[Forecast ranges on indicators; determini] Gate all of the above behind an infrastructure precondition and enforce it in code: if the run record contains fewer than N stored ensemble samples for an indicator, the UI renders the value with NO interval and NO distribution — it renders 'Single run — no range computed' as an explicit state. Do not synthesise a band from a variance parameter, a heuristic, or a model. Add a build-time or test-ti
  REC[Forecast ranges; PLAN mode; dossier D-2 ] Do NOT make hypothetical outcome plots the default. Permit animation only as an explicitly invoked, dismissible 'Run this 20 times' control, available in EXPLORE and PLAN modes only and never in COMMAND, never as the sole representation of a value, always alongside the static quantile dotplot, always with a prefers-reduced-motion opt-out that falls back to the static dotplot. Frames must be drawn 
  REC[Verbal probability; LLM narration layer;] Publish a versioned, engine-owned probability lexicon as a constant exposed through the API, modelled on the IPCC's seven-term table, and render the word and the band together, always, as one atomic string: 'Likely (66–100%)'. Narration models may only select from lexicon terms by key; free-text probability adjectives from any model output are rejected at the validation boundary. Version the lexic
  REC[Unknown rows; Restricted rows; all ten p] Render 'Unknown' as a full-height row with five parts, never as an empty cell, em-dash, hidden row, or greyed placeholder: (1) the field name at normal weight and normal contrast — do NOT dim it; (2) the Unknown chip; (3) a REASON CODE from a closed enum — never-observed / not-observable-to-you / source-lost-access / superseded-and-unrefreshed / conflicting-and-unresolved; (4) an age figure where 
  REC[Macro indicator charts; meso cohort tren] For indicator time series with observation gaps: draw observed segments solid at full weight; draw the gap as a CONTINUOUS but visually distinct segment (dashed centre line inside a low-opacity band spanning the plausible range), with the endpoints of the gap explicitly marked. Never break the line. Never draw a solid line across the gap. Never zero-fill. If the engine imputes, name the method in 
  REC[Uncertain map contacts; centre operation] For uncertain map contacts, forbid the crisp point icon. Render every non-Confirmed contact as: (a) TWO nested containment rings, not one — inner at 50%, outer at 90% — each carrying an in-canvas numeric label ('50%: 4.2 km'), because a single ring is read as a hard boundary; (b) a centre glyph that is hollow and one size step smaller than a Confirmed contact; (c) an in-canvas caption stating what
  REC[Regional macro/meso layers; map chorople] For regional/choropleth indicator layers, use a value-suppressing uncertainty palette rather than a bivariate grid: four uncertainty tiers mapping to {8, 4, 2, 1} distinguishable value bins, with the top uncertainty tier rendering a single flat neutral so no value can be read off it at all. The legend must be the wedge/tree shape that shows bins collapsing, not a square matrix — the legend's own g
  REC[Visual constitution; map layers; low-con] Ban blur, sketchiness and hand-drawn rendering from any mark whose size, length, area or position encodes a quantity. Permit sketchy/dashed treatment ONLY for non-quantitative boundary objects (an unconfirmed territorial line, a rumoured supply route), and only with a redundant text label attached to the object itself. Never use opacity alone as the sole uncertainty channel on a mark that overlaps
  PITFALLS: Encoding confidence by hue alone. Fails WCAG 1.4.1's normative text verbatim, and in a crisis UI it also collides with severity colour — the player cannot tell whether red means bad or means poorly-evidenced.; Shipping capped error bars or '±' as the default range mark. Hoekstra et al.: 562 participants, six false statements, >3 endorsed on average, and researchers performed no better than statistically untrained first-years. Training your users out of this is not an available option.; Making animated HOPs the house style. They significantly INCREASED mean-estimation error under high variance (p<0.001, Hullman et al. 2015) — and mean estimation is the most common read in an indicator dashboa
## Provenance and fact-versus-narration separation in an interface: a specification proposal for MERIDIAN's per-element AI provenance marking
SUMMARY: MERIDIAN's charter demands that every AI-generated advisory text carry a visible provenance tag distinguishing it from engine-computed fact, per element, at a glance, surviving a screenshot of a single block. Research across four literatures — accessibility standards (WCAG, CSS forced-colors), advertising/editorial separation rules (CAP Code, native-ad recognition research), AI content provenance (C2PA, EU AI Act Art. 50, IBM Carbon), and empirical label studies (PNAS Nexus n=7,579; arXiv 2503.05711 n=911; implied-truth-effect literature; NN/g banner blindness) — converges on five hard constraints that eliminate most of the obvious designs.

(1) The fashionable AI treatments are technically void: gradients (`background-image`) and `box-shadow` are FORCED TO `none` in forced-colors/high-con
  FIND(well-established) Gradient, glow and shadow treatments — the dominant industry visual language for 'this is AI' — are erased entirely in forced-colors / Windows High Contrast mode. | WHY: MERIDIAN cannot use a gradient border, shimmer or tinted glow as the primary provenance carrier. If it does, high-contrast users see narration and engine fact as identical text, silently violating the charter's provenanc
  FIND(well-established) Warning labels applied to a subset of content raise the perceived accuracy of the unlabelled remainder (the implied truth effect). | WHY: MERIDIAN's design instinct — tag the AI text, leave engine output bare — actively inflates the authority of everything untagged. Any narration block that misses its tag (render bug, new component, export path) is not mer
  FIND(well-established) AI-provenance labels reliably shift belief about origin but have essentially no effect on user behaviour. | WHY: Provenance marking in MERIDIAN must be justified as epistemic hygiene and auditability, not as a safety control that stops a player acting on narration. If the team claims the tag 'prevents players treating flavour text 
  FIND(well-established) Generic 'AI-generated' wording is read by users as 'wholly authored by AI', and process-only labels are the weakest performers. | WHY: MERIDIAN's actual pipeline is: LLM proposes a composition of consequence primitives, the engine validates/prices/resolves, and a model narrates the engine's output. A bare 'AI-generated' chip misdescribes this in the dir
  FIND(well-established) Boxed, tinted, visually-distinct rectangles that differ from their surroundings are read as advertising and skipped — the very treatment most designers reach for to mark AI content. | WHY: Candidate strategy B (tinted panel per narration block) risks producing a provenance marker that is technically present and perceptually absent. Worse, it trains the 'hot potato' avoidance pattern: once a player learns t
  FIND(well-established) A government design system with substantial user research reports that its own container-based differentiation component is frequently not noticed. | WHY: This is direct, primary evidence against container-only provenance. MERIDIAN's dossier screens are exactly the 'complex pages near other visually prominent elements' case — ten tabs, confidence chips, relationship graphs
  FIND(well-established) The established accessible pattern for a semantic marker is: a decorative glyph hidden from assistive technology, plus visually-hidden text carrying the meaning — not an icon with a title attribute. | WHY: Gives MERIDIAN a copy-ready, standards-backed markup pattern for the provenance stamp that satisfies WCAG 1.3.1 (relationships conveyed by presentation must be programmatically determinable) without adding visible clutte
  FIND(well-established) WCAG 1.4.1 forbids colour as the sole carrier and accepts glyphs, patterns, text, or a hue+lightness difference of at least 3:1 as sufficient alternative cues; 1.4.11 requires 3:1 for graphical objects needed to understand content; 1.4.3 requires 4.5:1 for text below 18pt/14pt bo | WHY: These give exact, arguable numbers for the spec: the gutter rule is a graphical object required to understand the content, so ≥3:1 against the page background is mandatory, not optional; the stamp text is small text, so 
  FIND(well-established) Roughly 8% of men and 0.4% of women of Northern European ancestry have congenital colour vision deficiency, dominated by deuteranomaly (5.0% of men) — but tritan deficiency is vanishingly rare (0.008%). | WHY: Concrete design consequence: a red/green distinction between fact and narration fails for about 1 in 12 male users, while a blue/amber axis is safe for almost everyone. If MERIDIAN wants any colour component in the prove
  FIND(well-established) C2PA specifies a four-level progressive disclosure model for provenance metadata (L1 indicator → L2 compact summary → L3 full assertion breakdown → L4 forensic view) and warns against burning the indicator into pixel data. | WHY: Directly transferable architecture for MERIDIAN's 'show model/prompt version/temperature without clutter' problem: nothing beyond L1 belongs in the reading surface. It also delivers a caution MERIDIAN should heed — an in
  REC[VISUAL-CONSTITUTION and DESIGN-TOKENS; e] Adopt Candidate C+ as the specification: 'three-signal redundancy with a crop-invariant carrier'. Every model-narrated block renders with (1) a 3px solid left gutter rule running the FULL height of the block, offset 12px from the text; (2) a distinct type substrate — narration set in the secondary/humanist family at 15px/1.6, engine fact in the primary UI family at 14px/1.45 — so the letterforms t
  REC[VISUAL-QA-CHECKLIST; the narration block] Make the gutter rule the primary carrier BECAUSE it is the only crop-invariant element. Specify: `border-left: 3px solid` for narration; engine-fact blocks get `border-left: 3px solid transparent` (identical box model, so no layout shift on reclassification) plus their own confidence chip.
  REC[DESIGN-TOKENS; the forced-colors media q] Distinguish narration from fact by border STYLE, not border colour: narration = 3px solid; a second class, 'engine-computed but assumption-bearing', = 3px dashed (dash 4px, gap 3px). Never encode the fact/narration distinction in hue alone. Verify at forced-colors: the two must remain distinguishable when both are painted in the same system colour.
  REC[Copy standards in INTERACTION-PATTERNS; ] Word the stamp to name the ROLE, never the technology. Use 'Narration — describes engine result #A7F2' rather than 'AI-generated', 'Made with AI' or a bare sparkle. For the composition-proposal path use 'Proposed interpretation — not yet validated by the engine'. Ban the strings 'AI-generated', 'Made with AI', 'AI-powered' and 'Generated by AI' from provenance stamps via a lint rule.
  REC[The dossier projection layer (D-1/D-3 bo] Mark BOTH classes positively; make 'unmarked' an impossible state. Every text region must resolve to exactly one of: engine fact (carries one of the eight confidence labels), narration (gutter rule + stamp), or player input (its own treatment). Add a dev-mode overlay and a CI assertion that fails the build if any rendered text node has no provenance class.
  REC[SCREEN-SPECIFICATIONS; the provenance pa] Adopt a strict four-layer disclosure ladder for generation metadata; forbid anything above L1 in the reading surface. L1 = gutter rule + stamp line + 8-char record hash (always visible, static pixels). L2 = press/hover toggletip: model family, prompt template version, sampling parameters, seed, and the sentence 'This text describes engine state. It did not compute it.' L3 = a Provenance panel list
  REC[Narration record schema; the stamp compo] Put an 8-character resolvable record hash in every L1 stamp — e.g. 'Narration · nar_7f3a91c4 · 4 engine records'. A screenshot of a single block must be resolvable back to the replay record by a human typing that hash into the run's provenance lookup.
  REC[MOTION-SYSTEM (add as a hard prohibition] Never rely on hover, animation or motion to convey provenance. Prohibit shimmer sweeps, gradient animations and 'AI glow' entirely, at any disclosure level. All L1 information must be legible in a static, motionless, monochrome rendering.
  REC[DESIGN-TOKENS; VISUAL-QA-CHECKLIST as an] Specify exact numbers: stamp text 12px / 0.75rem minimum, sentence case, 0.02em tracking, contrast ≥4.5:1 against its background in BOTH light and dark themes. Gutter rule contrast ≥3:1 against the page background, unrounded. Glyph, if used, sized 16px for 12px type per Carbon's size-matching rule. All values validated at both themes as a release gate.
  REC[The narration block component's markup c] Use the GOV.UK warning-text markup pattern for the stamp: decorative glyph with aria-hidden="true", meaning carried by a visually-hidden span, wrapped in a semantic container. Emit the narration block as a <section aria-labelledby> or <blockquote cite> so the fact/narration relationship is programmatically determinable, not merely visual. Add a lang attribute per SC 3.1.2 if narration is ever loca
  REC[INTERACTION-PATTERNS; component API (the] Cap label density: exactly one stamp per contiguous narration region, never per paragraph. The gutter rule provides continuity across paragraphs within a region. Write an explicit 'when NOT to apply the provenance marker' list mirroring Carbon's: not decorative, not on engine-computed text, not as a trigger for regeneration actions, not on player-authored input.
  REC[DESIGN-TOKENS semantic colour scale] If any colour is used in the provenance system, put it on a blue-versus-amber axis, never red-versus-green. Reserve red exclusively for engine-computed danger states, never for 'this is AI'.
  PITFALLS: Using a gradient, glow, shadow or tinted background as the primary AI marker. All are forced to none or overridden in forced-colors mode (MDN), so the distinction silently vanishes for high-contrast users — invisible in normal QA because designers never open the build in high-contrast mode.; Marking only the AI text. The implied truth effect means untagged content gains perceived accuracy, so any narration escaping tagging is promoted to fact rather than degrading to 'unknown'. Mark both classes and make unmarked a build failure.; Encoding the distinction in border COLOUR. Forced-colors overrides border-color to a single system colour, collapsing a blue-vs-grey rule into one indistinguishabl
## Progressive disclosure and information density in expert interfaces — research synthesis and a three-layer disclosure specification for MERIDIAN
SUMMARY: The literature does not support a simple "hide complexity" doctrine. Nielsen's progressive disclosure (2006) is an assertion of benefit with no quantitative backing in the source article; Shneiderman explicitly frames "overview first, zoom and filter, then details-on-demand" as "only a starting point" and defines details-on-demand as applying only *after* a collection has been trimmed to a few dozen items — which is not MERIDIAN's situation. The real constraint on expert interfaces is not chunk-count (Nielsen himself says 7±2 is misapplied to menu length, because interfaces trade on recognition not recall) but attention allocation under time pressure. The hardest empirical numbers available come from process-control alarm management: HSE's Texaco Milford Haven sheet records two operators f
  FIND(well-established (that the claim is unbacked in that source); the underlying benefit is contested/plausible) Nielsen's canonical progressive-disclosure article contains no quantitative evidence for its central benefit claim. | WHY: MERIDIAN's documented defect is claiming properties that do not exist. If the project adopts progressive disclosure as doctrine citing 'improves 3 of usability's 5 components', it inherits an unbacked claim. Treat it as 
  FIND(well-established) Shneiderman explicitly limits his own mantra, and defines details-on-demand as a post-filtering step — not a general licence to hide. | WHY: MERIDIAN's dossier is a single entity with ~10 tabs and four epistemic views, not a collection of thousands. 'Overview first' does not straightforwardly apply. Applying it naively would push the entity's *authoritative* 
  FIND(well-established (as reported); the treemap figure is Shneiderman's own summary, not a cited controlled study) The same paper records concrete failure limits for density-management techniques: fisheye zoom factors capped around 5, overview+detail around 3–30, and complex treemaps requiring 10–20 minutes of user accommodation. | WHY: Any focus+context technique MERIDIAN considers for the world map or relationship graph carries a measurable training cost. A 10–20 minute accommodation period is acceptable for a returning operator and fatal for a first-
  FIND(well-established) Process-control alarm management supplies the only hard numbers on how much an expert can act on under time pressure, and they are brutally low. | WHY: This is directly transferable to MERIDIAN's bottom event stream and to how many consequences a tick may surface as demanding attention. It converts 'don't overwhelm the player' into a testable budget.
  FIND(well-established) HSE explicitly forbids dressing up status as alert — 'process status indicators should not be designated as alarms'. | WHY: MERIDIAN's engine will produce large volumes of state change per tick (indicator drift, cohort updates, relationship decay). Almost none of it requires player action. Promoting it into the event stream is exactly the fai
  FIND(well-established) Hover-only disclosure is an accessibility conformance failure unless three specific conditions are met, and tooltips must never carry task-critical content. | WHY: This is the sharpest available rule for MERIDIAN's 'never behind a hover' requirement. It turns a design preference into a pass/fail test, and it applies to confidence labels and provenance tags — which are load-bearing 
  FIND(well-established) Colour alone may not encode any distinction, and a lightness difference of 3:1 or greater counts as an additional visual distinction. | WHY: MERIDIAN has eight confidence labels plus a separate visibility axis. Eight hues are already beyond safe discriminability for colour-vision-deficient users; the spec must carry glyph and text redundancy. The 3:1 lightnes
  FIND(well-established) Hidden content is measurably under-consumed, and users avoid expanders because they fear navigation. | WHY: MERIDIAN's charter says 'Absence of knowledge is content'. If Unknown/Restricted rows are collapsed, the honesty guarantee is nominally satisfied and practically void. The GOV.UK finding is the strongest concrete stateme
  FIND(well-established (the recognition/recall distinction); the capacity figure itself is contested (7 vs 4)) Density is not limited by working-memory chunk capacity, because interfaces trade on recognition rather than recall — Nielsen says so explicitly against his own most-cited number. | WHY: This is the central permission slip for MERIDIAN's dense command shell. The common objection ('7±2, so keep it sparse') is a misapplication, and MERIDIAN's design review should be able to refuse it by citation.
  FIND(contested) The minimalism-vs-embellishment argument is genuinely unsettled, and 'chartjunk' lacks consensus definition among practitioners. | WHY: MERIDIAN must not adopt Tufte's data-ink maximisation as a constitution-level rule and then justify stripping affordances by citing it. State the tension openly in VISUAL-CONSTITUTION rather than picking a side silently.
  REC[VISUAL-CONSTITUTION, SCREEN-SPECIFICATIO] Adopt a named three-layer model in VISUAL-CONSTITUTION with these exact boundaries. L1 STANDING ANSWER: visible with zero interaction on first paint, no scroll required for the top strip, renders from already-fetched data, no network call. L2 DELIBERATE EXPANSION: reachable in exactly one activation (click, Enter/Space, or tab switch), must be addressable by URL, must survive reload, must render i
  REC[Person dossier, SCREEN-SPECIFICATIONS] Fix the L1 contents of the person dossier as exactly six blocks and forbid additions without a documented removal: (1) identity header with entity type and fidelity tier; (2) OBSERVER BANNER naming whose view this is — 'Player intelligence profile · as assessed by [org] · last updated tick N'; (3) FICTIONAL WORLD badge; (4) a 'why this matters now' strip of at most 5 rows, each row = one sentence 
  REC[VISUAL-CONSTITUTION, INTERACTION-PATTERN] Write an explicit NEVER-BEHIND-HOVER list into the constitution, enumerated, not paraphrased: (a) any confidence label; (b) any provenance tag distinguishing AI-generated text from engine-computed fact; (c) the fictional-world disclosure; (d) any Unknown or Restricted row; (e) the cost, duration, and irreversibility of any COMMAND-mode action; (f) any legality or validation failure reason; (g) the
  REC[DESIGN-TOKENS, the eight confidence labe] Specify the confidence chip concretely: a text chip carrying the full word (Confirmed / Reported / Assessed / Disputed / Unknown / Possibly deceptive / Outdated / Restricted — never an abbreviation, never a bare dot), a distinct glyph per label, a distinct border treatment per label (solid / dashed / dotted / double), minimum 11px type, 4.5:1 text contrast against chip background, 3:1 contrast bet
  REC[Dossier tables, four-views model, confid] Render VISIBILITY and CONFIDENCE as two structurally separate columns in every dossier table, never merged into a single chip. Column 1 = visibility (Visible / Restricted), column 2 = confidence (the other seven, or n/a when Restricted). A Restricted row shows the attribute NAME, the visibility chip, and an empty-but-present value cell reading 'Not available to this observer' — never a blank, neve
  REC[Bottom event stream, tick resolution, en] Budget the bottom event stream against the EEMUA/HSE figures, adapted: at most 10 items per tick may be typed ACTION REQUIRED; use exactly three priority levels; target a distribution near 5% high / 15% medium / 80% low across a run; and enforce HSE's rule that state changes requiring no player decision are STATUS, not events — status goes to an indicator or a separate 'World changes' log, never t
  REC[Outcome/consequence rendering, Eight Que] Make the Eight Questions an L1 fixture with L2 depth, not an L2 feature: for any outcome the player opens, render all eight question labels as always-present rows with a one-line engine-computed answer or an explicit 'Not computed by this rule' — then put the evidence chain, rule text, alternative-outcome distribution and actor-reaction detail behind per-row L2 expansion. The eight rows are never 
  REC[Dossier filtering, world-map layer contr] Route all filtering (tier, entity type, confidence threshold, time window) through client-side operations over an already-materialised projection, with a hard sub-100ms update target and no server round trip per control change. If a filter cannot meet 100ms, it becomes an explicit 'Apply' button with a spinner — never a silently slow live filter.
  REC[Multi-layer strategy, user settings, INT] Make disclosure state player-controlled and persistent, never system-adaptive. Provide an explicit density/detail control with three named settings (e.g. Brief / Standard / Full) that the player sets and that persists per account; never auto-promote UI complexity based on inferred skill, playtime, or the entity fidelity-tier promotion mechanic. Expansion state of named L2 sections persists across 
  REC[World map, relationship graph, MOTION-SY] Do not use distortion-based focus+context (fisheye, hyperbolic, perspective-wall style) as the primary world-map or relationship-graph interaction in v1. Use overview+detail with a linked, non-distorting detail panel instead. If focus+context is prototyped later, it must ship behind the player's own detail control and be measured against a time-to-first-correct-answer task.
  REC[L3 forensic surface, ADR-006 determinism] Structure L3 as a genuinely separate route ('/run/{id}/trace/{event_id}') exposing: the full causal parent chain, every consequence primitive with its priced inputs, the rule identifier that fired, the named RNG substream and draw index, the assumption set, and a byte-level replay diff. It may take up to 10s with a progress indicator and may be export-first (JSON/CSV) before it is ever rendered as
  REC[Dossier tabs, INTERACTION-PATTERNS] Ban the collapsed-by-default pattern for any content the majority of players need, and cap L2 expanders per screen at 12 with no nesting deeper than one level. Where more than 12 groupings exist, use tabs (which advertise their own existence) rather than accordions (which do not). Never nest an L3 link inside a collapsed L2 section.
  PITFALLS: Citing Nielsen's 'improves 3 of usability's 5 components' as if it were a measured result. The article contains no study, no sample, no effect size. Repeating it as evidence would reproduce exactly the defect MERIDIAN is trying to eliminate.; Applying 'overview first' to a single-entity dossier. Shneiderman's details-on-demand explicitly assumes 'a collection has been trimmed to a few dozen items'. A dossier is one item with four epistemic views; an 'overview' of it is itself an inference product and would silently become a fifth, unmodelled view.; Collapsing Unknown and Restricted rows to save space. This nominally satisfies 'absence of knowledge is content' while voiding it in practice — G
## Causal explanation interfaces for MERIDIAN — "why did this change?", walking the chain backwards, and rendering alternatives that did not happen
SUMMARY: Note on method: this session's WebSearch budget was exhausted before I ran a single search, so all evidence below comes from direct WebFetch against named primary sources (arXiv abstracts/PDFs, W3C Recommendations, vendor docs, NN/g, practitioner writing). Four fetches failed (Fandom 402, RimWorld wiki 403, two Microsoft/Paradox 404s), so game-UI evidence is thinner than the rest and is labelled accordingly. Nothing here is cited that I did not fetch.

The synthesis: MERIDIAN's Eight Questions are, whether or not the project realises it, a close cousin of the question-driven XAI framing (Liao et al., CHI 2020) — and the two questions every real system drops are exactly MERIDIAN's Q6 (uncertainty) and Q7 (alternative outcomes). The single most transferable result is Miller's: human explanat
  FIND(well-established) Human explanation is contrastive, selective, causal rather than statistical, and social — Miller's four findings are the backbone result for any 'why did this change?' surface. | WHY: MERIDIAN's instinct will be to lead with a complete itemised modifier ledger. Miller's selectivity finding says that is the wrong layer-one artefact: people ask 'why P rather than Q?' and accept one or two causes. The le
  FIND(well-established) Explanations increase acceptance of AI output without increasing team accuracy — they raise trust indiscriminately. | WHY: This is the finding most damaging to a naive MERIDIAN trace panel. If the causal trace makes players more likely to accept engine/LLM output whether or not it is sound, MERIDIAN becomes a confidence machine, which for tr
  FIND(well-established) More transparency can make people WORSE at catching large model errors, via information overload. | WHY: Directly contradicts the assumption that MERIDIAN's causal depth is automatically a virtue. It argues for hard caps on layer-one density and for testing rather than reasoning about the trace panel.
  FIND(well-established) Practitioners over-trust and misuse interpretability tools, and most cannot correctly describe what the visualisations mean. | WHY: MERIDIAN's audience is not ML researchers. If trained data scientists misread SHAP plots, MERIDIAN players will misread contribution charts. Every novel visual encoding in the trace surface needs an in-place legend and a
  FIND(well-established) Counterfactual explanation is a legitimate, standalone explanation form that does not require exposing internal mechanism, and serves three distinct user goals. | WHY: Gives MERIDIAN licence to answer Q7 (alternative outcomes) with recomputed counterfactuals rather than by dumping the whole model. The three goals map cleanly onto MERIDIAN's charter: understand, contest, and act differe
  FIND(well-established) User explainability needs are best modelled as a bank of prototypical QUESTIONS, and there is a documented gap between what algorithmic XAI produces and what users ask. | WHY: Validates MERIDIAN's Eight-Questions framing as a design instrument rather than a documentation checklist — and warns that a question-shaped spec does not automatically produce question-shaped UI.
  FIND(well-established) Major vendor guidance explicitly pairs 'explain why' with an over-reliance warning, and decomposes explanation into seven named patterns including a 'What if?' pattern. | WHY: MERIDIAN can adopt a ready-made taxonomy of explanation types rather than inventing one, and can cite a vendor precedent for shipping the caveat alongside the explanation.
  FIND(well-established) Google PAIR names counterfactuals — 'explaining what didn't happen and why' — as a first-class explanation type, and flags numeric probability display as risky. | WHY: Directly relevant to MERIDIAN's requirement to show alternative outcomes, and to the eight confidence labels: PAIR's categorical-band approach ('Best match / Good match / Unsure') is the pattern MERIDIAN's labels already
  FIND(well-established) W3C PROV gives a standard three-type provenance vocabulary (Entity / Activity / Agent) with fixed relations and fixed diagram shapes. | WHY: This is the single highest-leverage import for MERIDIAN. It turns ADR-006's determinism boundary from prose into a checkable graph invariant, and supplies unbranded diagram conventions for free.
  FIND(well-established) Influence diagrams already have a published, learnable node/arc grammar that distinguishes decisions, uncertainties, deterministic nodes and values, and distinguishes 'what you'll know' from 'what depends on what'. | WHY: MERIDIAN's PLAN/COMMAND modes need to show a player 'what may stop this, who resists, what it costs.' The informational-arc semantics ('the decision is made with the outcome of all nodes at their tails known beforehand')
  REC[Right context panel 'Why?' header; event] Layer one of every 'why did this change?' surface is a single engine-generated CONTRASTIVE sentence, hard-capped at one sentence and at most two named causes, using the fixed template: '<what changed, with magnitude> rather than <foil>, because <cause 1> and <cause 2>.' The foil is not authored — define it as the engine's already-computed no-action baseline projection for that indicator at that ti
  REC[Right context panel, expanded state; any] Layer two is a reconciling LEDGER, not a chart. Every contributing term renders as a row: name, signed value, unit, mechanism id, and a provenance origin badge. The ledger MUST sum exactly to the displayed delta. Terms below a display threshold collapse into a single row reading 'N smaller contributions, totalling ±X' — never silently dropped. Any arithmetic gap renders as an explicit 'UNEXPLAINED
  REC[The 'Why?' panel core layout; the full-s] Make the Eight Questions a FIXED eight-row scaffold, always rendering all eight rows, never a prose paragraph. Any question the engine cannot answer renders as a present row carrying a machine-set reason code — NOT_MODELLED / INSUFFICIENT_EVIDENCE / RESTRICTED_TO_THIS_OBSERVER / DEFERRED_TO_TICK_N. A build-time test asserts that no shipped outcome renders an empty row without a reason code.
  REC[Q7 row of the Eight-Questions scaffold; ] Split 'alternative outcomes' into two visually and semantically distinct kinds and never merge them. (a) NOT SELECTED — branches in the resolved distribution the RNG did not draw. Render as a single horizontal outcome-space band, realised segment filled and labelled, unrealised segments ghosted with p values, sorted by probability descending, capped at five with an 'other outcomes, p=X' remainder.
  REC[Engine RNG layer (blocking); Q7 renderin] A counterfactual re-run MUST consume the identical RNG substream draws as the actual run, so the perturbed input is the ONLY difference. This requires named, seeded, per-mechanism RNG substreams, which MERIDIAN does not have — make substreams a hard prerequisite for shipping any counterfactual UI, and until they exist, ship the Q7 row in NOT_MODELLED state rather than shipping a noisy counterfactu
  REC[Q5 row; PLAN mode pre-commit review; exp] Give assumptions a first-class typed record — assumption_id, plain-language statement, value_used, unit, source enum (SCENARIO_CONSTANT / CALIBRATED / ENGINE_DEFAULT / PLAYER_SUPPLIED / MODEL_PROPOSED), and a computed sensitivity. Render them RANKED BY SENSITIVITY, never alphabetically, and flag the top contributor with a 'this assumption is doing the work' marker. Any assumption with source MODEL
  REC[Trace data model; ADR-006 conformance te] Adopt W3C PROV Entity/Activity/Agent as the literal data model of the trace, and enforce one invariant in CI: every authoritative numeric Entity must be wasGeneratedBy an ENGINE Activity, and no authoritative Entity may be wasGeneratedBy an Activity whose associated Agent is a language model. An LLM turn is an Activity that generates only PROPOSAL entities, which an engine Activity `used`.
  REC[Full-screen Trace view (a separate route] Render the trace graph as a LAYERED (Sugiyama-style) DAG with a fixed spatial grammar: x = tick (time, left to right), y-band = tier (macro / meso / micro as three horizontal lanes), all edges pointing one direction, long edges routed through dummy vertices. Bound it in the DATA layer, not the layout layer: default to the k highest-contribution ancestors (k=7 proposed) plus one aggregate node read
  REC[Command shell navigation model; right co] Resolve the three-layer tension by making layer 3 a NAVIGABLE DESTINATION rather than a third nested disclosure: layers 1 and 2 are in-place progressive disclosure inside the right context panel; layer 3 is the full-screen Trace view with its own route, its own back affordance, and a breadcrumb naming the change record it was opened from.
  REC[Trace view toolbar; EXPLORE mode; after-] Build 'when did this become inevitable?' as a bisect: the player writes or picks a predicate over state (e.g. 'famine_index > 0.4'), the engine binary-searches the tick range from checkpoints, and returns two facts — the first tick at which the predicate held, and the last tick at which a single-decision counterfactual could still have prevented it. Borrow git's untestable escape hatch: ticks wher
  REC[Global status bar; command shell chrome;] Implement time travel as RECOMPUTATION from an immutable, append-only log of committed structured inputs, never as snapshot restore. UI: a tick scrubber; selecting a past tick renders a read-only projection. Signal the read-only state with a persistent full-shell treatment — a continuous border and a status-bar band reading 'VIEWING TICK 41 OF 58 · READ ONLY' — not a small corner badge. Every comm
  REC[Trace view layout; narration rendering; ] Every node in the trace carries an ORIGIN badge — ENGINE / INPUT / PROPOSED / NARRATED — and NARRATED nodes are rendered OUTSIDE the graph in a side rail, structurally incapable of appearing on a causal path. Enforce with a test: assert that no path in any rendered trace traverses a node of origin NARRATED.
  PITFALLS: Assuming explanation depth is self-evidently good. The three strongest empirical results here (Bansal, Poursabzi-Sangdeh, Kaur) all point the other way: explanations raise acceptance without raising accuracy, transparency reduced detection of large errors via overload, and trained practitioners over-trust and misread interpretability visualisations. MERIDIAN's causal surface must be evaluated, not assumed.; Shipping counterfactuals before named RNG substreams exist. Without draw-identical re-runs, 'if X had been different, Y would have been' is partly resampling noise presented as causation — the exact class of unfounded claim the project identifies as its defining defect.; Building time tra
## Alerts, attention and time pressure: an evidence base and concrete alert model for MERIDIAN's decision queue, event stream and priority taxonomy
SUMMARY: RESEARCH CONDITIONS — STATED UP FRONT FOR HONESTY. This session's WebSearch budget was exhausted before I issued a single query, so all sourcing was done by directly fetching known URLs. Nine sources were fetched and read successfully, including three full primary documents (HSE CHIS6, Mark/Gudith/Klocke CHI 2008, Iqbal & Horvitz CHI 2007) read end-to-end as PDFs. Five targeted fetches FAILED (HTTP 403): the Joint Commission Sentinel Event Alert 50, the USENIX Security 2022 SOC-analyst study ("99% False Positives"), FAA AC 25.1322-1 on flightcrew alerting, 14 CFR 25.1322 via eCFR, and SKYbrary. Consequently: the aviation strand of this brief is UNDER-EVIDENCED and I have not asserted the standard aviation figures (TCAS/EGPWS nuisance rates, the widely-cited ~70% automation-reliability thre
  FIND(well-established) EEMUA 191 sets a long-term average alarm rate target of no more than one alarm per ten minutes during normal operation, and no more than ten alarms displayed in the first ten minutes following a major plant upset. | WHY: These are the only widely-adopted hard numbers for how much alerting a human operator can absorb. They give MERIDIAN a defensible starting budget for its decision queue and event stream rather than an arbitrary designer 
  FIND(well-established) EEMUA 191 recommends about three priority levels, based on the potential consequence if the operator fails to respond, distributed approximately 5% high / 15% medium / 80% low. | WHY: Directly supplies MERIDIAN's priority taxonomy shape and — more importantly — a testable distribution invariant. A priority scheme with no enforced distribution degrades into everything-is-important, which is the observe
  FIND(well-established) The defining alarm-flood accident is quantified: in the last 11 minutes before the 1994 Texaco Milford Haven explosion, two operators had to recognise, acknowledge and act on 275 alarms. HSE's investigation named 'too many alarms, poorly prioritised' and control-room displays tha | WHY: It is the concrete, citable proof that alert volume is a design defect with consequences, not a taste question. For MERIDIAN it also names the correct failure: not that alarms were wrong, but that they were undifferentia
  FIND(well-established) EEMUA/HSE state that process status indicators should NOT be designated as alarms, and that alarms with no defined operator response should be eliminated or reviewed. | WHY: This is the single most transferable rule in the whole corpus and it maps cleanly onto MERIDIAN's queue/feed split: if the engine cannot enumerate at least one legal action the player could take, the item is a status cha
  FIND(well-established) Interrupted work was completed FASTER than uninterrupted work, with no difference in quality, but at significantly higher stress, frustration, time pressure, effort and mental workload. | WHY: This inverts the usual design justification. It means MERIDIAN cannot validate its alert model with throughput or completion metrics — those will look fine or even improve while the experience degrades. Subjective-load m
  FIND(contested) Whether the CONTENT of an interruption relates to the current task makes no difference to its disruption cost — any discontinuity carries the cost. | WHY: Kills a tempting MERIDIAN design shortcut: 'we'll only interrupt with things relevant to what the player is currently doing.' Relevance-matching will not buy back the interruption cost. Only reducing count and giving the
  FIND(well-established) After an alert, users spent roughly 9–10 minutes on the diversion and a further 11–17 minutes in the resumption phase before regaining their prior task state; 27% of alert-driven suspensions resulted in more than two hours before resumption. | WHY: Quantifies the true cost of a single 'small' notification as far larger than the notification itself. For MERIDIAN this argues for a strict budget on anything that steals focus, and for explicit resumption support after 
  FIND(well-established) Suspended windows that were less than 25% visible took significantly longer to return to than windows more than 75% visible; visibility of the prior context acts as a resumption cue. | WHY: This is a direct, empirically-grounded layout constraint. It says: do not handle a high-priority alert with a modal that covers the operational view. Use a side panel or an overlay that leaves the prior context visible.
  FIND(well-established) Users perform state-stabilising actions before responding to an alert, at rates an order of magnitude above baseline, when allowed to choose the moment. | WHY: Evidence that deferrable alerts are cheaper than immediate ones because people self-manage the switch. Supports giving MERIDIAN players an explicit 'handle now / handle at next tick boundary' control rather than forcing 
  FIND(well-established) In a 31-day ICU study, 2,558,760 alarms occurred across 77 beds (187 audible alarms per bed per day), and 88.8% of the 12,671 annotated arrhythmia alarms were false positives. | WHY: The clearest available quantification of what happens when alert precision is not enforced. Individual categories were worse: accelerated ventricular rhythm 94.8% false, ventricular tachycardia 87.2% false. This is the o
  REC[Bottom event stream; command shell layou] Split the bottom bar into two structurally different objects that never share a list: a DECISION QUEUE and an EVENT STREAM. The queue is a fixed-height left segment of the bottom region showing at most 3 items plus an overflow count; each queue item is a record with required fields {alert_class_id, subject_entity_id, mechanism_id, deadline_tick_or_null, deadline_kind ∈ {engine_enforced, actor_expe
  REC[Decision queue; consequence-primitive va] Gate entry to the decision queue on a machine-checkable affordance test: an item may enter the queue only if the engine can enumerate at least one legal action composition (over the ~16 consequence primitives) that the player could execute in response, with that composition passing validation and pricing. If zero affordances resolve, the item is downgraded to a stream event automatically. Implemen
  REC[Priority taxonomy; CI test suite; refere] Adopt exactly three priorities — P1 IMMEDIATE, P2 SCHEDULED, P3 RECORD — defined by consequence-of-non-response, not by drama. P1: irreversible loss of a currently-available option, or an engine-enforced deadline expiring within 1 tick. P2: a consequence that is reversible or deferrable at a priced cost. P3: no player decision exists; informational only, and P3 items live in the stream, never the 
  REC[Alert scheduler; speed control; telemetr] Rate-limit against the PLAYER'S WALL CLOCK, not simulated time. Budget: at most 1 P1 per 10 minutes of real play as a long-run average, and at most 10 queue items surfaced in the first 10 real minutes following a declared crisis escalation. Instrument this: ship a dev-build HUD counter showing P1-per-real-hour and queue-items-per-real-10-minutes, and log both to the run record. When the budget is 
  REC[Alert scheduler; macro-indicator layer; ] Make alerts STATE-BASED, not edge-based. Every alert instance carries a stable dedup key = hash(alert_class_id, subject_entity_id, mechanism_id, threshold_id). An alert has an explicit lifecycle {opening, open, acknowledged, shelved, closing, closed} and produces exactly ONE row that updates in place, showing 'open since tick N' and an occurrence count, plus a sparkline of the underlying quantity.
  REC[Alert scheduler; macro-indicator layer; ] Require dual-window confirmation before promotion to the queue. A condition must hold in BOTH a short window (current tick) and a long window (N consecutive ticks, N configured per AlertClass, minimum 2) before it surfaces as P1 or P2. Where the underlying quantity is a rate rather than a level, alert on the rate of consumption of a bounded budget rather than on the level itself, with the window/t
  REC[AlertClass registry; ADR-006 determinism] Priority is an immutable property of a registered AlertClass, never a per-instance field, and the LLM cannot write it. Define an AlertClass registry checked into the repo with fields {id, priority, channel, may_auto_pause: bool, affordance_resolver, dedup_key_template, default_outcome_if_ignored}. Instances reference a class id and may not override priority, channel or may_auto_pause. Any model-pr
  REC[Alert projection layer; RNG substream wo] Alert computation is a PURE PROJECTION. Extend dossier properties D-1 and D-2 verbatim: computing the alert set mutates nothing and consumes ZERO randomness. Add a property test — open, close and recompute the queue and stream an arbitrary number of times at a fixed tick and assert the serialised simulation state is byte-identical and the RNG substream cursor has not advanced. Any narrative render
  REC[Stream and queue row components; provena] Every queue and stream row carries three chips that are legible in greyscale and never rely on colour: (1) PROVENANCE — engine-computed vs model-generated, rendered as text plus a distinct glyph plus a distinct border treatment (engine = solid 1px left rule, model = dashed 1px left rule), minimum 11px, minimum 4.5:1 contrast against its own background; (2) one of the eight CONFIDENCE labels; (3) t
  REC[Event stream; eight confidence labels; v] Render absence of knowledge as a present row in the stream. When something material occurred but the player's intelligence profile does not permit visibility, emit a row on the VISIBILITY axis reading as a Restricted item — subject, mechanism class, and the fact that detail is withheld — rather than silently omitting the event. Separately, when the player's information is stale or contested, the r
  REC[Global status bar; speed control; versio] Auto-pause the clock on the first unacknowledged P1 of any tick, and never advance simulated time while a P1 is unacknowledged. Expose speed as 0/1/2/3 in the global status bar together with the REASON the current speed was set, in plain language: 'Paused automatically — 1 decision requires approval'. Auto-pause events are recorded as versioned external inputs in the run record with their trigger,
  REC[Right context panel; command shell layou] Handle P1s in the right context panel, never in a centre-screen modal that occludes the operational view. On resolving or deferring a P1, restore the exact prior view state — centre view, camera, selection, scroll position, open tab — in one action, and offer a 'what I was doing' strip showing the player's last 3 actions with their timestamps and sim ticks.
  PITFALLS: BUILDING ALERTS ON TOP OF THE SATURATION BUG. MERIDIAN's macro indicators rise monotonically because there is no cost, cooldown, decay, budget, prerequisite or revert mechanism. Any edge-triggered threshold alert will therefore fire every tick, on every indicator, forever, from the first build. Specify the alert model now; do not ship it until decay/cooldown exist and state-based alerting with stable dedup keys is in place.; BUILDING THE QUEUE BEFORE EVENT-SOURCED HISTORY EXISTS. causal_parents are currently discarded and nothing is persisted to the DB. Without stable event identity you cannot dedupe, cannot group children under a cause, cannot resume a deferred item, and cannot replay the q
## Relationship graph visualisation at scale — node-link vs matrix, hairball mitigation, directed/asymmetric edge encoding, temporal edges, restricted-edge affordances, and layout stability — applied to MERIDIAN's entity Relationships tab
SUMMARY: The empirical literature is unusually decisive on the questions MERIDIAN actually faces, and decisive in a way that contradicts the instinct to build "a network graph". Ghoniem/Fekete/Castagliola's controlled experiment (36 subjects, 7 tasks, graphs of 20/50/100 vertices at densities 0.2/0.4/0.6) found matrices beat node-link diagrams on six of seven tasks once graphs exceed ~20 vertices — but node-link won path-finding decisively on small sparse graphs (99% vs 70% correct at 20 nodes). Since MERIDIAN's Relationships tab is an ego-network (one entity plus its alters, almost always under 20 nodes) and its highest-value task is causal-chain tracing ("who did A influence, through whom"), the evidence points to node-link as the default and matrix as an explicitly-offered second representation 
  FIND(well-established) Matrices outperform node-link diagrams on most graph-reading tasks above roughly 20 vertices, but node-link wins path-finding — the crossover is task-dependent, not purely size-dependent. | WHY: MERIDIAN's Relationships tab is an ego-network of typically 5–20 alters, and its signature task is tracing influence chains. Both facts point at node-link as the default, contradicting the common assumption that 'serious
  FIND(well-established) The node-link advantage at path-finding reverses at high density: matrices scored 95% correct vs node-link's 85% at density 0.6, while node-link scored 99% vs matrix's 70% on 20-node graphs. | WHY: MERIDIAN cannot pick one representation for all relationship views. A focal person's ego-net (sparse, small) and a state's internal faction web (potentially dense) need different defaults, and the switch condition is mea
  FIND(well-established) Both papers' authors explicitly say path tasks need dedicated interaction rather than a better static layout. | WHY: MERIDIAN should not spend effort tuning layout aesthetics for chain-tracing. It should build a path-highlight interaction, which is cheap and evidence-backed.
  FIND(well-established) Tapered edges (wide at source, narrow at target) are the best-performing static encoding of edge direction; biased curvature is consistently the worst; glyph/texture encodings are explicitly not recommended. | WHY: MERIDIAN needs A→B legible independently of B→A. This gives a specific, tested encoding rather than defaulting to arrowheads, which the same authors identified as causing arrowhead-overlap clutter.
  FIND(well-established) There is a measured speed/accuracy tradeoff between tapered (faster) and animated-compressed (more accurate), and animation of edges carries a general time cost. | WHY: MERIDIAN's charter values explanation over speed, but a perpetually animated relationship graph is a background-attention tax on a screen the player keeps open. The tradeoff should be a deliberate decision, not a default
  FIND(well-established) Small multiples beat interactive animation for dynamic graph comprehension on every task tested, but animation retains two specific advantages: highlighting, and disambiguating identity across large spatial reorientations. | WHY: MERIDIAN's relationship history should default to juxtaposed static states (time encoded spatially), with animation reserved for short transitions — matching its own doctrine that motion should explain state, not decorat
  FIND(contested) The value of layout stability ('mental map preservation') is genuinely contested in the literature — a survey of pre-2012 results found no evidence it helps, while two later studies found conditional benefit. | WHY: MERIDIAN should adopt stable layout for determinism-doctrine and trust reasons, and should not claim in documentation that stability is empirically necessary. Overclaiming here would reproduce the project's named defect.
  FIND(contested) Where stability does help, it helps specifically for 'map-like' tasks — locating specific nodes and following long paths — and only when the task involves five or more pertinent nodes not otherwise highlighted. | WHY: This is exactly MERIDIAN's relationship-tracing task profile. It gives a defensible, bounded justification for stable layout rather than a blanket one.
  FIND(emerging) For missing/absent data, highlighting and annotating produce higher perceived data quality and more accurate interpretation than downplaying or removal; removal can cause outright incorrect responses when it breaks visual continuity. | WHY: This is the strongest available empirical basis for MERIDIAN's 'Unknown and Restricted must be present rows' doctrine — and it argues directly against the natural implementation of rendering restricted edges as faded gho
  FIND(emerging) Uncertainty annotation only helps when it preserves visual continuity; annotations that break continuity produced low confidence and low perceived quality. | WHY: A 'restricted edge' rendering that breaks the visual line between two entities (e.g. a gap, or omission with a footnote) is predicted to perform worse than one that draws a continuous but distinctly-styled connector.
  REC[Relationships tab, all entity types (per] Default the Relationships tab to a node-link ego-network centred on the dossier subject, showing only alters at graph distance 1, capped at the 12 highest degree-of-interest alters, with a persistent chip reading 'Showing 12 of 47 known relationships' that opens the full list. Do NOT default to a matrix and do NOT default to a whole-world graph.
  REC[Relationships tab node selection; also r] Compute which alters appear using an explicit three-term degree-of-interest score — DOI(alter) = intrinsic_salience(alter) + query_match(alter, active player search/filters) + relationship_strength_distance(subject, alter) — store it on the projection, and expose it in a 'why is this shown?' popover listing the three component values numerically. Never use an unexplained heuristic.
  REC[Relationships tab expansion interaction] On expand, add at most 5 new nodes per click (top-N by DOI), and mark at most 4 nodes with an 'expandable' affordance carrying a numeric badge of undisplayed neighbours (e.g. '+13'). Repeated clicks add the next 5. Never expand a node's full neighbourhood in one action.
  REC[All directed relationship rendering in n] Encode direction with tapered edges — full stroke weight at the source, tapering to approximately 15% weight at the target — not arrowheads. Reserve arrowheads only for the single currently-selected relationship, where overlap is not a concern.
  REC[Relationships tab node-link rendering wh] Render an asymmetric pair as ONE relationship channel, not two edges. Split the channel lengthwise into two parallel half-ribbons offset by 3–4px: the upper half tapers subject→alter and carries the subject's stance; the lower half tapers alter→subject and carries the alter's stance. When the two stances differ by more than a defined threshold, stroke the channel's midline with a dedicated 'asymme
  REC[Relationships tab list/table view, which] Surface asymmetry as a first-class textual answer, not only a visual one. Every relationship row in the list view renders two lines: 'Subject → Alter: Trusts (Assessed)' and 'Alter → Subject: Distrusts (Reported)', each with its own confidence label. The visual channel is the summary; the two-line row is the authority.
  REC[Restricted visibility axis rendering, ac] Render Restricted edges as continuous, full-opacity connectors with a distinct structural treatment — dashed stroke at the same weight as visible edges, a lock glyph at the channel midpoint, and a text chip reading 'Restricted' — leading to a stub node labelled with what IS known (e.g. 'Undisclosed party — 1 relationship'). Never render restricted or unknown edges as faded, low-opacity, or omitted
  REC[Restricted-edge and unknown-endpoint ren] Never let a restricted or unknown edge break the visual line between two entities. If the far endpoint is unknown, terminate the channel at a visible stub node rather than fading it into empty space or omitting it.
  REC[All eight confidence labels wherever ren] Render each of the eight confidence labels as a text chip with three redundant channels: the literal word, a distinct glyph, and a distinct border treatment (solid / dashed / double / dotted). Minimum 11px, minimum 4.5:1 contrast against chip background. Colour may reinforce but must never be the sole differentiator, and the chip must remain unambiguous in greyscale.
  REC[Relationship history / the Timeline tab'] Default relationship history to small multiples: a horizontal strip of 4–6 static ego-net snapshots at chosen time points, each with a caption stating what changed, positions held identical across all frames. Provide animation ONLY as a short (≤400ms) transition when the player jumps between two non-adjacent time points, and only to disambiguate node identity.
  REC[Relationships tab layout engine; blocks ] Make graph layout fully deterministic: node positions computed from a stable hash of entity ID plus the projection's node set, using a seeded layout whose RNG is a dedicated presentation substream, never the simulation RNG. Freeze positions for a given dossier session; reflow only on explicit player action ('Re-arrange'), and animate that reflow.
  REC[Relationships tab representation switch;] Offer a matrix view as an explicit, labelled second representation — not a toggle buried in settings — and auto-suggest it (never auto-switch) when the current projection exceeds 20 nodes or 0.4 density, via an inline prompt: 'This network is dense. A matrix view may read more clearly.' Include the ordering algorithm's name and a one-line description in the matrix header.
  PITFALLS: Dropping in a standard force-directed graph library. Nearly all are randomly seeded, which silently violates dossier property D-2 ('a dossier must consume ZERO randomness') while appearing to work correctly. This will not surface as a bug; it will surface as a failed replay determinism test much later.; Fading restricted or unknown edges to signal uncertainty. Song & Szafir found downplaying produced lower perceived data quality and worse outcomes than highlighting or annotating. Fading is the visually obvious choice and the empirically wrong one, and it also contradicts MERIDIAN's own rule that 'absence of knowledge is content'.; Rendering asymmetric relationships as two separate arcs betwe
## Situation awareness and the command shell: SA theory, ecological interface design, mode error and workload, applied to MERIDIAN's EXPLORE/PLAN/COMMAND interface
SUMMARY: METHOD CAVEAT FIRST, because this project's defining defect is documentation claiming properties it does not have: this session's WebSearch budget (200/200) was exhausted before my first query, so I could not run the 5-8 searches the assignment specifies. I worked instead by fetching known-stable URLs directly. Nine fetches succeeded (W3C WCAG Understanding documents, Nielsen Norman Group articles, several Wikipedia reference articles); three failed (SKYbrary mode-awareness returned 403, a Distributed Situation Awareness article 404'd, and the Bainbridge 1983 "Ironies of Automation" PDF downloaded but could not be parsed — poppler is not installed, so I have NOT read it and make no claims from it). The W3C and NN/g sources are primary. The Wikipedia sources are secondary summaries of Endsl
  FIND(well-established (the three-level model); the Eight-Questions mapping is my inference) Endsley's SA model has three levels — perception of elements, comprehension of their meaning against goals, and projection of future states — and MERIDIAN's own Eight Questions decompose cleanly onto them. | WHY: It gives MERIDIAN an external, testable structure for its internally-invented Eight Questions, and immediately exposes that the proposed shell has no region dedicated to Level 3 projection.
  FIND(contested — this is the contest itself) SA theory is genuinely contested, not settled — critics charge circularity and unfalsifiability, and the SA-to-performance link is probabilistic rather than deterministic. | WHY: MERIDIAN should adopt SA as a design vocabulary and an evaluation scaffold, but must not claim in documentation that an SA-grounded design 'ensures' good player decisions. That would be exactly the overclaiming defect th
  FIND(well-established) SAGAT works by freezing the simulation and comparing the operator's reported perceptions against ground truth; SART is a subjective self-rating with limited diagnostic detail and overconfidence risk. | WHY: MERIDIAN is unusually well-placed to run SAGAT — it IS a simulation with authoritative state and can freeze, probe and auto-score — but the naive implementation would be wrong (see next finding).
  FIND(emerging — my inference, unverified against the measurement literature) For MERIDIAN specifically, SAGAT ground truth must be the player-intelligence projection (view #4), not authoritative reality (view #1) — otherwise the measurement penalises a correctly-designed interface. | WHY: MERIDIAN's dossier deliberately withholds and degrades information. An interface that correctly renders 'Assessed' and 'Possibly deceptive' would score badly against omniscient ground truth, and the team would 'fix' the 
  FIND(well-established) Rasmussen's abstraction hierarchy has five levels — functional purpose, abstract function, generalised function, physical function, physical form — with abstract function meaning the 'underlying laws and principles that govern the goals of the system'. | WHY: It is the scaffold for the society mapping below, and the definition of 'abstract function' is what makes MERIDIAN's missing conservation mechanisms diagnosable as a structural absence rather than a to-do item.
  FIND(emerging — explicitly my inference) A society's abstraction hierarchy maps as: functional purpose = the state's competing purposes and their trade-offs; abstract function = conservation laws (fiscal balance, sourced legal authority, finite political capital, finite coercive capacity, attention scarcity); generalise | WHY: It aligns with MERIDIAN's existing macro/meso/micro tiers, gives each tier a principled display obligation, and identifies the abstract-function layer as the one MERIDIAN has entirely omitted.
  FIND(emerging — my inference) MERIDIAN's two open criticals are the same defect viewed from two angles, and both are abstraction-hierarchy failures: macro saturation is the absent 'abstract function' level, and tier decoupling is the absent means-ends links between levels. | WHY: This reframes the criticals from 'engine bugs' to 'the interface has nothing true to show', which correctly blocks UI work that would otherwise paper over them with charts.
  FIND(well-established (that EID claims this); the fit to MERIDIAN is my inference) EID's documented benefit is specifically supporting unanticipated events and knowledge-based reasoning, which is precisely MERIDIAN's charter requirement that a player can propose something unexpected. | WHY: It supplies an external design tradition for the charter's hardest constraint — 'any design that collapses back into a menu of pre-authored options has failed'. EID exists to avoid exactly that collapse.
  FIND(anecdotal — flagged unverified, do not cite as established) EID was built for causal physical domains where means-ends links are law-governed; a socio-political domain's links are intentional and defeasible, so an abstraction hierarchy of a society cannot promise the same display guarantees. | WHY: It is the honest boundary on the mapping above. An interface that renders social means-ends links with the visual confidence of a mass-balance display would be lying.
  FIND(well-established) Mode error is a well-documented and fatal failure class: users act appropriately for one state while unknowingly in another, caused by unnoticed state changes, forgetting the current mode, and interruptions. | WHY: MERIDIAN's COMMAND mode mutates authoritative state. A mode slip in MERIDIAN is not a cosmetic error; it is an unintended, possibly irreversible, simulation mutation.
  REC[Command shell layout / right context pan] Give Level 3 projection a permanent, non-collapsible home in the shell. Reserve the top third of the right context panel as a PROJECTION band that is always populated for the currently selected entity or pending action, containing exactly three rows: (a) trajectory — named indicator, direction, and an explicit horizon in simulation ticks, never a bare arrow; (b) contested — the single strongest fo
  REC[Command shell layout / design review pro] Bind each of the Eight Questions to exactly one owning shell region and publish the binding as a layout contract: Q1 (what happened) = bottom event stream; Q2-Q5 (cause, rule/mechanism, reacting actors, assumptions) = right context panel 'Why' stack; Q6-Q8 (uncertainty, alternatives, changed future options) = right context panel projection band; centre operational view owns spatial and relational 
  REC[EXPLORE/PLAN/COMMAND mode architecture] Make COMMAND a non-sticky mode. EXPLORE and PLAN may persist; COMMAND must be entered per-action and must self-revert to PLAN the instant an action resolves, is cancelled, or the input loses focus for more than one interaction. There is no idle state in which the interface sits in COMMAND. Do not implement COMMAND as a toggle, a tab, or a persistent segmented-control segment.
  REC[Mode indication / conversational interfa] Indicate mode redundantly in three places with three different channels, never colour alone: (1) a persistent text chip in the global status bar carrying the literal words EXPLORE / PLAN / COMMAND plus a distinct glyph and a distinct border weight; (2) the input field's own border treatment and placeholder text, which changes per mode; (3) the commit control's verb — 'Ask' in EXPLORE, 'Draft' in P
  REC[Mode indication / intent parsing] Never auto-switch mode on the player's behalf. If the intent parser is confident the player is attempting a mutation while in EXPLORE or PLAN, render an inline offer — 'This looks like an action. Move to COMMAND to attempt it.' — with an explicit control. The parse result may be retained and carried forward; the mode change may not happen without a discrete player act.
  REC[Mode indication / accessibility / replay] Announce every mode transition as a WCAG 4.1.3 status message using role="status" with aria-live="polite" for EXPLORE and PLAN, and role="alert" for entry into COMMAND. The COMMAND announcement must state the mode and its consequence in one sentence — 'Command mode. Actions submitted here will change the simulation.' Log every transition as a versioned external input with timestamp and provenance.
  REC[Evaluation methodology / dossier and fou] Build a SAGAT harness as an internal dev tool, scoring against the PLAYER-KNOWLEDGE projection (view #4), not authoritative reality (view #1). Freeze the run, blank all panels, auto-generate probes from the projection ('Which faction inside Ministry X currently opposes the policy?', 'What is your confidence label on entity Y's stated resources?'), and auto-score. Report two separate numbers: proje
  REC[Evaluation methodology / workload measur] Do not use SART as a primary measure. Use Raw NASA-TLX (six subscales, no pairwise weighting) administered per-screen after scripted scenario tasks, and treat any screen scoring high on Mental Demand AND high on Frustration as a redesign trigger rather than a training problem.
  REC[Provenance rule / determinism boundary (] Render engine-computed fact and model-generated advisory in structurally different containers, differing on at least three simultaneous non-colour channels: a leading glyph, a border treatment, and typeface class (engine facts in the numeric/mono family, advisories in the prose family). Never place an advisory in the global status bar, never in the event stream, and never above the fold in the con
  REC[COMMAND mode / determinism boundary / au] Before COMMAND executes, require a one-line player pre-commitment: 'What do you expect this to achieve?' — free text, stored, non-blocking in content but mandatory in presence. After resolution, render the player's stated expectation directly above the engine's actual outcome, with the divergence explicit.
  REC[World model / macro-meso-micro tiers / o] Add an explicit 'abstract function' display layer between macro indicators and institutions, and do not build it until the engine has real conservation mechanisms. It should show, for each conserved quantity (fiscal balance, sourced legal authority, political capital, coercive capacity), the current level, the committed/reserved portion, and the free remainder — a balance display, not a gauge. Unt
  REC[Bottom event stream / Eight Questions] Make the bottom event stream goal-organised, not merely chronological: every event row carries a persistent 'Why' affordance that populates the right context panel's Q2-Q5 stack IN PLACE, without navigation, without changing the centre view, and without collapsing the projection band. Filters are by entity and by goal, not by severity alone. The stream must never steal focus — it is a WCAG role="l
  PITFALLS: Treating ADR-006 as sufficient protection against over-trust. The determinism boundary is architectural; automation bias is perceptual. Reported findings that prominence and on-screen detail increase automation bias mean a correctly-architected system can still produce a player who rubber-stamps every LLM proposal.; Adding a provenance tag and considering the provenance rule satisfied. A tag is one channel, is easily filtered out by habituation, and does nothing about placement or prominence — which the automation-bias literature suggests matter more.; Building the abstraction-hierarchy 'abstract function' display before the engine has costs, budgets, cooldowns and prerequisites. It would re
## Natural-language command and action-confirmation interfaces for MERIDIAN's EXPLORE / PLAN / COMMAND modes
SUMMARY: The historical failure of natural-language interfaces to complex systems is not parsing accuracy — it is the user's inability to envision what the system can be asked to do, and the user's inability to evaluate whether the returned result is what they meant (Subramonyam et al.'s "gulf of envisioning", arXiv 2309.14459). Modern LLM-agent UX has converged, in shipped systems, on a specific answer: do not render intent as conversation, render it as a structured, inspectable, editable artifact that the deterministic system prices, and gate execution on approval of that artifact. Claude Code's plan mode is the clearest shipped instance — read-only research, a written plan, an editor-openable plan artifact (Ctrl+G), and a four-option approval prompt whose options differ in how much subsequent au
  FIND(well-established) The dominant failure of natural-language interfaces is capability envisioning, not language understanding. Users cannot determine whether the system can do the task, how to instruct it, or whether the output met their goal. | WHY: MERIDIAN's charter demands the player propose something UNEXPECTED. If the player cannot form a model of what is proposable, they will regress to the safe subset they have already seen work — which is functionally the 'm
  FIND(well-established) Shipped agent products have settled on plan-as-artifact: a read-only research phase produces a written plan that the user can open, edit directly, and approve — and the approval choice itself selects the autonomy level of the execution phase. | WHY: This is a working existence proof for MERIDIAN's PLAN→COMMAND boundary, including the detail that approval is not binary. MERIDIAN can copy the structure (not the trade dress): approve-and-execute-step-by-step vs approve
  FIND(well-established) Confirmation dialogs lose power in proportion to how often they fire, and escalating their drama makes users faster, not more careful. | WHY: MERIDIAN will generate many commands per session. A uniform 'Confirm order?' modal will be click-through within an hour and will provide no protection on the one irreversible order that matters.
  FIND(contested) Whether typed confirmation ('type the name to proceed') is good design is genuinely contested. | WHY: MERIDIAN should not adopt typed confirmation as a default reflex. It is defensible only where reversal is genuinely impossible and the typed token itself carries disambiguating information (which object, which target).
  FIND(well-established) Invisible or implicitly-entered modes cause severe, safety-critical errors; the remedy is either modelessness or a continuously visible, deliberately-held mode indicator. | WHY: EXPLORE/PLAN/COMMAND is a three-mode design in which the same text box means three different things. The identical sentence typed in the wrong mode is either a harmless question or a committed national-scale action. This
  FIND(well-established) Indirect prompt injection is a live, demonstrated attack class against real LLM-integrated applications, and it arises precisely from mixing instructions and retrieved data. | WHY: MERIDIAN plans to feed in-world documents, uploaded images, map annotations and voice transcripts into intent interpretation. Every one of those is attacker-authorable content in a world where the fiction itself can cont
  FIND(well-established) Human approval is explicitly acknowledged by security researchers as an insufficient defense against prompt injection, because approval fatigue produces reflexive consent. | WHY: If MERIDIAN's answer to injection is 'the player confirms every command card', that answer is already known to fail. The defense must be architectural: untrusted content must be structurally incapable of originating or r
  FIND(well-established) There are named, published structural defenses that fit MERIDIAN's architecture almost exactly — plan-then-execute, and the dual-LLM / quarantined-LLM pattern with capability (taint) tracking and isolated context windows for untrusted reads. | WHY: MERIDIAN already has the hard prerequisite most systems lack: a fixed, finite vocabulary of ~16 consequence primitives resolved by a deterministic engine. That is a natural control-flow/data-flow split. The primitive com
  FIND(emerging) Structural defenses cost capability, and the cost is measurable rather than hypothetical. | WHY: MERIDIAN should budget for the fact that a hard determinism/taint boundary will make some player intents unresolvable. That is the correct trade for this project — but the UI must then explain the refusal, not fail silen
  FIND(well-established) A production agent system reduces approval fatigue not by removing approvals but by classifying actions into auto-approved, classifier-checked, always-prompted, and never-auto-approved tiers — with a small protected set that no user rule can pre-approve. | WHY: Directly transplantable to MERIDIAN's command tiers. The design insight is that the tier is a property of the ACTION's consequence class, not of the player's patience, and that a protected set is exempt from all convenie
  REC[EXPLORE/PLAN/COMMAND mode shell; global ] Make the three modes quasimodal at the input, not global-state. The mode is rendered in three simultaneous places that cannot disagree: (1) a persistent chip in the global status bar; (2) the composer's left gutter label and border treatment — EXPLORE 1px dotted, PLAN 2px solid, COMMAND 2px solid with a 4px inset accent rule; (3) the submit control's verb, which is the mode: 'Ask', 'Draft order', 
  REC[Command card component; PLAN mode centre] Replace the assistant chat bubble with a COMMAND CARD as the only object that can ever be committed. Fixed section order, always all present, never collapsed away when empty: (1) INTERPRETED INTENT — one sentence, engine-restated, editable; (2) COMPOSITION — the ordered list of consequence primitives with per-primitive parameters; (3) AUTHORITY — the legal/institutional basis, or the named missing
  REC[Command card provenance strip; commit bu] Tag provenance PER FIELD, not per card, using a chip carrying three redundant signals: a distinct glyph, a distinct border treatment (solid = engine-computed, dashed = model-proposed, double = player-authored), and a text label. Minimum 11px, minimum 4.5:1 contrast against the card surface, never colour alone. The commit control stays disabled while any field in sections 2, 3, 4 or 9 carries a das
  REC[Commit flow; engine reversibility classi] Grade confirmation by an engine-computed REVERSIBILITY CLASS and spend confirmation budget only where the class demands it. R0 (engine can revert, no world observer saw it): no confirmation, commit immediately, 10-second undo affordance in the event stream. R1 (revertible at cost): single-step commit with effects preview expanded by default; undo offered but priced, with the price shown before com
  REC[Command card reversibility section; even] Do not fake undo. MERIDIAN currently has no revert, cooldown or decay mechanism — an acknowledged open critical. Until a real revert exists, the reversibility strip must render 'This cannot be un-ordered. The world will have moved before you could.' as an engine-computed statement, and no undo control may appear anywhere in the command surface. When revert lands, undo appears only for R0/R1 and on
  REC[Composer; rejection/refusal copy; onboar] Build a browsable CAPABILITY INDEX keyed on the 16 consequence primitives, reachable from the composer via a persistent 'What can I attempt?' control, structured as verb-first plain-language entries ('Seize an asset', 'Create a legal obligation', 'Expose information') each showing what it requires, who typically resists, typical lead time, and two worked examples drawn from CURRENT world state rat
  REC[Intent parser; command card slots; entit] Resolve ambiguity by ENGINE-ENUMERATED slot filling, never by model guessing. When intent parsing yields an unresolved referent, the card renders an UNRESOLVED SLOT chip inline in the section where it belongs, and the resolution UI offers candidates drawn from the world-model entity index using the player's own intelligence-profile view (#4), each carrying its confidence label. Cap at three unreso
  REC[Intent pipeline; in-world document inges] Adopt plan-then-execute with a hard reader/planner split for all in-world content. The primitive composition and its targets are fixed BEFORE any untrusted in-world document, image, transcript or annotation is read. A quarantined reader model, running in an isolated context, may extract only into typed quoted data slots (dates, quantities, entities resolved against the existing index) and is struc
  REC[Command card; document ingestion; event ] Render a TAINT chip on every card field whose value derives from in-world content, showing the source document and its engine-computed confidence label, and treat taint as an escalator on the reversibility ladder: any R1 order containing tainted fields is promoted to R2 confirmation. Add a standing rule to the interface contract: no in-world document may ever cause a command card to appear unpromp
  REC[Voice input pipeline; commit flow; acces] Voice may draft but may never commit. A voice transcript enters as a versioned external input, is displayed as recognised text for correction before it becomes a card, and the commit control is disabled for cards whose most recent edit came from voice until the player performs a non-voice confirmation. For accessibility, provide a voice-commit path requiring the engine to read back the COST, RESIS
  REC[Telemetry; reversibility classifier tuni] Instrument approval behaviour and treat rapid approval as a system defect signal, not a player trait. Log dwell time between card render and commit, the fraction of card sections ever expanded, and edit rate. When a player commits R2 orders below a dwell threshold repeatedly, do not add a nag — reduce R2 frequency by re-examining the reversibility classifier, and surface a session-level summary in
  REC[Command card pricing; RNG substream desi] Make the card a projection with the same correctness properties already specified for the dossier: drafting, re-pricing, expanding or abandoning a card must consume ZERO authoritative randomness and must leave the simulation byte-identical. Forecast ranges in the UNCERTAINTY and SECOND-ORDER sections draw from a named RNG substream seeded by (card id, revision), so re-opening a card shows identica
  PITFALLS: Treating parser accuracy as the problem. The evidence points at capability envisioning and output evaluation instead — a perfect parser attached to an unknowable capability space still collapses into the pre-authored-menu failure the charter forbids.; Letting the LLM's proposed composition render as if it were engine fact. Without per-field provenance and a commit control mechanically gated on it, the provenance rule stays documentation — precisely this project's named defect class.; Uniform confirmation modals on every order. They will be click-through within an hour, and will fail on the one irreversible order that mattered (NN/g's cry-wolf finding).; Reaching for typed confirmation as a g
## Map and layer design for multi-domain data — cartographic basemaps for dark analytical UIs, layer taxonomy, thematic technique choice, decluttering, geographic + non-geographic substrates, and uncertain position — applied to MERIDIAN
SUMMARY: Research constraint declared up front: this session's WebSearch budget was exhausted before the first query, so all evidence below comes from direct fetches of primary sources (Axis Maps cartography guide, W3C WCAG 2.2, Mapbox Style Spec, Leaflet docs, Uber H3 docs, NN/g, Wikipedia reference articles, EU4 wiki, MU Collective). Four intended sources 404'd or were unreadable (CARTO basemap rationale, Axis Maps heat-map page, IDL value-suppressing uncertainty palettes, MacEachren's geospatial-uncertainty paper, Shneiderman semantic substrates PDF); every claim that depended on them is marked unverified and is presented as inference, not as citation.

The core finding is that MERIDIAN's map problem is not a cartography problem — it is a determinism-and-honesty problem wearing cartographic clot
  FIND(well-established) Choropleth maps require normalised (rate/ratio) data and are actively wrong for raw counts — and MERIDIAN's macro tier supplies neither. | WHY: The default instinct for a 'crisis map' is to recolour countries/regions by an indicator. MERIDIAN's macro tier is a set of national scalars: there is no sub-national denominator, no areal unit population, no exposure ba
  FIND(well-established) Per-tick recomputation of classification breaks (quantiles / natural breaks / auto-fit ramps) would conceal MERIDIAN's open macro-saturation critical finding rather than expose it. | WHY: MERIDIAN currently has no cost, cooldown, decay, budget, prerequisite or revert mechanism, so macro indicators climb monotonically. If the colour scale is refitted every tick, the map always shows a healthy spread of lig
  FIND(well-established) Dot-density maps induce false precision: readers infer dot locations as real locations, and mitigation is to restrict dot placement to areas where the phenomenon actually exists and to avoid over-detailed scales. | WHY: This is the single most direct cartographic analogue of MERIDIAN's fidelity-tier architecture. Cohort-tier (meso) population is statistical; if it is rendered as individually placed dots at a zoom where a player can sing
  FIND(well-established) Proportional symbols are systematically misjudged: people under-estimate area differences, and the error grows with the magnitude of the difference. | WHY: MERIDIAN will be tempted to size markers by 'importance', 'casualties', 'economic exposure'. Continuous area scaling will be read wrongly and, worse, wrongly in a direction that flatters large values (they look smaller t
  FIND(well-established) The modifiable areal unit problem means any regional aggregate MERIDIAN renders is partly an artefact of the boundaries the scenario author drew — with two distinct components, scale effect and zone effect. | WHY: MERIDIAN authors its own fictional administrative geography. That means its regional boundaries are literally 'arbitrary, modifiable, and subject to the whims of whoever did the aggregating' — the textbook definition. An
  FIND(well-established) Hexagonal binning avoids the zone-effect half of MAUP (no administrative boundaries) but does not avoid the scale effect, and hierarchical hex containment is only approximate. | WHY: Hexbin is the right aggregation substrate for MERIDIAN's household/local-network fidelity tier: it lets the engine show density without asserting either individual positions (dot false precision) or authored boundaries (
  FIND(well-established) Established layer UI draws a hard line between mutually exclusive base layers (radio buttons) and additive overlays (checkboxes) — the exclusivity is encoded in the control affordance itself. | WHY: This is the cheapest correct thing MERIDIAN can adopt wholesale. Most bad layer panels are bad because they render everything as a checkbox list, allowing the user to stack three mutually unreadable fills. The control ty
  FIND(well-established) A large shipped strategy game runs 40+ thematic 'map modes' as a switchable recolouring of the same geometry, with a small set of persistent attributes surviving every mode — and hotkey-bound cycling rather than a nested tree. | WHY: This is a real, shipped existence proof that a very large number of information views can be managed without a layer tree, provided (a) only one thematic recolour is active at a time, (b) a small invariant substrate pers
  FIND(well-established) Symbol decluttering in production map engines is an explicit, configurable collision system with per-property control over which element yields — and the winner is chosen by a sort key. | WHY: MERIDIAN cannot treat decluttering as a rendering detail. Whether a marker survives a collision determines what the player sees, and if that is decided by feature insertion order or tile arrival order it is non-determini
  FIND(well-established) Military symbology deliberately encodes affiliation redundantly in both frame shape and colour so that it survives monochrome rendering, and encodes temporal status (present vs planned) as solid vs dashed line. | WHY: This is a directly transferable, standards-grade solution to two of MERIDIAN's hardest rendering problems: (a) how to encode an eight-value confidence vocabulary without relying on hue, and (b) how to distinguish engine-
  REC[Shell architecture; centre operational v] Demote the map: the centre operational view is a SUBSTRATE SWITCHER holding three co-equal substrates — MAP (space, jurisdiction, distance, reach), SOCIETY (entity/relationship graph), CAUSAL CHAIN (the Eight Questions rendered as a directed outcome graph). One keystroke each (M / S / C). The map is the default substrate for a run's opening tick, but is never architecturally privileged: no shell a
  REC[Layer panel; map rendering pipeline; VIS] Adopt a strict five-class layer taxonomy with exclusivity enforced by control type, not by validation: CLASS 0 BASE (radio, exactly one, never encodes simulation state — terrain / administrative / blank); CLASS 1 THEMATIC FILL (radio, at most one, may be None — the areal recolour); CLASS 2 OVERLAY (checkbox, additive, unlimited — entities, events, infrastructure, movement, reach footprints); CLASS
  REC[ADR-006 determinism boundary; world mode] Extend dossier properties D-1 and D-2 to the map and to every substrate, and state them as engine invariants: MAP-1 — substrate/layer/zoom/selection state is a PROJECTION, never a MUTATION; any sequence of pans, zooms, layer toggles and substrate switches leaves simulation state byte-identical. MAP-2 — rendering consumes ZERO randomness; no jitter, no random dot placement, no force-directed layout
  REC[Map rendering; determinism boundary; VIS] Drive symbol collision with a deterministic engine-computed salience key. Every renderable feature carries salience: int, computed by the engine from (player-relevance, recency, magnitude class, confidence rank), tie-broken by stable entity ID — never by insertion order, tile arrival, or feature index. Set icon-allow-overlap=false, text-allow-overlap=false, text-optional=true, icon-optional=false,
  REC[Entity rendering; confidence label syste] Render uncertain position as REGION-PRIMARY geometry with no point symbol. Bind rendering to the confidence label: Confirmed → point symbol at true coordinates, solid 1.5px border. Reported → point symbol with dashed border plus a containing region at 60% ring opacity. Assessed / Disputed / Possibly deceptive → NO point symbol at all; render only the probability region, filled at ≤12% opacity with
  REC[DESIGN-TOKENS; confidence label renderin] Encode confidence with crispness + border treatment + text chip, and NEVER with hue. Reserve hue exclusively for nominal semantic domain (entity class / affiliation / institutional sector). Each of the eight confidence labels renders as: (a) a distinct border style — solid / long-dash / short-dash / dotted / double / feathered / none / lock-hatched; (b) a distinct edge crispness value; (c) a text 
  REC[Thematic fill layer; legend component; m] Freeze classification breaks per scenario, never per tick, and print the full provenance in the legend. Every thematic fill legend must display, permanently and without hover: variable name, denominator (or the literal text 'UNNORMALISED — COUNT'), classification scheme, the numeric break values, class count (3–7), and the tick at which breaks were fixed. Any fill whose denominator is null renders
  REC[World model fidelity tiers; map renderin] Bind rendering technique to fidelity tier and enforce a resolution ceiling. Focal individuals and named secondaries → point symbol at true coordinates. Households / local networks → hexbin cells only (H3 or equivalent), never individually placed dots. Population cohorts → areal fill only, no discrete marks of any kind. Enforce a hard zoom ceiling per tier: when the player zooms past the resolution
  REC[Provenance rule; conversational interfac] Ban AI-generated text from the map canvas entirely. No generated labels, captions, region annotations, or event descriptions may be drawn into the map surface at any zoom. All model-proposed narration renders exclusively in the right context panel, inside a container with a persistent provenance header that remains visible while scrolled. Corollary: engine-computed labels on the canvas are, by con
  REC[VISUAL-CONSTITUTION; DESIGN-TOKENS; VISU] Design the dark basemap as a narrow luminance band with declared headroom, and gate it with a blur test in CI. Basemap occupies a bounded band (target L* 8–22; no pure #000000, no element above the band ceiling). Chroma of every basemap element ≤10% of the palette maximum — full saturation is reserved for data. Every Class 1–3 element must clear a declared minimum ΔL* above the band ceiling AND me
  REC[Society substrate; relationship model; d] Build the SOCIETY substrate as a deterministic semantic-region layout, not a force-directed graph. Nodes are placed in fixed named bands by entity class (state organs / institutions / firms / media / communities / households / individuals), ordered within each band by a stable engine-computed key, with position a pure function of (entity set, sort key) and no physics simulation. Render each direct
  REC[Organisation entities; map symbology; do] Make organisations render as portals with visible internal disagreement, on every substrate. An organisation's map marker carries a segmented ring showing its constituent factions sized by internal influence, with segment colour showing each faction's stance on the currently selected proposal (support / oppose / abstain / unknown). A monolithic single-colour organisation marker is a defect, not a 
  PITFALLS: Choropleth-first. Recolouring regions by a macro indicator is the default instinct and is nearly meaningless here: macro is a national scalar with no sub-national denominator, and Axis Maps is explicit that choropleth demands normalised rates and 'never' raw counts. Building it first spends the most expensive surface on the least informative encoding.; Per-tick auto-classification. Refitting quantile/natural-break/auto-ramp scales each tick will hide the open macro-saturation critical behind a permanently healthy-looking colour spread, and will manufacture visual dynamism from a monotonic climb.; Random or physics-based placement. Random dot placement for density, jittered markers to resolve
## Onboarding and learnability for very deep simulations — evidence review and a concrete first-session architecture for MERIDIAN
SUMMARY: METHOD CAVEAT FIRST: the WebSearch budget for this session was exhausted before my first query, so this is built from ~24 directly-fetched sources (NN/g, Wikipedia, Steam store + global achievement stats, Paradox community wikis, Game Developer, Game Accessibility Guidelines) rather than a broad search sweep. Four intended sources were unreachable (Rock Paper Shotgun and Eurogamer are blocked for this tool; the PC Gamer Terra Invicta review 404'd; terrainvicta.wiki.gg returned 401). Specific unverified items are named in the findings.

The evidence converges on four things. (1) The controllable variable is EXTRANEOUS load, not depth — cognitive load theory splits load into intrinsic (inherent difficulty), extraneous (imposed by presentation, "under the control of instructional designers"),
  FIND(well-established) The designer-controllable component of difficulty is extraneous cognitive load, not intrinsic depth — so 'MERIDIAN is inherently hard to learn because it is deep' is a false framing. | WHY: It licenses MERIDIAN to keep full simulation depth while treating every unexplained number, every un-anchored proper noun and every split-attention layout as a defect with a named owner. It reframes onboarding work as lo
  FIND(well-established) Instructional support that helps novices measurably degrades expert performance (expertise reversal effect), so every teaching affordance requires a designed fading path. | WHY: MERIDIAN's target user becomes an expert inside one long session. A permanent explainer layer will make the product feel patronising and slow precisely to the users who stay. Fading must be a first-class engineering requ
  FIND(well-established) Progressive disclosure improves learnability, efficiency AND error rate simultaneously, and NN/g reports no evidence that hiding advanced settings causes users to form limiting mental models. | WHY: This is the licence for MERIDIAN to expose only a subset of the 16 consequence primitives in the first session. But the second half of the claim is the load-bearing part: if hiding DID damage mental models, staged exposu
  FIND(well-established) Front-loaded overlay/coach-mark tutorials fail structurally: users must memorise before they can apply, retention decays in ~20 seconds, and chained hints accelerate dismissal. | WHY: Rules out the cheapest onboarding MERIDIAN could ship — a next/next/next tour of the command shell. Directly motivates a one-hint-at-a-time, act-to-dismiss rule and just-in-time contextual presentation.
  FIND(well-established) Practitioner consensus names eight recurring tutorial defects, of which text-overload, unskippable/forced repetition, and punishing inexperience are the most relevant to MERIDIAN. | WHY: Gives a concrete pre-ship checklist. MERIDIAN's temptation is exactly failure mode 2 (screens of fictional-world exposition) and failure mode 8 (assuming a well-designed command shell is self-explanatory).
  FIND(well-established) Terra Invicta loses roughly half its players before mid-game systems, measured by global achievement completion. | WHY: Quantifies the cost of deep-sim onboarding failure in a title closely adjacent to MERIDIAN's genre (grand-strategy layer over an alien-invasion crisis sim). It sets the benchmark MERIDIAN's first-session funnel must beat
  FIND(well-established) Victoria 3's drop-off is steeper still, and is NOT an Ironman-mode artefact — its top achievement is 42.2% and full-campaign completion is 18.2%. | WHY: Achievement funnels in Paradox titles are usually dismissed because achievements require Ironman. Victoria 3 is the exception, which makes it the cleanest available public evidence that a mainstream, well-reviewed deep s
  FIND(contested) Tutorial quality is judged per-subsystem, not globally — the same game can have a praised tutorial and an unusable subsystem. | WHY: MERIDIAN must not run 'is the onboarding good?' as one review. Each subsystem (intent parsing, dossiers, confidence labels, resolution, AAR) needs its own learnability gate, because a single opaque subsystem is what revi
  FIND(well-established) A clean, well-praised interface does not imply learnable onboarding — the two are separable properties. | WHY: MERIDIAN's owner thesis is a strong visual/IA thesis ('conventional usability underneath, command-system styling on top'). This finding says executing that thesis perfectly still leaves the onboarding problem entirely un
  FIND(well-established) Documentation dependence is an externalised design cost, and Dwarf Fortress is the canonical case: a deliberate decision not to prioritise interface pushed a 10,000-article wiki into the critical path of play. | WHY: MERIDIAN's charter demands the engine answer eight questions per outcome. If those answers live in a wiki rather than the interface, the charter is technically unmet regardless of how good the wiki is. Wiki traffic shoul
  REC[Conversational interface (EXPLORE/PLAN/C] Build the INTENT LEDGER as the primary teaching surface and ship it before any tutorial. A fixed left-to-right strip renders every proposal in five columns: (1) YOUR WORDS — the verbatim player input, versioned, with its provenance tag; (2) INTERPRETED AS — one chip per consequence primitive, each chip removable and addable by the player before commit, each labelled with the primitive's plain-lang
  REC[Engine validation layer; consequence pri] Make REFUSAL a first-class typed response object, never a string. Every rejected proposal returns: failed_check (named rule id), required_value, current_value, blocking_actor (which institution or entity is the obstacle), and at least one remediation_path with its own cost and lead time. Render it as a card with a single primary action: 'Attempt the remediation path'. Forbid, by schema validation 
  REC[First session; scenario authoring standa] Open the first session on a SITUATION BRIEF of exactly five slots — what is happening / who is affected / what you control / what you cannot control / what decision is due and by when — capped at 150 words total, one map, three entity chips. Enforce a THREE-ANCHOR RULE: no more than three fictional proper nouns may appear before the player's first action. Every fictional-world fact shown in sessio
  REC[Consequence primitive composer; staged f] Expose exactly four of the sixteen consequence primitives as ACTIVE in session one — spend/reserve resources, trigger an approval process, change a belief, create public exposure — chosen to span the money / legality / opinion triad. Render the other twelve as PRESENT BUT INACTIVE rows in the composer, each with a one-line reason ('no military asset is in scope in this scenario', 'requires a diplo
  REC[AI advisory text; provenance rule; deter] Build the adviser as a FUNCTIONAL RAIL, not a character. Four labelled lanes — Legal, Finance, Operations, Public — with no name, no face, no personality and no voice. Every adviser utterance carries the provenance tag inline (not in a footer) and a 'computed from' affordance that expands to the specific engine facts and rule ids it cites. HARD GATE: an adviser line that cannot resolve at least on
  REC[Eight Questions; outcome rendering; lear] Make an AFTER-ACTION REVIEW panel the permanent core loop, opened automatically after any tick that changed authoritative state, and never skippable-by-default but always dismissible-per-instance. Top strip = the four AAR questions in fixed order: what did you intend / what actually happened / why the difference / what changes next. Below it, the Eight Questions render as eight labelled sections i
  REC[All contextual guidance; settings; acces] Implement PER-CONCEPT FADING with a restore registry. Each teaching affordance (primitive chip explainer, check-row auto-expansion, confidence-label first-encounter callout, dossier tab hint) carries a per-concept success counter. After three successful uses of that concept, the affordance collapses to icon-only but stays one-click re-expandable. Every faded item is listed in Settings > Guidance w
  REC[Codex/encyclopaedia; entity dossiers; wo] Generate the codex/encyclopaedia FROM engine state, and enforce a publication gate: a codex entry that cannot link to at least one rule id, one consequence primitive, or one named mechanism does not ship. Every entity attribute shown in a dossier carries a 'affects:' line naming the mechanism it feeds. Add a CI check that fails the build when a codex entry's mechanism reference does not resolve to
  REC[Telemetry; QA gates; codex; interface re] Instrument documentation dependence as a DEFECT METRIC with a numeric tripwire. Log every glossary open, codex search and confidence-label callout expansion with the term and the time since that term's first on-screen appearance. Tripwire: if more than 20% of sessions open the glossary for term T within 60 seconds of T's first exposure, T's first-exposure surface is defective and gets a UI fix, no
  REC[Confidence labels; entity dossiers; four] Teach the eight confidence labels BY CONTRAST inside a forced decision, not by a legend. Session one must contain one decision where two dossier rows disagree — one Confirmed, one Disputed — and where trusting the Disputed row produces a materially different, non-fatal outcome that the AAR then names explicitly ('you acted on a Disputed assessment; the engine had evidence weight 0.3'). Render ever
  REC[Practice/sandbox mode; RNG substreams; d] Ship a REHEARSAL mode: a forked branch of the run where proposals resolve fully and produce complete AAR output, then the branch is discarded. It must be enforced technically — rehearsal draws from a dedicated named RNG substream and its results are written to a separate projection store, so discarding it leaves the authoritative timeline byte-identical, consistent with dossier properties D-1 (pro
  REC[Contextual guidance; interaction pattern] Adopt a ONE-HINT rule as a lint-enforced invariant: at most one contextual hint may be on screen at any time; it anchors to the element it describes; it persists until the described action is performed or it is explicitly dismissed; it is visually distinct from real controls (different border and background family, never the same chrome as an interactive control). Explicitly forbidden: modal welco
  PITFALLS: Building an anthropomorphic adviser with a name, face and voice. Players transfer authority to characters, which directly undermines the provenance rule and the determinism boundary — the two claims MERIDIAN is staking its credibility on. Functional lanes deliver the benefit without the authority transfer.; Shipping a scripted tutorial with rails. Any 'now click here, now click there' sequence teaches that the correct move is the authored one, which is the charter's explicitly named failure condition — a design that collapses back into a menu of pre-authored options. Teach through the composer and the refusal, not through a script.; Hiding consequence primitives to reduce first-session load.
## Time, pause, replay, branching and counterfactual presentation for MERIDIAN's event-sourced crisis simulation UI
SUMMARY: METHOD NOTE FIRST, because it bears on how much weight this deserves: this session's WebSearch budget (200/200) was already exhausted before my first search call, so I could not run the 5-8 exploratory searches the brief requires. I worked entirely by fetching primary sources at known URLs. Seventeen fetches succeeded; eight failed (rr-project.org timeout, globalsecurity TC 25-20 403, IPCC AR5 PDF unparseable, Adobe Premiere timeout, Blender docs 403, TortoiseGit 404, Paradox wikis lacked the relevant sections, SAGE blocked, NASA-STD-7009 body PDF 404). Everything below is sourced to something I actually read, or explicitly marked as inference/unverified. Where I would normally have found GDC talks, dev post-mortems and Steam-review evidence of player experience, I have none — that is a re
  FIND(well-established) Event Sourcing already names timeline branching as a native capability: replaying an event log lets you "determine the application state at any point in time" and explore "multiple time-lines (analogous to branching in a version control system)", via Complete Rebuild, Temporal Qu | WHY: MERIDIAN does not need to invent a branching model. It needs to implement a documented one. This also means the branch feature is gated on the event log actually existing — and MERIDIAN currently discards causal_parents 
  FIND(well-established) Replay must not re-fire external effects. Fowler requires gateways "configured to ensure that no messages go out unless you are processing an event 'for real'", and separately that "the response to every external query needs to be remembered" so that rebuilds reuse historical res | WHY: For MERIDIAN the external query IS the LLM. If replay re-calls the model, replay is nondeterministic, costs money per scrub, and silently violates ADR-006's determinism boundary and the dossier property D-3. Stored narra
  FIND(well-established) Retroactive Event supplies the exact mechanics for a counterfactual branch: corrections are applied at a "branch point" in the log, reached either by rebuilding from the last snapshot before it or by "reverse the events backwards from the latest event to the branch point", then r | WHY: Gives MERIDIAN a named, testable definition of what a branch operation is, and a snapshot-placement requirement: snapshot density determines how fast a branch can be spawned from an arbitrary past tick.
  FIND(well-established) Fowler warns that retroactive correction becomes intractable once external systems are involved — the system must detect what was sent incorrectly, compare to what should have been sent, and issue compensating actions, which "isn't possible to automate" in many real cases; human  | WHY: MERIDIAN's branches must therefore be strictly sandboxed rather than reconciled. A branch is a throwaway parallel world, never something that can be merged back into the canonical run. Design the UI so merging is not eve
  FIND(well-established) Parallel Model is the pattern for what-if analysis: "allow an alternative representation of the state of an application, either at a different time or in a hypothetical state", implemented either as a separate Parallel System with its own database or as embedded in-memory models  | WHY: Directly names MERIDIAN's hardest branching bug in advance: the same simulated person existing in two worlds. Entity IDs must be namespaced by world/branch, or dossiers will silently render cross-world state.
  FIND(contested) Hypothetical Outcome Plots beat static uncertainty displays on comparison tasks but lose on single-quantity estimation. HOPs were dramatically better at Pr(B > A) — F(2,573) = 73, 57, 220, all p < 0.001 — and at the three-variable task F(2,573) = 43, p < 0.001, with error bars sh | WHY: Branch comparison is literally a Pr(B > A) judgement, so it should use discrete-outcome/animated-draw displays. A single indicator readout is not, and should use a static interval. One house chart style would be wrong fo
  FIND(well-established) The HOPs authors themselves list substantial caveats: subjects likely above-average in numeracy, tasks may not represent real-world applications, sampling distributions/confidence intervals untested, static scatterplots omitted, viewer strategies unexplained, and frame rate unopt | WHY: Prevents MERIDIAN over-claiming the evidence. The finding supports animated draws for ordering judgements specifically; it does not license animating everything, and the 400ms frame rate is not a validated constant.
  FIND(well-established) Debriefs produce a large, robust performance gain: approximately 25% improvement over control, d = .67, across 46 samples and 2,136 participants, holding across team vs individual, simulated vs real, and medical vs non-medical samples, with facilitation and structure as moderator | WHY: This is the single strongest justification for building an AAR mode at all, and it says the gain comes from structure and facilitation — i.e. from the interface imposing a review sequence, not from merely making the repl
  FIND(well-established) AAR doctrine is comparative and explicitly non-punitive: an AAR "begins with a clear comparison of intended versus actual results achieved", focuses on "participants' own actions", is "forward-looking", and "assigning blame or issuing reprimands is antithetical to the purpose of  | WHY: Gives MERIDIAN's after-action screen a required top-level structure (intended vs actual, side by side) and a prohibition (no score, no grade, no ranking). It also implies two AAR surfaces at different weights, not one.
  FIND(well-established) Free-form branch graphs are computationally doomed to clutter. Sugiyama layered graph drawing depends on crossing minimisation (NP-hard) and cycle removal via feedback arc set (NP-complete); real implementations use median/average positioning plus adjacent-pair swapping, producin | WHY: This is the mechanical reason git graph UIs are unreadable, and it will not be fixed by better styling. MERIDIAN should refuse the general DAG as a default view.
  REC[Global command shell status bar; time co] Model time as four discrete modes, not a speed slider: HELD, STEP, RUN, REVIEW. Render the mode as a text chip in the global status bar carrying a distinct glyph AND a distinct border weight (never colour alone), minimum 11px, 4.5:1 contrast, with the sim clock and the integer tick index adjacent (e.g. "HELD · Day 41 06:00 · tick 9,842"). RUN offers exactly three named pacings (Slow / Normal / Fas
  REC[PLAN and COMMAND modes; right context pa] Implement active pause with explicitly staged orders. While HELD, COMMAND-mode actions do not execute; they enter a 'Staged' list in the right context panel, each showing the primitive composition, the engine-computed cost, and the validation verdict. Resuming executes all staged orders at tick T+1, and each resulting event records provenance 'staged at tick N while HELD'. Staged items render with
  REC[Settings; event stream; hold banner] Build auto-hold as a user-editable rule table in Settings, not as hardcoded interrupts. Each row: trigger predicate (plain-language), scope (any entity / watched entities only), and a three-way action (Hold / Notify only / Ignore). Ship with roughly six defaults, all overridable, and surface a one-click 'Never hold on this again' directly in the hold banner that fires.
  REC[Engine test suite; ADR on replay; RNG su] Make REVIEW mode strictly non-mutating and prove it with a property test. Extend dossier properties D-1 and D-2 to time: scrubbing, replaying, opening any past tick, and spawning a branch must consume zero randomness from the canonical run's substreams and must leave authoritative state byte-identical. Test: run N ticks, hash authoritative state; on a second run scrub to 500 pseudorandomly chosen 
  REC[LLM gateway; narration storage; ADR-006 ] Forbid LLM invocation in REVIEW and branch modes at the gateway, not at call sites. Introduce a single ExecutionContext with values LIVE, REPLAY and BRANCH; the model gateway raises if called under REPLAY. Replay renders the narration that was stored with that tick, verbatim, with its original provenance tag. If no narration was stored, render a present row reading "No advisory was generated at th
  REC[Timeline component; branch navigation] Default branch view is a linear spine with a branch gutter, not a graph. The canonical timeline renders as a single horizontal spine. Branches render as short stubs in a fixed-width left gutter, each labelled by WHAT WAS CHANGED ("Withheld the port subsidy, tick 9,842") — never by an ID or hash. Hard cap: at most two branches on screen in comparison mode. A full node-link 'Branch map' exists only 
  REC[Branch comparison screen] Branch comparison is a three-column divergence table, not two dashboards side by side. Columns: Actual | Counterfactual | Δ. Rows are quantities that diverged, sorted by absolute standardised delta. Above the table, a fixed three-part header states (a) the branch tick, (b) the single changed input, (c) the held-fixed set. Rows whose delta falls inside the run-to-run noise band render greyed with t
  REC[Branch comparison; indicator panels; vis] Use discrete-outcome displays for branch comparison and static intervals for single-indicator readouts. For 'did the branch change the ordering / probability of outcome X' questions, run the branch under N seeds and present the draws as either small multiples of individual runs or an animated frame sequence with mandatory pause and step controls. For a single indicator's current value, use a stati
  REC[Counterfactual/branch surfaces; provenan] Enforce counterfactual language as an engine-generated template, never as authored or model-written prose. The only permitted frame is: "Under the same rules and the same seed, changing <input> to <value> produced <outcome> in this simulation." Ban the strings 'would have', 'predicts', 'forecast' and 'expected' from counterfactual surfaces by lint rule over the template catalogue. Every counterfac
  REC[Branch execution; branch comparison scre] Never present a single counterfactual run as the counterfactual. A branch always executes under at least N seeds (suggest N configurable, default in the tens) and the UI always reports the spread plus the count. If only one seed has been run, the screen shows "1 of N runs complete" and withholds the comparison table rather than showing a provisional answer.
  REC[After-action review mode; PLAN-mode inte] Build an explicit AAR mode whose top-level structure is intended vs actual, and which contains no score, grade, rank or star rating. Screen order: (1) what you intended, drawn from the player's own stated plan captured in PLAN mode; (2) what actually happened; (3) the divergence points, each linking to a tick; (4) the Eight Questions rendered per divergence; (5) forward-looking 'what options chang
  REC[AAR mode; event store schema] Store review annotations in a separate stream that the simulation cannot read. An annotation is {tick, entity_id or event_id, author, body, created_at} written to a review stream; the engine's event replay must not consume it, and a test should assert that a run with annotations and a run without produce identical authoritative state hashes.
  PITFALLS: Drawing a free-form branch DAG as the default view. Crossing minimisation is NP-hard and feedback arc set NP-complete; heuristic layouts accumulate clutter as the graph grows, and branching undo trees are independently documented as hard to comprehend and remember. Styling will not rescue it.; Labelling branches by ID or hash instead of by the change that created them. This is a large part of why git graph UIs are illegible: the nodes carry no semantic content, so the user must reconstruct meaning from topology.; Re-invoking the LLM during replay. It makes replay nondeterministic, costs money on every scrub, and breaks ADR-006 and D-3 silently rather than loudly. Fowler's rule is that extern
## Accessibility for MERIDIAN's dark, dense, colour-coded command interface — standards analysis and concrete specification
SUMMARY: MERIDIAN's stated visual direction (dark command shell, five persistent regions, semantic colour, eight engine-computed confidence labels, a live event stream, map selection, voice input) sits on four accessibility fault lines that are structural, not cosmetic: (1) the WCAG 2.x contrast formula is known to overstate contrast near black, so a dark UI can pass 4.5:1 and still be functionally unreadable — while the proposed replacement, APCA, is an explicitly non-normative WCAG 3 draft, so building to APCA alone would break a WCAG 2.2 AA conformance claim; (2) eight confidence labels exceed any defensible colour-coding budget, and the obvious encodings (red/green, amber/green) collapse under deuteranomaly, which alone affects roughly 5% of men; (3) a continuously updating event stream is the 
  FIND(well-established) WCAG 2.2 SC 1.4.3 requires 4.5:1 for body text and 3:1 for large text, where large text is at least 18pt / 14pt bold — approximately 24px and 18.5px at the standard 1pt = 1.333px conversion. | WHY: This is the floor MERIDIAN must clear for any conformance claim. The 18.5px bold / 24px regular threshold is far larger than the type sizes a dense command UI naturally reaches for, so in practice almost all MERIDIAN tex
  FIND(contested) The WCAG 2.x contrast formula is documented as overstating contrast for dark colours, to the point where a 4.5:1 pair can be functionally unreadable when one colour is near black — the exact failure mode of a dark command interface. | WHY: MERIDIAN's aesthetic is dark-first. A team that treats the automated 4.5:1 checker as sufficient will ship a technically-conformant, practically-unreadable interface. This is the single most consequential finding for MER
  FIND(well-established) APCA is a candidate for WCAG 3 and is explicitly still in development — a draft, not normative. Its thresholds are Lc 90 (body text, no smaller than 14px/400), Lc 75 (minimum for columns of body text at 18px/400), Lc 60 (non-body text, min 24px normal or 16px bold), Lc 45 (headli | WHY: MERIDIAN cannot substitute APCA for WCAG 2.2 in a conformance claim without lying — precisely the defect the private-until-honest doctrine exists to prevent. But APCA is the better design tool for a dark UI. The resoluti
  FIND(well-established) Red/green colour vision deficiency affects approximately 1 in 12 men (8%) and 1 in 200 women (0.5%), roughly 300 million people worldwide and about 4.5% of the UK population. Deuteranomaly alone is approximately 5% of men; deuteranopia, protanopia and protanomaly are approximatel | WHY: It sets the actual cost of colour-only encoding and tells MERIDIAN which colour axis survives: the common deficiencies are red-green, so a blue↔orange/amber axis is far more robust than red↔green for neutral/caution/crit
  FIND(well-established) SC 1.4.1 Use of Color requires that 'Color is not used as the only visual means of conveying information, indicating an action, prompting a response, or distinguishing a visual element' — and explicitly permits colour coding provided it is complemented by another visual indicatio | WHY: It licenses MERIDIAN's semantic-colour thesis rather than forbidding it, but only under the redundancy rule. Critically, the standard accepts a lightness difference of 3:1 as itself a valid non-colour cue — giving MERIDI
  FIND(well-established) SC 1.4.11 Non-text Contrast requires 3:1 against adjacent colours for the visual information needed to identify UI components and their states, and for graphical objects required to understand content — and the computed value must not be rounded (2.999:1 fails). | WHY: This is the criterion a dark command UI fails silently. Panel borders, the boundary between operational view and context panel, chart gridlines and series strokes, map symbol outlines, the selected/focused state of a nav
  FIND(contested) Empirical evidence on dark mode is mixed and, for users with typical or corrected vision, tends to favour light mode. A 2013 Ergonomics study found light mode won across visual-acuity and proofreading measures for younger and older adults; a 2017 Applied Ergonomics study found li | WHY: MERIDIAN's dark aesthetic carries a measurable legibility cost for most users, not a neutral stylistic choice. It does not mean abandon dark — it means dark cannot be the only theme, and it raises the burden on type size
  FIND(well-established) ARIA live regions must exist in the DOM before content is inserted; aria-live='assertive' interrupts and is documented as 'extremely annoying and disruptive and should only be used sparingly'; role='log' and role='status' carry implicit polite, role='alert' carries implicit asser | WHY: MERIDIAN's bottom event stream is exactly the case these caveats are about. A stream firing several events per tick, marked polite, will queue announcements faster than they can be spoken and render the interface unusabl
  FIND(well-established) WCAG 2.2 adds SC 2.4.11 Focus Not Obscured (Minimum) at Level AA: 'When a user interface component receives keyboard focus, the component is not entirely hidden due to author-created content.' Sticky headers/footers, non-modal dialogs and semi-transparent overlays are the named o | WHY: MERIDIAN's specified shell is a global status bar plus a bottom event stream — i.e. sticky content pinned top and bottom, the exact configuration that produces this failure. A keyboard user tabbing down the centre operat
  FIND(well-established) SC 2.4.13 Focus Appearance (Level AAA) specifies the focus indicator be at least as large as a 2 CSS pixel thick perimeter of the component (for a rectangle, area 4h + 4w) and have at least 3:1 contrast between the same pixels in focused and unfocused states — a change-of-state c | WHY: It gives a concrete, testable focus-ring spec, and the change-of-state framing is the part teams get wrong: a ring that is 3:1 against the panel but only 1.4:1 against the component's own unfocused border still fails. Fo
  REC[DESIGN-TOKENS, VISUAL-CONSTITUTION, CI t] Adopt a DUAL-GATE contrast rule in DESIGN-TOKENS: every text/background pair must pass BOTH WCAG 2.2 SC 1.4.3 (4.5:1 body; 3:1 only for text at ≥18.5px bold or ≥24px regular) AND an APCA floor of Lc 75 for body text, Lc 60 for secondary/metadata text, Lc 45 for headings ≥24px bold. Non-text — panel dividers, chip borders, chart strokes, map symbol outlines, focus rings, selected-state indicators —
  REC[VISUAL-CONSTITUTION, DESIGN-TOKENS, conf] Cap the semantic colour axis at THREE states — neutral / caution / critical — built on a blue↔amber↔desaturated-red progression rather than red↔green or amber↔green. Never encode any of the eight confidence labels by hue. Colour may reinforce severity; it may never be the carrier of confidence, visibility, entity type, faction, or tier.
  REC[Confidence labels, dossier views 1–4, IN] Specify the confidence chip as triply redundant: (a) the full label word as text — 'Confirmed', 'Reported', 'Assessed', 'Disputed', 'Unknown', 'Possibly deceptive', 'Outdated', 'Restricted' — never abbreviated to a letter or number; (b) a distinct monochrome glyph per label, each distinguishable by silhouette at 16px in a single colour (test by rasterising all eight to pure black on white); (c) a 
  REC[Dossier tabs, entity views 1–4, Eight Qu] Render 'Unknown' and 'Restricted' rows with a distinct non-colour treatment AND expose the two axes separately in the accessibility tree: visibility (Restricted vs visible) as an explicitly-labelled restricted state, and confidence (the other seven) as text inside the row's accessible name. A screen reader reading a Relationships row must hear something equivalent to: 'Trusts — target: Delegate Va
  REC[Bottom event stream, notification system] Implement the event stream as: an outer container with role='log' aria-live='polite' aria-relevant='additions' aria-atomic='false', present in the DOM from first paint and starting empty; a hard rate limit of at most one announcement per 5 seconds; and a separate single-slot role='status' element carrying a batched summary ('4 new events, highest severity: budget request blocked') rather than the 
  REC[ADR-006 presentation layer, all LLM-narr] Give every AI-generated advisory a provenance treatment with four independent channels: a visible text label ('AI-generated advisory — not engine-computed'), a glyph, a container with role='note' whose accessible name states the provenance, and inclusion of the provenance string in the copied text when the block is copy-pasted. A border colour, background tint, or italic typeface alone is a violat
  REC[ADR-006, dossier properties D-1/D-2, rep] Add an accessibility-invariance test to the determinism suite: a property test that runs a fixed scenario to a fixed tick, then toggles every accessibility setting (theme, UI scale, reduced motion, announce mode, colour-vision palette, font size) and asserts the authoritative state hash is byte-identical and the RNG draw count unchanged. Accessibility preferences must be stored outside the version
  REC[Command shell, SCREEN-SPECIFICATIONS, IN] Build the five-region shell as five landmark regions with accessible names (status / navigation / operational view / context / event log), a keyboard cycle between regions on a single non-simultaneous key (INFERENCE: F6 forward, Shift+F6 back, following desktop convention), roving tabindex within each region so a region costs one tab stop rather than forty, skip links to each region, and CSS scrol
  REC[DESIGN-TOKENS, all interactive component] Specify the focus indicator as a dual-tone ring: a 2px inner ring in the light token plus a 1px outer ring in the dark token (or vice versa), so at least one of the two achieves 3:1 against whatever it sits on. Target the SC 2.4.13 area rule — indicator area ≥ 4h + 4w for a rectangular component — and 3:1 change-of-contrast between focused and unfocused pixels of the same element. Never remove the
  REC[Map view, icon rails, panel headers, mar] Set 24×24 CSS px as the minimum target for every control, and where icon rails or panel headers use 16–20px glyphs, meet SC 2.5.8's spacing exception by ensuring 24px-diameter circles centred on each target do not intersect — roughly 24px centre-to-centre minimum. For map markers, which cannot satisfy either rule when clustered, invoke the 'equivalent' exception by shipping a keyboard-navigable li
  REC[SCREEN-SPECIFICATIONS, settings, conform] Support text scaling to 200% per SC 1.4.4 AND ship an in-app UI scale control spanning 80%–200% independent of OS settings, with the whole shell tested at 1280×720 at 150% scale. Do NOT claim SC 1.4.10 Reflow (320 CSS px) conformance for the five-region shell — instead declare a documented minimum supported viewport, state the deviation explicitly in the conformance record, and provide a single-re
  REC[MOTION-SYSTEM, cinematic moments, ambien] Split motion into two named classes in MOTION-SYSTEM. INFORMATIONAL motion (state transitions, a value changing, a causal link highlighting) survives prefers-reduced-motion but is reduced to opacity/colour cross-fades under 200ms with no transform. DECORATIVE and CINEMATIC motion (camera moves, parallax, scaling reveals, the reserved cinematic moments) is removed entirely under prefers-reduced-mot
  PITFALLS: Treating a passing automated 4.5:1 checker as sufficient for the dark theme. The WCAG 2.x formula is documented as overstating contrast near black, so the checker will bless pairs that are functionally unreadable. Highest-probability accessibility failure for MERIDIAN specifically, because it is invisible to the tooling the team will actually run.; Claiming WCAG 2.2 AA conformance on the basis of APCA compliance. APCA is 'the candidate for WCAG 3' and 'still in development' — not normative. Documenting it as WCAG conformance would reproduce the project's own named defect: documentation asserting a property that does not exist.; Colour-coding all eight confidence labels. Eight hues are not re
## Building an original "near-future command system" visual language for MERIDIAN without trade dress: typography, tokens, colour, dark-surface elevation, motion, and the legal/ethical originality question
SUMMARY: Research constraint, stated up front: this session's WebSearch budget (200/200) was exhausted before my first query, so every source below was fetched directly by URL. That worked well for standards, specs and design-system source code, and badly for one area — practitioner commentary from the film-FUI community. Mark Coleran's site (coleran.com), the usual primary citation for the term "FUI", now resolves to a parked gambling domain; I could not verify any Coleran quote and have not invented one. The FUI-critique strand of the brief is therefore the weakest part of this report and is labelled as such.

The central finding is that a credible technical/institutional aesthetic is not produced by styling. It is produced by four mechanical disciplines that happen to look austere as a side effe
  FIND(well-established) Tabular numerals are a correctness feature for changing values, not a typographic preference: proportional figures cause horizontal reflow every time a digit changes, so a live indicator visibly jitters and column alignment breaks. | WHY: MERIDIAN's entire macro/meso layer is changing numbers under simulation tick. Jitter reads as instability and makes scan-comparison across rows impossible — the opposite of the institutional credibility the visual langua
  FIND(well-established) Encoding meaning in SHAPE first and colour second is proven operational doctrine, not a design opinion — NATO APP-6/MIL-STD-2525 deliberately makes shape and colour redundant so symbology survives a monochrome display. | WHY: This is the single most directly transferable precedent for MERIDIAN's eight confidence labels and its four entity views. It is also trade-dress-safe: it is an open interoperability standard, not a product's visual ident
  FIND(well-established) WCAG 1.4.1 forbids colour as the ONLY means of conveying information, and 1.4.11 requires 3:1 non-text contrast for UI component state indicators, boundaries and meaning-bearing graphics — with no rounding (2.999:1 fails). | WHY: MERIDIAN's confidence labels, provenance tags and fictional-world disclosure are all information-bearing chrome. If any of them is a coloured dot or a coloured border alone, it fails at Level A — the cheapest and most em
  FIND(contested) Dark mode is an aesthetic and contextual choice, NOT an accessibility improvement. The controlled evidence favours light mode for normal vision; dark mode benefits a specific low-vision population. | WHY: MERIDIAN's thesis assumes a dark command shell. That is defensible on ambience and low-light-use grounds, but the research does not license shipping dark-only and calling it accessible. This finding forces light + high-c
  FIND(contested) The WCAG 2.x contrast ratio formula is credibly disputed specifically for dark surfaces, where it is argued to overstate contrast for near-black colours — but the critique's main source is partisan and the replacement is not yet a standard. | WHY: MERIDIAN's shell is dark. If the 4.5:1 arithmetic is misleading in exactly that regime, passing a contrast checker is not proof of readability. This argues for empirical checking rather than formula-worship — while still
  FIND(well-established) Elevation on dark surfaces must be built from BORDERS and background steps, not shadows, because Windows High Contrast / forced-colors mode forces box-shadow and text-shadow to `none` and background-image to `none`. | WHY: A shadow-based elevation system silently collapses to a flat undifferentiated plane for forced-colors users — panels, popovers and the right context panel become indistinguishable from the operational view. On a dark sur
  FIND(well-established) There is a real, interoperable, vendor-neutral design-token specification (DTCG Design Tokens Format Module) with defined semantics for references, composite types and groups — token architecture does not have to be invented. | WHY: MERIDIAN's VISUAL-CONSTITUTION and DESIGN-TOKENS documents are unwritten. Adopting a published format gives the project a machine-checkable source of truth, tool interop, and — usefully for the originality argument — a d
  FIND(well-established) Mature design systems ship TWO motion registers with different easing curves — a fast functional one and a slower emphatic one — rather than a single house curve. | WHY: This validates MERIDIAN's "cinematic treatment reserved for specific moments" thesis at the token level: the 10% cinematic layer becomes a named, bounded, auditable second easing register rather than a licence to animate
  FIND(well-established) Practitioner duration guidance converges on roughly 100–500ms, with exits shorter than entrances, and >500ms perceived as obstruction. | WHY: Gives MERIDIAN a defensible numeric duration scale immediately, and a hard ceiling that prevents the 10% cinematic layer from becoming a tax on every interaction.
  FIND(well-established) The correct reduced-motion pattern is SUBSTITUTION, not deletion — and motion that conveys information must not be removed, only re-expressed. | WHY: MERIDIAN's motion is explicitly meant to explain state ("motion that explains state"). A blanket `* { animation: none }` reduced-motion rule would delete causal information for exactly the users who most need it stated e
  REC[VISUAL-CONSTITUTION §typography; DESIGN-] Adopt a strict two-family type system and forbid a third. Family A = one UI sans carrying the institutional tone, chosen for OpenType disambiguation rather than for its silhouette: it MUST ship tabular figures (tnum), slashed zero (zero), and a same-family disambiguation set for I/l/1 and O/0. Inter is the concrete candidate (SIL OFL, ships tnum + zero + ss02 disambiguation). Family B = one monosp
  REC[ADR-006 determinism boundary; every indi] Make numeric rendering a system invariant, not a component decision. Every engine-computed number renders with `font-variant-numeric: tabular-nums slashed-zero`. Every identifier renders in Family B. Implement as exactly two utility classes (.num-authoritative, .id-machine) plus a CI lint rule that fails any component rendering a value from engine state without one of them. Add a third class .num-
  REC[VISUAL-CONSTITUTION §typography; SCREEN-] Ship a seven-step type scale with paired line-heights and gate the extremes. Concretely: 32/36 (display, gated), 24/30 (view title), 20/26 (panel title), 16/24 (body, default), 14/20 (dense table row, secondary), 13/18 (chip/label/caption — hard floor, must meet 4.5:1), 11/14 (gated, permitted ONLY for non-essential decorative micro-labels that duplicate information available elsewhere). No arbitr
  REC[DESIGN-TOKENS; the four-region command s] Base the spatial system on a 4px atom with an 8px default rhythm, expressed as tokens (space.025=2, .05=4, .1=8, .15=12, .2=16, .3=24, .4=32, .6=48, .8=64). Provide exactly two density modes — Comfortable and Compact — implemented by swapping SPACING tokens only. Density must never alter type-size or colour tokens.
  REC[VISUAL-CONSTITUTION §surfaces; command s] Build elevation border-first on dark surfaces. Define exactly four surface levels as (background token + 1px border token) pairs — surface.base / surface.raised (panels) / surface.overlay (popovers, dossier) / surface.scrim. Every level must remain distinguishable using border alone. Shadows are permitted ONLY as a redundant reinforcement of a difference already carried by border + background step
  REC[DESIGN-TOKENS colour; VISUAL-QA-CHECKLIS] Define a colour budget as an enforceable rule, not an aspiration: a 12-step neutral ramp plus ONE brand accent plus a semantic set (info/positive/caution/critical). Hard rules — (a) the accent is reserved for interactive affordance and current selection, never for meaning; (b) semantic colour may only be applied to elements that ALSO carry a non-colour cue; (c) in the resting state of any screen, 
  REC[Eight confidence labels; dossier rows; a] Specify the eight confidence labels as a triple-coded chip: TEXT (the full word, never abbreviated) + a distinct GLYPH + a distinct BORDER TREATMENT, with colour as a fourth, strictly optional channel. Concretely — Confirmed: solid 1px border, filled glyph. Reported: solid 1px border, outline glyph. Assessed: dashed 1px border, outline glyph. Disputed: solid 1px border, split/divided glyph. Unknow
  REC[Person-dossier ten tabs; visibility axis] Give absence a positive visual form. Unknown and Restricted rows render with the SAME row height, label column and chip position as populated rows; the value cell renders a hatched or dotted fill (never empty whitespace, never an em-dash alone) plus a reason string in the secondary type step ('Restricted — clearance', 'Unknown — no collection on this entity'). Restricted additionally renders a dis
  REC[PROVENANCE RULE; EXPLORE/PLAN/COMMAND co] Make the provenance tag a CONTAINER treatment, not a badge. Any AI-generated advisory text renders inside a container with: a 3px left rule, a distinct surface token (surface.advisory), proportional (non-tabular) figures per the numeric rule, a persistent header chip reading 'Advisory — generated, not computed', a forced-colors fallback of a full 1px border, and a visually-hidden prefix in the acc
  REC[DESIGN-TOKENS document; theming; VISUAL-] Author tokens in DTCG JSON as the single source of truth, in three enforced tiers: (1) primitive — colour.neutral.900, never referenced by a component; (2) semantic — surface.raised, text.secondary, border.confidence.disputed, motion.duration.quick, all defined as {alias} references to tier 1; (3) component — dossierRow.background, aliasing tier 2 only. Build to CSS custom properties. Enforce with
  REC[MOTION-SYSTEM document; cinematic reserv] Ship a four-value duration scale and two easing registers, and bind them semantically. Durations: instant 0ms (state toggles that must feel like direct manipulation, or ≤100ms feedback), quick 160ms (panel/chip/tooltip state), staged 240ms (overlay and dossier entry/exit), emphatic 400ms (RESERVED — reserved-moment transitions only, requires named justification in the screen spec). Hard ceiling 40
  REC[MOTION-SYSTEM; the eight questions (esp.] Make motion mean causality and nothing else. Permitted uses, exhaustively: (a) a value changed — a 160ms delta chip appears adjacent to the number, the digits themselves do NOT animate; (b) causal propagation — when a consequence primitive resolves, affected entities/panels flash a 160ms border-emphasis in dependency order, so the player literally watches the causal chain; (c) enter/exit of overla
  PITFALLS: NEVER animate the digits of an authoritative value. Count-up/roulette/odometer transitions on engine-computed numbers make the value unreadable during the transition, imply a continuity the discrete simulation tick does not have, and directly contradict the determinism boundary by making a stepwise state change look like a smooth physical process. Show the new value instantly; animate a delta chip beside it if anything.; NEVER let colour be the sole carrier of a confidence label, an affiliation, a provenance tag, or an alert level. This fails WCAG 1.4.1 at Level A, fails for the ~8% of men with colour-vision deficiency, and fails completely under forced-colors mode where all author colours a
## Frontend technical feasibility for MERIDIAN: framework, packaging, live-state transport, map/graph/table/chart rendering, tokens, testing, performance budgets, and honest fixture-driven development
SUMMARY: METHOD NOTE AND LIMITATION, STATED UP FRONT BECAUSE THIS PROJECT'S DEFINING DEFECT IS OVERCLAIMING: the WebSearch budget for this session was already exhausted (200/200) before I issued a single query. I therefore could not run the 5-8 exploratory searches the brief requires. Instead I fetched 16+ primary sources directly by known URL — official docs and specs (deck.gl, MapLibre, sigma.js, Cytoscape.js, cosmos.gl, TanStack Virtual/Query, uPlot, Radix, Melt UI, Storybook, MSW, Style Dictionary, DTCG, Playwright, MDN SSE, FastAPI, Tauri, AG Grid, WCAG, web.dev INP, js-framework-benchmark). Every number and quotation below is traceable to one of those fetches. Where I could NOT verify something this session — most importantly the React-vs-Svelte runtime performance ranking, MapLibre's gzip bu
  FIND(well-established) deck.gl natively supports non-geographic rendering: the 'cartesian' coordinate system is "the default coordinate system when rendering into non-geospatial views", used with OrthographicView or OrbitView. | WHY: MERIDIAN's world is fictional, so an abstract non-geographic map is technically viable and does not require faking latitude/longitude. This is a real fork in the road: geographic-with-authored-GeoJSON versus abstract-car
  FIND(well-established) MapLibre can render a complete map with no tile server at all: the geojson source accepts "A URL to a GeoJSON file, or inline GeoJSON", and the image source positions a static image via a coordinates property of four [longitude, latitude] corner pairs. | WHY: This is the decisive feasibility fact for MERIDIAN's map. A fictional country can ship as a versioned GeoJSON scenario asset with zero external network dependency, zero attribution obligations, and zero risk of real-worl
  FIND(well-established) Server-Sent Events are strictly unidirectional, auto-reconnect by default, and support resume via Last-Event-ID; but over HTTP/1.1 the browser allows only 6 concurrent connections per browser+domain across all tabs, rising to a negotiated default of ~100 streams over HTTP/2. | WHY: MERIDIAN's tick/event feed is exactly a server-to-client broadcast, so SSE is the honest fit and its Last-Event-ID cursor maps directly onto the event-sourced replay foundation P0.6 is meant to build. The 6-connection li
  FIND(well-established) FastAPI supports WebSockets via @app.websocket() with Depends/Cookie/Header/Query, but its own documentation warns that in-memory broadcast "will only work while the process is running, and will only work with a single process" and points to encode/broadcaster with Redis/PostgreS | WHY: Both SSE and WebSocket fan-out inherit this constraint. A single-process uvicorn is fine for the interview demo and for a solo product, but MERIDIAN should not document 'realtime' as a delivered property without stating 
  FIND(well-established) Honest graph-rendering ceilings differ by an order of magnitude per technology: sigma.js targets "graphs of thousands of nodes and edges" via WebGL and its own FAQ recommends D3 instead for "a few hundreds of nodes and edges" when custom rendering matters; cosmos.gl claims "hundr | WHY: MERIDIAN's relationship view is a differentiator, so the temptation is to render everything. The library choice should follow a design cap, not the other way round: pick the cap first, then the cheapest library that clea
  FIND(contested) Cytoscape.js's documented performance flags — hideEdgesOnViewport, textureOnViewport, motionBlur, motionBlurOpacity — are described by its own docs as "now largely moot, as a result of performance enhancements", and the docs do not state a WebGL renderer. | WHY: Cytoscape.js is the default suggestion for graph work and carries a strong biology-network heritage and look. For MERIDIAN it offers rich layout/selector features but no verified WebGL path, so it is not the right choice
  FIND(well-established) The DTCG design token format is explicitly a preview draft that says of itself: "Do not attempt to implement this version of the specification. Do not reference this version as authoritative in any way." It defines $value (mandatory), $type, $description, $deprecated, $extensions | WHY: MERIDIAN must adopt the DTCG shape without claiming DTCG compliance. Claiming conformance to a spec that disclaims its own authority would be a textbook instance of the exact documentation defect the project exists to co
  FIND(well-established) Style Dictionary is "forward-compatible with Design Tokens Community Group spec" and compiles token JSON to CSS custom properties, JS, iOS, Android and documentation outputs. | WHY: Gives MERIDIAN a one-source-of-truth token pipeline that survives a later native or design-tool integration, without betting on the unstable spec.
  FIND(well-established) Radix Primitives ship "without styles, giving you complete control over the look and feel", adhere to "the WAI-ARIA design patterns where possible", and handle "aria and role attributes, focus management, and keyboard navigation" — but the documentation is React-centric with pack | WHY: This is the strongest single argument for React in MERIDIAN's case. The project has a hard no-trade-dress rule AND a non-optional accessibility rule; headless-plus-own-tokens is the only combination that satisfies both, 
  FIND(contested) The leading Svelte headless equivalent, Melt UI, is still pre-1.0: latest release v0.86.6 dated 28 March 2025, 197 releases, README states the project is "under active development". | WHY: This is the concrete, checkable form of the 'ecosystem tax' argument against Svelte for MERIDIAN. A pre-1.0 dependency underneath a mandatory accessibility guarantee is a real risk for a solo developer with no time to ma
  REC[Framework choice; VISUAL-CONSTITUTION an] Adopt React 19 + TypeScript (strict) + Vite, built on Radix headless primitives with 100% of visual identity coming from your own tokens. Record the decision in an ADR that states plainly that Svelte would likely produce a smaller and faster runtime and that the choice is being made for ecosystem and accessibility-primitive maturity instead — do not claim React is faster. If you copy shadcn/ui com
  REC[Packaging decision; repo capability clai] Ship browser-only in Horizon 1. Serve the built SPA from the existing FastAPI process via StaticFiles at /app. Defer Tauri and reject Electron outright. Keep the Tauri path cheap by banning three things now: any Node-only API in browser code, any Chromium-only CSS feature without a tested fallback, and any assumption of a filesystem.
  REC[Live simulation feed; C1 live state conn] Use one EventSource for the simulation feed and ordinary POST for every command. Serve it over HTTP/2 in any deployment, or hold strictly one EventSource per tab. Define the envelope now: id = monotonic engine event sequence number, event ∈ {tick, causal_event, advisory, run_status}, data carries { snapshot_version, event_id, causal_parent_ids }. Treat the SSE id field as the replay cursor and rec
  REC[ADR-006 determinism boundary; PROVENANCE] Enforce the never-authoritative-client rule in types and in one test. Define Sourced<T> = { readonly value: T; readonly provenance: 'engine' | 'fixture' | 'advisory'; readonly asOfVersion: number } and forbid any component from consuming a bare network-derived value. Split state into exactly three stores with different mutability contracts: SnapshotStore (deep-frozen in dev via Object.freeze, repl
  REC[COMMAND mode of the EXPLORE/PLAN/COMMAND] Ban optimistic updates for anything the engine prices, in a written rule and a lint check. A proposed action renders in exactly four states — DRAFTED (client-only), SUBMITTED (POST in flight), PRICED (engine returned cost/feasibility/resistance), RESOLVED (engine applied it at snapshot_version N). DRAFTED and SUBMITTED must be visually distinct from PRICED and RESOLVED and must never display an en
  REC[Map rendering; B5 fictional-world techni] Render the map with MapLibre GL JS driven by inline/authored GeoJSON, with no tile server, no basemap and no attribution dependency. Author the fictional country in real WGS84 coordinates placed inside an open-ocean region so projection maths stays valid, ship it as world.geojson under scenario versioning, and use an image source with its four-corner coordinates array only if hand-painted terrain 
  REC[C0 visual proof; Kestral Strait scenario] Treat map content authoring as a first-class scheduled work item with its own budget, not an incidental asset. Specify the minimum viable fictional geography explicitly: one coastline polygon, 5-8 administrative regions, 3-5 ports or cities, one strait or chokepoint feature, and a shipping-lane linestring set. Author it in a GeoJSON editor and commit it as scenario data, never as code, consistent 
  REC[Relationships tab of the ten-tab dossier] Design the relationship view as a capped ego-network before choosing a library: depth ≤ 2 hops, hard cap of ~150 visible nodes, everything beyond demoted to a ranked virtualised table with an explicit 'N further relationships not shown' row. Implement with d3-force rendered to canvas. Render every edge as a directed arc with a visible arrowhead, and allow the two directions of a pair to be separat
  REC[Entity lists; the ten dossier tabs; the ] Use TanStack Virtual with your own markup for every list over 100 rows; do not buy AG Grid. Fix row height at a token-derived constant (e.g. 32px) so estimateSize is exact and the scrollbar does not jitter, use overscan of 5, and — critically — make the virtualiser's item count the count of FIELDS, not the count of KNOWN fields.
  REC[Macro indicator panels; the eight questi] Split charting in two. Use uPlot for long macro-indicator time series and the event-volume feed. Hand-build the small number of explanatory charts — the eight-questions panel, before/after state, alternative outcomes — as bespoke SVG components driven by design tokens. Pair every canvas chart with a visually-hidden but screen-reader-available data table.
  REC[DESIGN-TOKENS document in the unwritten ] Adopt the DTCG token SHAPE ($value, $type, $description, {group.token} brace aliases) in a tokens/ directory compiled by Style Dictionary to CSS custom properties — but pin your own internal version number and state in the repo that MERIDIAN follows the DTCG draft's structure and does NOT claim conformance. Add a lint rule banning raw hex, raw px and raw ms literals in component files.
  REC[Eight confidence labels; visibility-vers] Specify every confidence and provenance chip to a fixed, testable contract: text label, PLUS a distinct glyph, PLUS a distinct border treatment (solid / dashed / dotted / double / none), never colour alone; minimum 11px; 4.5:1 text contrast against its own background; and for the eight confidence labels the border treatment must remain distinguishable in a greyscale screenshot. Restricted and Unkn
  PITFALLS: I could not run any web searches this session — the 200-call budget was exhausted before my first query. Everything here rests on directly-fetched primary sources plus clearly-marked inference. The most significant consequence is that the React-versus-Svelte performance comparison is UNVERIFIED: I confirmed only what js-framework-benchmark measures (row create/replace/partial-update/select/swap/remove/append/clear, plus memory and Lighthouse startup, using a weighted geometric mean since Chrome 118), not any current ranking. Re-run that check before writing the framework ADR.; Optimistic UI is the default frontend reflex and it directly violates ADR-006 here. An optimistically-rendered cost,